---
title: "Caso 1 · ¿Ocurre el evento? ¿Y cuándo?"
subtitle: "Respuestas binarias, politómicas y tiempo hasta el evento — Modelos Lineales Generalizados con R"
author: "A.M.Mayoral, con asistencia de Claude Opus 4.8"
date: today
lang: es
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: false      # numeración MANUAL (1.1, 1.2, ...) en los títulos
    code-fold: show
    code-tools: 
      source: false # quita "View source"; no se expone el qmd
      toggle: true # mantiene show all/hide all code
    theme: cosmo
    df-print: paged
  pdf:
    toc: true
    number-sections: false
bibliography: ../references.bib
nocite: |
  @*
csl: https://www.zotero.org/styles/apa 
execute:
  warning: false
  message: false
editor: 
  markdown: 
    wrap: 72
---

```{r}
#| label: setup
#| include: false

# Núcleo de software (cada unidad carga además lo suyo: lme4, nnet, ordinal,
# pROC, performance, DHARMa, marginaleffects, car, survival...).

library(broom)
library(aplore3)
library(patchwork)
library(see)
library(DHARMa)
library(arm)
library(performance)
library(tidyverse)
library(MuMIn)
library(readr)
library(GGally)

SEMILLA_CURSO <- 20252026L
set.seed(SEMILLA_CURSO)
theme_set(theme_minimal(base_size = 12))
```


::: callout-note
## Mapa del caso

En esta unidad vamos a trabajar sobre respuestas categóricas. Iniciamos
con respuesta binaria, seguimos con su agrupación natural en respuestas
binomiales, y extendemos a respuesta politómica y multinomial. Por el
camino incorporamos los efectos aleatorios en la modelización, y por
último estudiamos también el abordaje como un problema de supervivencia
para ir más allá de la predición del evento, incorporando datos
censurados y prediciendo también cuándo se va a producir el evento.

Trabajaremos con la base de datos GLOW y usaremos la cohorte simulada
para el estudio de supervivencia y el estudio de caso sobre el que
trabajarás tú estudiante, para demostrar que has aprendido a modelizar
datos discretos.

| Unidad | Contenido |
|-----|------------------------------------|
| **1.1** | Cuando la recta no llega: OLS falla en binaria; tres componentes del GLM; tipología de respuestas; primer `glm`. <br> Objetivos: RA1 (GLM vs OLS) |
| **1.2** | Formulación de los GLM: familia exponencial, enlace, IWLS, *deviance*; inferencia; interpretación (OR, efectos marginales); evaluación y comparación de clasificadores.<br> Objetivos: RA2 (ajuste) |
| **1.3** | Respuesta categórica generalizada: binomial agrupado (ajuste y bondad de ajuste con χ²) y respuestas politómicas — multinomial nominal y ordinal (odds proporcionales). <br> Objetivos: RA3 (interpretación en la escala correcta), <br> RA4 (diagnóstico, separación, calibración), <br> RA5 (comparación de modelos) |
| **1.4** | Efectos aleatorios (GLMM logístico, intercepto aleatorio); reactivación del ordinal mixto. <br> Objetivos: RA6 (efectos mixtos) |
| **1.5** | Supervivencia: función de supervivencia, hazard, riesgos proporcionales, Cox; el tiempo discreto como GLM (`cloglog` persona-periodo). <br> Objetivos: RA7 (censura y tiempo-a-evento como  casos del marco) |
| **1.6** | Estudio de caso, exposiciones y evaluación (actividades, instrucciones, rúbricas).<br>Objetivos: RA8 (estudio de caso reproducible) |
:::

# Presentación del caso {#sec-presentacion}

Iniciamos pues la unidad, presentando los bancos de datos con los que
vamos a trabajar. La pregunta que vertebra el caso tiene dos
tiempos básicos: 

1. **«¿Ocurre el evento?»**: una respuesta binaria que motiva
el marco GLM y la regresión logística. 
2. **«¿Cuándo?»**: el mismo evento como **tiempo hasta que ocurre**, con **censura**, que se
reformula como un GLM sobre datos persona-periodo.

En el recorrido, surgen dos matices que también trataremos: 

- los sujetos vienen **agrupados** (por centro), e introducimos los **efectos aleatorios** y los **modelos mixtos**, 
- y la respuesta puede tener **más de dos categorías**, motivando la extensión politómica, nominal u ordinal.

Para trabajar todas estas cuestiones utilizaremos dos fuentes de datos con
papeles distintos: un conjunto **real** para el ejemplo logístico, y una
**cohorte simulada** con proceso generador conocido, que vertebra la
extensión de la modelización a la supervivencia.

## Dato real — GLOW {#sec-datos-glow}

`glow500` procede del estudio **GLOW** (*Global Longitudinal Study of
Osteoporosis in Women*) y se distribuye en el paquete `aplore3`
(licencia GPL-3), compañero del manual de @hosmer2013. Es una muestra de
**500 mujeres** seguidas un año en **6 centros**. La respuesta es
binaria —`fracture`: alguna fractura en el primer año— y el resto son
factores de riesgo basales:

| Variable | Descripción | Tipo |
|------------------------|------------------------------------------------|----------|
| `fracture` | Fractura en el primer año (No/Yes) — **respuesta** | Binaria |
| `age` | Edad al ingreso (años) | Continua |
| `weight` | Peso al ingreso (kg) | Continua |
| `height` | Estatura al ingreso (cm) | Continua |
| `bmi` | Índice de masa corporal (kg/m²) | Continua |
| `priorfrac` | Antecedente de fractura previa (No/Yes) | Binaria |
| `premeno` | Menopausia antes de los 45 (No/Yes) | Binaria |
| `momfrac` | Fractura de cadera en la madre (No/Yes) | Binaria |
| `armassist` | Necesita los brazos para levantarse de una silla (No/Yes) | Binaria |
| `smoke` | Fumadora actual o pasada (No/Yes) | Binaria |
| `raterisk` | Riesgo de fractura autopercibido (Less/Same/Greater) | Ordinal (3) |
| `fracscore` | Puntuación compuesta de riesgo de fractura | Discreta |
| `sub_id`, `site_id`, `phy_id` | Identificadores de paciente, centro y médico | ID |

Creamos también una versión 0/1 de la respuesta `fracture` para el
experimento, que utilizaremos en esta unidad.


```{r}
#| label: carga-datos
data(glow500)
glow <- glow500 |>
  as_tibble() |>
  mutate(fractura01 = as.integer(fracture) - 1L)   # No -> 0, Yes -> 1

glow |> count(fracture) |> mutate(prop = round(n / sum(n), 3))
```


Visualizamos en la @glow-eda los datos y las relaciones entre ellos,
diferenciando por color entre las pacientes que han sufrido fractura y
las que no.


```{r}
#| label: glow-eda
#| fig-width: 10
#| fig-height: 10
#| out-width: "100%"
#| fig-cap: "Descripción de GLOW. En la diagonal distribución de las variables, diferenciadas en color por `evento`. En los cruces la relación entre ellas."

# Covariables sustantivas de GLOW (dejo fuera los identificadores)
vars <- c("age", "weight", "height", "bmi", "fracscore",   # continuas / discretas
          "priorfrac", "premeno", "momfrac",               # factores de riesgo binarios
          "armassist", "smoke", "raterisk")                # binarios + ordinal

glow |>
  select(fracture, all_of(vars)) |>
  ggpairs(
    columns = vars,
    mapping = aes(color = fracture, alpha = 0.6),
    legend=1,
    upper = list(continuous = wrap("cor", size = 2.3),     # correlaciones (global y por grupo)
                 combo      = wrap("box_no_facet"),         # continua ~ factor: boxplots
                 discrete   = wrap("count")),               # factor ~ factor: recuento
    lower = list(continuous = wrap("points", alpha = 0.25, size = 0.4),
                 combo      = wrap("facethist", bins = 20),
                 discrete   = wrap("facetbar")),
    diag  = list(continuous = wrap("densityDiag", alpha = 0.5),
                 discrete   = wrap("barDiag")),
    progress = FALSE
  ) +
  theme_bw(base_size = 7) +
  theme(strip.text = element_text(size = 6))+
  theme(legend.position = "bottom")
```


## Cohorte simulada {#sec-datos-cohorte}

Está inspirada en el problema de GLOW y simula el seguimiento de las pacientes en el
tiempo a lo largo de 6 años, para una cohorte de mujeres, repartidas en
varias clínicas (24).

El simulador `simular_cohorte()` está disponible en `R/dgp_cohorte.R` y parte de unas
condiciones establecidas sobre las relaciones entre las variables consideradas 
(recuperables en `str(attr(cohorte, "verdad"))`).

Cada paciente acude a **revisiones periódicas anuales** —seis como
máximo— y en cada una se constata si **ha sufrido una fractura desde la
revisión anterior**. No observamos el **instante exacto** de la
fractura, solo el **periodo** en que se detecta: por eso **el tiempo es
discreto**. Así, `tiempo` $\in\{1,\dots,6\}$ representa el periodo en
que **termina la observación** de cada mujer —bien porque se le detecta
una fractura (`evento` = 1) o porque el seguimiento acaba sin ella
(`evento` = 0, **censura**)—. `tiempo` = 1 significa que el desenlace
ocurre en la **primera** revisión; `tiempo` = 6, que llega a la **sexta
y última**. La expansión **persona-periodo** convierte a cada mujer en
una fila por cada revisión a la que llega sin fractura, con `y` = 1 solo
en aquella donde se detecta.

El simulador utiliza dos predictores `x1` y `x2` abstractos, que podrían
representar distintas características; a lo largo del curso **adoptamos
una caracterización fija** para facilitar la interpretación:

- `x1` = **fragilidad ósea** (a partir de la densidad mineral,
  estandarizada en puntuación z): factor de riesgo **continuo**; a mayor
  valor, hueso más frágil y **más** riesgo de fractura ($\beta_1>0$).
- `x2` = **tratamiento** preventivo (1 = lo recibe, 0 = no): factor
  **binario** y **protector**; reduce el riesgo ($\beta_2<0$).
- `centro` = la **clínica** (24 en total); su intercepto aleatorio
  recoge que unas clínicas tienen más riesgo basal que otras —la
  *fragilidad* estadística / efecto mixto—.

Las variables simuladas son:

| Variable | Significado (lectura adoptada) | Tipo | Se usa en |
|-----------|---------------------------------|---------------|-------|
| `id` | Identificador de la paciente | — | — |
| `centro` | **Clínica** (grupo del efecto aleatorio / fragilidad) | Factor (24 niveles) | 1.4, 1.5 |
| `x1` | **Fragilidad ósea** (baja densidad mineral, estandarizada) | Continua | 1.4, 1.5 |
| `x2` | **Tratamiento** preventivo (1 = sí, 0 = no) | Binaria | 1.4, 1.5 |
| `tiempo` | **Periodo de revisión** (1–6) en que termina la observación | Entero | 1.5 |
| `evento` | 1 = fractura **observada** en ese periodo; 0 = **censura** | Binaria | 1.5 |
| `ever` | ¿Hubo fractura durante el seguimiento? (= `evento`) | Binaria | 1.4 |
| `sever_ord` | **Severidad basal ordinal** de la osteoporosis (Leve \< Moderado \< Grave) | Factor ordenado (3) | 1.4 |
| `clase_nom` | **Endotipo nominal** de fragilidad ósea A/B/C (sin orden) — ilustra el contraste; el multinomial del curso se ajusta sobre GLOW (`raterisk`) | Factor (3) | — |

Las respuestas se generan a partir de un modelo de **riesgo en tiempo
discreto** con enlace `cloglog` (lo estudiaremos más adelante):
$$\operatorname{cloglog}(h_{ijt})=\alpha_t+\beta_1\,x_{1i}+\beta_2\,x_{2i}+u_j,
\qquad u_j\sim\mathcal{N}(0,\sigma_u^2),$$
con **intercepto aleatorio por clínica** $u_j$ (fragilidad) y **censura
administrativa no informativa**. De ahí salen la respuesta binaria
«¿hubo fractura?» y el reloj de supervivencia (en qué periodo se
produjo). Construimos también una variable de **severidad ordinal**
(`sever_ord`), que se genera aparte, con un cumulative logit más
intercepto aleatorio por clínica, y la usa el ordinal mixto de 1.4. Las
respuestas categóricas son **independientes** del reloj de supervivencia
—comparten covariables y clínicas, pero no se derivan de si hubo
fractura, ni de cuándo la hubo—, una decisión de diseño para no entrar
en riesgos competitivos (fuera del alcance de esta unidad).

**Las dos respuestas categóricas, en clave clínica.**

Aunque comparten las covariables `x1` y `x2` con el reloj de
supervivencia, `sever_ord` y `clase_nom` cuentan historias clínicas
distintas y se generan con estructuras distintas; conviene fijar su
lectura para interpretarlas con soltura.

- `sever_ord` es una **gradación de severidad**: un único eje latente
  ordena los tres niveles —el supuesto de *odds proporcionales*— y las
  clínicas difieren en su reparto basal (intercepto aleatorio por
  centro). Admite dos lecturas equivalentes: como **estadio
  densitométrico de la osteoporosis** (Leve \< Moderado \< Grave ≈
  osteopenia \< osteoporosis \< osteoporosis establecida) o como el
  **grado semicuantitativo de fractura vertebral de Genant** (grados 1
  \< 2 \< 3), que la literatura etiqueta literalmente
  leve/moderado/grave. Nos quedamos con la primera: "estadio
  densitométrico de la osteoporosis". Las hipótesis del simulador, **en
  tendencia**: a **mayor fragilidad ósea** (`x1`) la severidad tiende a
  **desplazarse hacia grados más altos**; el **tratamiento** (`x2`) se
  asocia a grados **más leves** (lectura protectora); y, a igualdad de
  paciente, **unas clínicas tienden a registrar severidades mayores que
  otras** (distinto *case-mix* o criterio del lector). La relación con
  el tratamiento es **asociativa**, no causal.

- `clase_nom` es, en cambio, un **fenotipo cualitativo sin orden**: cada
  categoría sigue su propio patrón —efectos específicos de categoría— y
  la clínica **no** interviene (sin efecto aleatorio), de modo que es un
  rasgo del **paciente**. La leemos como un **endotipo de fragilidad
  ósea** con referencia **A** (baja densidad sin etiología secundaria),
  un fenotipo **B** de **caídas / fragilidad musculoesquelética** y un
  fenotipo **C** de **osteoporosis secundaria** (corticoidea o
  endocrina). En tendencia: a **mayor fragilidad ósea** (`x1`), más
  probable **alejarse de la referencia** hacia B y, sobre todo, hacia C;
  y el **tratamiento** (`x2`) **separa** ambos fenotipos —tiende a
  asociarse **menos** con el de caídas (que se maneja con prevención de
  caídas) y **más** con la osteoporosis secundaria (donde la profilaxis
  ósea casi se protocoliza)—. Como antes, se trata de una asociación de
  **indicación/selección**, no de causalidad.

**Simulación de los datos** Aplicamos el simulador generado y
construimos la `cohorte` completa.


```{r}
#| label: cohorte-sim
#| 
library(readr)
source("R/dgp_cohorte.R")                        # define simular_cohorte(), expandir_persona_periodo()
cohorte <- simular_cohorte()                     # nivel individuo (binaria, nominal, ordinal)
# y guardamos las simulaciones
#write_csv(cohorte, "cohorte.csv")        # nivel individuo
# resumen
glimpse(cohorte)
head(cohorte)
```


Más tarde, para trabajar la supervivencia con un modelo logístico (1.5),
habremos de utilizar su versión binaria, en la que desglosamos por filas
la respuesta de cada paciente en cada uno de los seis periodos de tiempo
observados durante el seguimiento, identificando con  (0/1) la ausencia/presencia 
de fractura en cada periodo. Este banco de datos desglosado por periodo está
disponible en `pp`:


```{r}
# transformamos la cohorte simulada a datos binarios
pp      <- expandir_persona_periodo(cohorte)     # persona-periodo (supervivencia, 1.5)
#write_csv(pp,      "cohorte_pp.csv")     # formato persona-periodo
head(pp,6)
```


Visualizamos los datos simulados en la @cohorte-eda, y la relación entre los centros en la @cohorte-centro.


```{r}
#| label: cohorte-eda
#| fig-width: 10
#| fig-height: 10
#| out-width: "100%"
#| fig-cap: "Descripción de `cohorte`. En la diagonal distribución de las variables, diferenciadas en color por `evento`. En los cruces la relación entre ellas."

# Preparo factores legibles (x2, tiempo y el desenlace como categóricos)
cohorte_eda <- cohorte |>
  mutate(
    evento = factor(evento, levels = c(0, 1), labels = c("Sin fractura", "Fractura")),
    x2     = factor(x2,     levels = c(0, 1), labels = c("No", "Sí")),
    tiempo = factor(tiempo, levels = 1:6)
  )

# Variables sustantivas (fuera 'id', 'centro' y 'ever' = duplicado de 'evento')
vars <- c("x1", "x2", "tiempo", "clase_nom", "sever_ord")

cohorte_eda |>
  select(evento, all_of(vars)) |>
  ggpairs(
    columns = vars,
    mapping = aes(color = evento, alpha = 0.6),
    legend=1,
    upper = list(continuous = wrap("cor", size = 2.5),
                 combo      = wrap("box_no_facet"),
                 discrete   = wrap("count")),
    lower = list(continuous = wrap("points", alpha = 0.25, size = 0.4),
                 combo      = wrap("facethist", bins = 20),
                 discrete   = wrap("facetbar")),
    diag  = list(continuous = wrap("densityDiag", alpha = 0.5),
                 discrete   = wrap("barDiag")),
    progress = FALSE
  ) +
  theme_bw(base_size = 8)+
  theme(legend.position = "bottom")
```

```{r}
#| label: cohorte-centro
# 'centro' (24 niveles) no cabe en la matriz: se ve mejor como tasa de evento por clínica
cohorte |>
  group_by(centro) |>
  summarise(tasa_fractura = mean(evento), n = n(), .groups = "drop") |>
  ggplot(aes(reorder(centro, tasa_fractura), tasa_fractura)) +
  geom_col(aes(fill = n)) +
  coord_flip() +
  labs(x = "Centro (ordenado por tasa)", y = "Proporción de fractura",
       fill = "n", title = "Variación entre clínicas")
```


<!-- ===================================================================== -->

<!--  UNIDADES: cada una en su propio fichero parcial (_unidad_1_N.qmd).    -->

<!-- ===================================================================== -->

<!-- Partial: incluir con {{< include _unidad_1_1.qmd >}}. Sin YAML. Hereda el
     entorno de la madre (glow, fractura01, cohorte, pp). Chunks prefijados u11-. -->

# 1 · Cuando la recta no llega: del dato binario al marco GLM {#sec-u11}

Iniciamos nuestro curso descubriendo las limitaciones del modelo lineal cuando la respuesta ni es normal, ni hay modo de transformarla para hacerla lineal, como ocurre cuando tenemos una variable dicotómica por predecir. Nos apoyamos en la base de datos GLOW para ilustrar dónde falla el modelo lineal, y mostrar las bondades que reporta un modelo lineal generalizado, en el que se tiene en cuenta la distribución adecuada para la respuesta, y se busca una transformación del predictor lineal que pueda generar predicciones viables sin salir de un modelo lineal.
Comparamos en esta sección, los resultados de un modelo lineal y los de un modelo lineal generalizado, para fundamentar el desarrollo de este modelo en la siguiente sección.

::: {.callout-note}
## Sobre esta unidad
**RA implicados:** RA1 y, en su primer
contacto, RA2.
:::

## 1.1 El modelo lineal en GLOW

La primera tarea antes de proponer una modelización, pasa por observar el comportamiento de los datos. Nos interesan, de momento, las variables `fracture`, que actúa como respuesta e indica si la paciente ha sufrido una fractura, `age`, con la edad de la paciente, y `priorfrac`, que indica si tiene antecedentes de haber sufrido alguna fractura previa. Indagamos pues, cómo se relaciona la respuesta con la edad, distinguiendo por antecedente de fractura previa (en la @fig-glow-eda-box):

```{r}
#| label: fig-glow-eda-box
#| fig-width: 9
#| fig-height: 4
#| out-width: "100%"
#| fig-cap: "Distribución de la edad según haya habido o no fractura en el primer año, condicionada por el antecedente de fractura previa (priorfrac)."
glow |>
  ggplot(aes(x = fracture, y = age, fill = fracture)) +
  geom_boxplot(alpha = 0.75, outlier.alpha = 0.3) +
  facet_wrap(~ priorfrac,
             labeller = labeller(priorfrac = c(No = "Sin fractura previa",
                                                Yes = "Con fractura previa"))) +
  labs(x = "Fractura en el primer año", y = "Edad (años)") +
  guides(fill = "none")
```

Observamos que las mujeres que **sí** fracturan son, en mediana, **algo mayores** que las que no, en los dos
paneles: la edad es un **factor de riesgo**. Pero la separación es **modesta** y las cajas se
**solapan** bastante —la edad por sí sola no discrimina bien, de ahí que el efecto
lineal de la edad resultará débil, como veremos más tarde—. Además, el **antecedente de fractura previa** (panel derecho)
eleva el riesgo por sí mismo —hay fracturas a cualquier edad—, así que conviene un modelo con
**ambas** covariables, no una recta única sobre la edad.

Tiene sentido pues, plantear un modelo predictivo para `fracture` en función de `age` y `priorfrac`. Para intentar capturar el efecto de la edad (`age`) sobre la probabilidad de fractura (y valorar), agrupamos la edad por franjas y calculamos la proporción de fracturas en cada franja,  diferenciando por el hecho de que la paciente hubiera sufrido o no, una fractura previa (`priorfrac`). En la @fig-u11-eda tenemos esta representación, y superponemos sendas rectas, que transmiten una considerable variabilidad de los puntos en torno a ellas, especialmente en las pacientes que han sufrido fractura.

```{r}
#| label: fig-u11-eda
#| fig-cap: "Proporción observada de fractura por tramos de edad y antecedente previo, con sendas rectas de regresión por grupo: el ajuste lineal es pobre y apenas captura tendencia."
glow |>
  mutate(edad_grupo = cut(age, breaks = seq(55, 95, by = 5))) |>
  filter(!is.na(edad_grupo)) |>
  group_by(edad_grupo, priorfrac) |>
  summarise(age = mean(age), p_fractura = mean(fractura01), n = n(), .groups = "drop") |>
  ggplot(aes(age, p_fractura, color = priorfrac)) +
  #geom_line() + 
  geom_point(aes(size = n)) +
  geom_smooth(aes(weight = n), method = "lm", se = FALSE, linewidth = 1) +
  labs(x = "Edad (media del tramo)", y = "Proporción de fractura",
       color = "Fractura previa", size = "n")
```

Planteamos pues, una primera aproximación a través de un modelo lineal (sin interacción) en el que la respuesta ha de ser numérica, por lo que consideramos la recodificación de `fracture` con valores 0/1 -`fracture01`-, y los predictores son `age` y `priorfrac`.

```
fit_ols = lm(fractura01 ~ age + priorfrac, data = glow)
```

Y comenzamos a identificar los problemas que van apareciendo. 

En primer lugar, un modelo lineal no garantiza una predicción de la respuesta dentro de un rango, y en particular como en este caso, dentro del rango $(0,1)$ de probabilidades. Aunque para el tramo observado de edad el modelo lineal se comporta bien a este respecto, dando predicciones dentro del rango $(0,1)$, vemos en la @fig-u11-ols-extrapola cómo la recta de ajuste da predicciones por debajo de 0 para edades inferiores a 40 años.

```{r}
#| label: fig-u11-ols-extrapola
#| fig-cap: "Predicción del modelo lineal extrapolada fuera del rango de edad observado (franja sombreada). Más allá de los datos, OLS predice probabilidades por debajo de 0: imposibles para una probabilidad."
fit_ols <- lm(fractura01 ~ age + priorfrac, data = glow)
rango <- range(glow$age)

grid_ext <- expand_grid(
  age       = seq(35, 110, length.out = 200),
  priorfrac = factor(c("No", "Yes"), levels = levels(glow$priorfrac)))
grid_ext$p_hat <- predict(fit_ols, newdata = grid_ext)

ggplot(grid_ext, aes(age, p_hat, color = priorfrac)) +
  annotate("rect", xmin = rango[1], xmax = rango[2], ymin = -Inf, ymax = Inf,
           alpha = 0.12, fill = "grey40") +
  geom_line(linewidth = 1) +
  geom_hline(yintercept = c(0, 1), linetype = "dashed") +
  labs(x = "Edad (rango extrapolado; sombreado = rango observado)",
       y = "Probabilidad predicha (OLS)", color = "Fractura previa")
```


Otro fallo se da en el diagnóstico de los residuos, como vemos en la @fig-u11-diagnostico-ols: a la izquierda apreciamos que los residuos, para un x dado, toman solo dos valores posibles, y se mueven con la media ajustada; se rompe la independencia de los residuos y la homocedasticidad. En el gráfico a la derecha se muestran los residuos versus los percentiles de una distribución normal, y de nuevo se aprecia la ruptura de la hipótesis de normalidad. Esto ya era previsible desde el inicio, puesto que la respuesta es Bernouilli y no Normal.

```{r}
#| label: fig-u11-diagnostico-ols
#| fig-width: 10
#| fig-height: 4
#| out-width: "100%"
#| fig-cap: "Diagnóstico del ajuste OLS sobre respuesta binaria. Izquierda: residuos frente a ajustados (no hay nube de puntos aleatoria en torno al cero). Derecha: distribución de los residuos frente a la Normal de igual media y desviación (fallo de normalidad)."
fit_ols <- lm(fractura01 ~ age + priorfrac, data = glow)

g_res <- tibble(ajustado = fitted(fit_ols),
                residuo  = glow$fractura01 - fitted(fit_ols),
                y = factor(glow$fractura01, labels = c("No fractura (y=0)", "Fractura (y=1)"))) |>
  ggplot(aes(ajustado, residuo, color = y)) +
  geom_point(alpha = 0.3) +
  geom_hline(yintercept = 0, linetype = "dashed") +
  labs(x = "Valores ajustados", y = "Residuo de respuesta (y − μ̂)",
       title = "Residuos vs ajustados (OLS)", color = NULL)+
  theme(legend.position="bottom")

g_qplot <- tibble(residuo = residuals(fit_ols)) |>
  ggplot(aes(sample = residuo)) +
  stat_qq(alpha = 0.4, size = 0.6) +
  stat_qq_line(color = "firebrick") +
  labs(x = "Cuantiles teóricos (Normal)", y = "Cuantiles de los residuos de OLS")

patchwork::wrap_plots(g_res, g_qplot, ncol = 2)
```

## 1.2 Por qué OLS no sirve para una respuesta binaria {#sec-u11-ols-falla}

El modelo OLS postula $Y = \beta_0 + \beta_1 x_1 + \dots + \varepsilon$ con $\varepsilon \sim
\mathcal{N}(0,\sigma^2)$. Como $Y\in\{0,1\}$, su esperanza condicional es
directamente la probabilidad de éxito, $\mathbb{E}[Y\mid x]=\Pr(Y=1\mid
x)=\pi(x)$: OLS estaría modelando una **probabilidad como función lineal**.
Cuatro problemas qurgen todos ellos críticos [@dobson2018, caps. 1–3; @fox2016, cap. 14]:

1. **Predicciones fuera de rango.** El predictor lineal recorre toda la recta
   real, pero una probabilidad vive en $[0,1]$. Para valores extremos de las
   covariables, OLS predice probabilidades **negativas o mayores que 1**.
2. **Varianza no constante.** Si $Y$ es Bernoulli con media $\pi(x)$, entonces
   $\operatorname{Var}(Y\mid x)=\pi(x)[1-\pi(x)]$: la varianza **depende de la
   media**. El supuesto de homocedasticidad se viola por construcción.
3. **Error no normal.** Fijado un $x$ concreto, $\pi(x)$ es una **constante** e
   $Y\in\{0,1\}$, así que $\varepsilon=Y-\pi(x)$ solo puede tomar **dos valores**:
   $-\pi(x)$ con probabilidad $1-\pi(x)$, o $1-\pi(x)$ con probabilidad $\pi(x)$.
   La distribución **condicional** del error (dado $x$) es de **dos puntos**, no
   una campana gaussiana. Al variar $x$ esos dos valores se desplazan —de ahí las
   dos bandas de residuos del laboratorio—, pero el supuesto de normalidad de OLS
   recae sobre la distribución condicional, que es la que aquí es falsa.
4. **Linealidad implausible.** OLS impone que el efecto de una covariable sobre
   la probabilidad sea **constante**: la misma pendiente $\beta$ tanto si $\pi$
   ronda $0{,}5$ como si ya está pegada a 0 o a 1. Eso no es creíble, porque en
   los extremos apenas queda margen para moverse: si una paciente tiene una
   probabilidad de fractura casi nula (o casi segura), un año más de edad casi no
   puede cambiarla; en cambio, en la zona central ese mismo año podría desplazar bastante la
   probabilidad. Lo razonable es entonces una relación **en forma de S**:
   pronunciada en el centro y **aplanada (saturada) en los extremos**. La inversa
   del logit dibuja exactamente esa curva y, de paso, mantiene $\pi$ dentro de
   $(0,1)$.
   

## 1.3 Los tres componentes de un GLM {#sec-u11-tres-componentes}


La alternativa que plantearon @nelder1972 unificó la regresión lineal, logística, de Poisson, etc., bajo una
estructura de **tres piezas**:

1. **Componente aleatoria.** $Y$ sigue una distribución de la **familia
   exponencial**. 
   
   >Para GLOW, $Y\mid x \sim \text{Bernoulli}(\pi(x))$. Esta pieza fija la relación media–varianza y absorbe los problemas 2 y 3.
   
2. **Componente sistemática.** Mantiene un **predictor lineal** $\eta=\mathbf{x}^\top
   \boldsymbol\beta$, lineal en los parámetros.
   
3. Introduce una **función de enlace** o *link* $g$, monótona y diferenciable, que liga la media con el predictor lineal, $g(\mu)=\eta$ y
   $\mu=g^{-1}(\eta)$. 
   
   >Para binaria, el enlace por defecto es el **logit**:
   $$
   g(\pi)=\log\frac{\pi}{1-\pi}, \qquad \pi=g^{-1}(\eta)=\frac{e^{\eta}}{1+e^{\eta}},
   $$
   que resuelve los problemas 1 y 4: el logit lleva $(0,1)$ a toda la recta real (nunca fuera de rango).


En la @fig-u11-logit-inversa se muestra cómo la función logit transforma una probabilidad cualquiera en el rango $[0,1]$ en un valor sobre toda la recta real. Su inversa, la sigmoide, a la izquierda, permite recuperar un valor real en un rango de probabilidades, y se utilizará para estimar la probabilidad a partir del predictor lineal.

```{r}
#| label: fig-u11-logit-inversa
#| fig-cap: "Función logit y sigmoide: las dos direcciones. El logit lleva (0,1) a toda la recta real; su inversa (la sigmoide) devuelve cualquier η a (0,1)."
library(patchwork)
g1 <- tibble(p = seq(0.001, 0.999, by = 0.001)) |>
  mutate(y = qlogis(p)) |>
  ggplot(aes(p, y)) +
  geom_line(linewidth = 1, color = "steelblue") +
  geom_hline(yintercept = 0, linetype = "dashed") +
  labs(title = "logit:\n estira (0,1) sobre la recta real",
       x = expression(pi), y = expression(log(pi/(1 - pi))))

g2 <- tibble(eta = seq(-6, 6, by = 0.05)) |>
  mutate(p = plogis(eta)) |>
  ggplot(aes(eta, p)) +
  geom_line(linewidth = 1, color = "firebrick") +
  geom_hline(yintercept = c(0, 1), linetype = "dotted") +
  labs(title = "sigmoide:\n devuelve el predictor a (0,1)",
       x = expression(eta), y = expression(e^eta/(1 + e^eta)))

g1 + g2
```

::: {.callout-important}
## OLS es un GLM
La regresión lineal es un modelo lineal generalizado con la familia Gaussiana y la función de enlace identidad. No
abandonamos OLS: lo integramos en los GLM. Por eso `lm(y ~ x)` y `glm(y ~ x)` se parecen
tanto.
:::

En esta unidad trabajaremos con:

| Tipo de respuesta | Ejemplo | Familia (por defecto) | Enlace canónico | Unidad |
|---|---|---|---|---|
| Binaria | fractura sí/no | Binomial | logit | Unidades [1](#sec-u11) y [2](#sec-u12) |
| Binomial | nº fracturas por edad | Binomial | logit | [Unidad 3](#sec-u13) |
| Politómica nominal | perfil A/B/C | Multinomial | logit (gen.) | [Unidad 3](#sec-u13)|
| Politómica ordinal | severidad Leve/Mod/Grave | Multinomial ordinal | logit acumulado | [Unidad 3](#sec-u13) |


## 1.4 GLM en datos binarios


Para modelizar con un modelo lineal generalizado la predicción de `fracture`, mantenemos el carácter dicotómico de la respuesta, y su modelización con una variable Bernouilli, con $p$ su valor esperado y la probabilidad de fractura. Buscamos pues, un modelo a través del que relacionar esta probabilidad con un predictor lineal construido en función de las variables `age` y `priorfrac`. Como hemos visto antes, la función `logit` nos permite manejar este cambio de escala de una probabilidad a toda la recta real, y por otro lado, la familia binomial integra a la distribución Bernouilli, por lo que planteamos lo que popularmente se conoce como **el modelo logit**, que aplicado a GLOW para predecir la fractura en función de la edad y el haber tenido una fractura previa, se formularía así:

$$y_i=\begin{cases} 1 \ \text{fractura, con probabilidad } \pi_i \\
0 \ \text{ no fractura, con probabilidad } 1-\pi_i
\end{cases} \ \rightarrow \ logit(\pi_i)=\beta_0 + \beta_1 \cdot \text{age}_i + \gamma_{\text{priorfrac}}\text{priorfrac}_i, \qquad i= 1, ..., n$$  


donde $\text{age}_i$ es la edad de la paciente $i$, y $\text{priorfrac}_i=1$ si la paciente tuvo una fractura previa y 0 en caso contrario.

:::: {.callout-tip}
## 🔧 En R. El `glm()` con datos binarios

La función `glm(y ~ x1+x2+x1:x2+...,family=binomial(link = "logit"),data)` ajusta un modelo lineal generalizado en R, integrando los tres componentes del GLM:

- **Respuesta (componente aleatoria):** Si se trata de una respuesta binaria, esta puede venir etiquetada (No/Yes) o recodificada (0/1), siempre vinculada a la familia binomial → `family = binomial`. La categoría de **"éxito"** -y esto es relevante porque define la probabilidad que se estima- siempre es "1" en la codificación 0/1, y la que alfabéticamente esté posicionada la última (en No/Yes, sería Yes, pues se asume No=0/Yes=1).
- **Predictor lineal (componente sistemática):** Se especifica igual que en un `lm()` -lineal en los parámetros: `x1+x2+x1:x2+...`.
- **Enlace:** `link = "logit"` conecta la media —la probabilidad— con el predictor y mantiene $\hat\pi$ en $(0,1)$.

Los principales descriptivos los obtenemos con:
- `summary(fit_glm)`, el resultado del ajuste
- `tidy(fit_glm)` formatea el resultado
-`glance()` da la **deviance** y el **AIC**, con las que evaluamos la calidad del ajuste.
- `fitted(fit_glm)` da la predicción de probabilidades en los registros observados.
- `broom::augment(fit_glm, type.predict = "response")` da los ajustes ordenados en `[0,1]`
- `predict()` devuelve la predicción en la **escala del predictor** (log-odds) para cualquier valor de los predictores. Para
obtenerla en **probabilidad** hay que pedir la escala de respuesta:

```r
predict(fit_glm, newdata = ..., type = "response")    # probabilidad en [0,1]
predict(fit_glm, newdata = ...)                       # por defecto: log-odds (type = "link")
```
::::

Ajustamos pues el modelo, y mostramos el resultado.

```{r}
#| label: u11-primer-glm
fit_glm <- glm(fracture ~ age + priorfrac, family = binomial, data = glow)
# Resultado del ajuste
summary(fit_glm)
```

Este ajuste proporciona, como el `lm()`, coeficientes, medidas de bondad de ajuste, que podemos mostrar con formato con la librería `broom`.

```{r}
#| label: u11-broom
tidy(fit_glm)                                     # coeficientes (escala log-odds)
glance(fit_glm)                                   # deviance, AIC, BIC, gl...
augment(fit_glm, type.predict = "response") |>    # ajustes en escala probabilidad
  dplyr::select(fracture, age, priorfrac, .fitted) |>
  slice_head(n = 5)
```


La @fig-u11-ols-vs-logistica muestra las predicciones que se obtienen en los dos modelos considerados hasta el momento: el lineal y el lineal generalizado.

```{r}
#| label: fig-u11-ols-vs-logistica
#| fig-cap: "Predicciones de los modelos (`age` + `priorfrac`) frente a las proporciones de fractura observadas por grupo de edad (el tamaño de los puntos representa el nº de casos). Ajustes OLS (izquierda) y logístico (derecha) deberían pasar cerca de los puntos del mismo color. En el rango observado ambos se mantienen en [0,1]."

# Rejilla de predicción de los modelos 
grid <- expand_grid(
  age       = seq(min(glow$age), max(glow$age), length.out = 100),
  priorfrac = factor(c("No", "Yes"), levels = levels(glow$priorfrac))
)
grid$OLS <- predict(fit_ols, newdata = grid)                    # escala probabilidad
grid$GLM <- predict(fit_glm, newdata = grid, type = "response") # escala probabilidad
grid_long <- pivot_longer(grid, c(OLS, GLM),
                          names_to = "modelo", values_to = "p_hat") |>
  mutate(modelo = factor(modelo, levels = c("OLS", "GLM")))

# Proporciones observadas por grupo de edad y priorfrac (las de la EDA),
# situadas en la edad media de cada grupo
grupos <- glow |>
  mutate(edad_grupo = cut(age, breaks = seq(55, 95, by = 5))) |>
  filter(!is.na(edad_grupo)) |>
  group_by(edad_grupo, priorfrac) |>
  summarise(age = mean(age), p_fractura = mean(fractura01), n = n(), .groups = "drop")

ggplot() +
  geom_point(data = grupos,
             aes(age, p_fractura, color = priorfrac, size = n)) +
  geom_line(data = grid_long,
            aes(age, p_hat, color = priorfrac), linewidth = 1) +
  geom_hline(yintercept = c(0, 1), linetype = "dashed") +
  facet_wrap(~ modelo) +
  labs(x = "Edad", y = "Proporción / probabilidad de fractura",
       color = "Fractura previa", size = "n")
```

Se aprecia en la @fig-u11-ols-vs-logistica la diferencia en la predicción de la probabilidad de fractura, asimilada en el OLS por sendas rectas paralelas y en el GLM con dos curvas que intentan adaptar mejor la forma de los datos.


Ya con el glm ajustado, al comparar los residuos medios por tramos (*binned residuals*, en la [Unidad 2](#sec-u12-diagnostico) para el ajuste OLS y el GLM (@fig-u11-residuos-ols-glm), apreciamos mejor rendimiento en este último, con la mayoría de residuos dentro de la banda de confianza, a diferencia de los residuos OLS, que fluyen fuera de la banda.

```{r}
#| label: fig-u11-residuos-ols-glm
#| fig-width: 10
#| fig-height: 6
#| out-width: "100%"
#| fig-cap: "Residuos medios por tramos para los ajustes OLS y GLM."

p_ols <- plot(performance::binned_residuals(fit_ols)) +
  labs(title = "OLS (modelo lineal de probabilidad)")
p_glm <- plot(performance::binned_residuals(fit_glm)) +
  labs(title = "GLM logístico")
patchwork::wrap_plots( p_ols, p_glm, nrow = 2) +
  patchwork::plot_annotation(title = "Residuos medios por tramos: OLS vs GLM")
```


::: {.callout-important}
## Tarea del estudiante (1.1) — alineada con RA1
1. **Demuestra el fallo de OLS con tu propio gráfico.** Elige una covariable
   continua distinta de `age` (p. ej. `bmi` o `height`), ajusta
   `lm(fractura01 ~ covariable)` y construye **un** gráfico que evidencie a la
   vez predicciones fuera de $[0,1]$ y el patrón de residuos en dos bandas.
2. **Mapea fallo → componente.** Tabla de dos columnas: los cuatro fallos de OLS
   y qué componente del GLM (aleatoria / predictor lineal / enlace) resuelve cada
   uno, con una frase de porqué.
:::

## Puente a la Unidad 2 {#sec-u11-puente}

Hoy dimos por buenos, sin justificarlos, varios ingredientes: que Bernoulli
pertenece a la familia exponencial, que el logit es el enlace canónico, que
`glm()` estima por máxima verosimilitud y que la `deviance` mide el ajuste. La
[Unidad 2](#sec-u12) abre esa caja negra para desarrollarla.

<!-- Partial: incluir con {{< include _unidad_1_2.qmd >}}. Sin YAML.
     Chunks prefijados u12-. Hereda de la madre: glow, fractura01, fit_glm
     (glm(fracture ~ age + priorfrac, binomial)), y la pila base (tidyverse,
     broom, performance, DHARMa, arm, see, patchwork). Esta unidad carga además
     marginaleffects, car, pROC, emmeans, ggeffects, MASS. -->

# 2 · Formulación de los GLM: estimación, inferencia, interpretación y evaluación {#sec-u12}

En la [Unidad 1](#sec-u11) dimos por buenos, sin justificarlos, cuatro
ingredientes: que la Bernoulli pertenece a la **familia exponencial**, que el
**logit** es su enlace canónico, que `glm()` estima por **máxima verosimilitud**
y que la **deviance** mide el ajuste. Esta unidad abre esa caja negra y la cierra
con lo que de verdad se hace en la práctica: estimar, hacer inferencia, comparar
modelos, interpretarlos en la escala correcta y evaluarlos como clasificadores.

::: {.callout-note}
## Sobre esta unidad
**RA implicados:** RA2 (ajuste), RA3 (interpretación en la escala correcta), RA4
(diagnóstico, separación, calibración), RA5 (comparación de modelos).
[@nelder1972; @mccullagh1989; @dobson2018; @agresti2015; @hosmer2013; @fox2016]
:::

```{r}
#| label: u12-setup
#| include: false

# La madre ya cargó tidyverse, broom, performance, DHARMa, arm, see, patchwork.
# Esta unidad añade lo suyo:
library(marginaleffects)   # efectos marginales / predicciones (Arel-Bundock et al., 2024)
library(car)               # Anova tipo II/III (Fox & Weisberg, 2019)
library(pROC)              # curva ROC y AUC (Robin et al., 2011)
library(emmeans)           # medias marginales estimadas
library(ggeffects)         # predicciones para graficar
library(MASS)              # confint por perfil, stepAIC (recommended package)

# Recuperamos el ajuste de 1.1 por si esta unidad se compila aislada:
if (!exists("fit_glm")) {
  fit_glm <- glm(fracture ~ age + priorfrac, family = binomial, data = glow)
}
```

## 2.1 Familia exponencial y relación media–varianza {#sec-u12-familia}

Un GLM exige que la respuesta siga una distribución de la **familia exponencial
de dispersión**, cuya densidad (o función de masa) se escribe en la forma canónica

$$
f(y;\theta,\phi)=\exp\!\left\{\frac{y\theta-b(\theta)}{a(\phi)}+c(y,\phi)\right\},
$$

donde $\theta$ es el **parámetro natural** (o canónico), $b(\theta)$ la **función
cumulante**, $\phi$ el **parámetro de dispersión** y $a(\phi)$, $c(y,\phi)$
funciones conocidas [@mccullagh1989; @dobson2018]. Esta forma no es un capricho
notacional: de ella se derivan, derivando la cumulante, los dos momentos que
gobiernan todo el marco.

::: {.callout-note}
## Quién vive en esta familia (y para qué sirve)
La misma forma canónica agrupa distribuciones muy distintas; cada una es la
herramienta natural para un tipo de fenómeno del mundo real:

- **Bernoulli / Binomial** — sucesos sí/no: ¿hay fractura? (GLOW), abandono de
  cliente (*churn*), impago de un crédito, resultado de un test diagnóstico.
- **Poisson** — recuentos por unidad de tiempo o espacio: reclamaciones de un
  asegurado al año, visitas a urgencias, fallos de una máquina, llamadas por hora.
- **Exponencial** — tiempo continuo hasta un evento con riesgo constante (sin
  memoria): vida útil de un componente; abre el hilo de supervivencia (Caso 3).
- **Geométrica** — número de intentos hasta el primer éxito: envíos hasta la
  primera respuesta, ciclos hasta el primer fallo.
- **Gaussiana** — magnitudes continuas sin restricción de signo: estatura, error
  de medida. Es el OLS que ya conoces, subsumido en el marco.
- **Gamma** — magnitudes positivas y asimétricas con variabilidad proporcional a
  la media: coste de un siniestro, gasto sanitario por paciente, duración de una
  sesión (Caso 3).
- **Inversa gaussiana / Tweedie** — duraciones de cola pesada, o datos con masa en
  cero y parte continua positiva a la vez (coste agregado de siniestros,
  precipitación diaria).
:::

Partiendo de que $\int f\,\mathrm{d}y=1$ y derivando bajo el signo integral se
obtiene el resultado central:

$$
\mathbb{E}[Y]=\mu=b'(\theta),\qquad
\operatorname{Var}(Y)=b''(\theta)\,a(\phi).
$$

La media depende solo de $\theta$ y la varianza se **factoriza** en dos piezas:
$a(\phi)$, común a la familia, y $b''(\theta)$, que reescrita en función de la
media define la **función de varianza** $V(\mu)=b''(\theta(\mu))$. Esa $V(\mu)$ es
la **huella digital** de cada familia: es lo que distingue una Bernoulli de una
Poisson o una Gamma, y lo que en 1.1 «absorbía los problemas 2 y 3» de OLS
(varianza no constante y error no normal).

::: {.callout-note}
## El otro factor de la varianza: $a(\phi)$ y los pesos a priori
En los modelos GLM estándar el primer factor de
$\operatorname{Var}(Y_i)=a(\phi)\,V(\mu_i)$ toma **habitualmente** la forma
$$a(\phi)=\frac{\phi}{m_i},$$
con $\phi$ el **parámetro de dispersión** (común a la familia) y $m_i$ un **peso a
priori conocido** que registra cuántas unidades elementales hay detrás de cada fila.
En general $a(\phi)$ es solo *alguna* función de $\phi$; la forma $\phi/m_i$ es la
convención que surge cuando cada observación resume $m_i$ unidades i.i.d.

Con datos **individuales** —una fila, una observación— se tiene $m_i=1$ y por tanto
$a(\phi)=\phi$. Es la situación de todo el Caso 1: cada mujer de GLOW aporta un único
ensayo Bernoulli.

El peso $m_i\neq1$ aparece cuando el dato llega **agregado**:

- **Binomial como proporción.** Si $y_i$ es la fracción de éxitos sobre $m_i$ ensayos,
  $m_i$ es el denominador binomial y $\operatorname{Var}(\bar Y_i)=\mu_i(1-\mu_i)/m_i$.
  En R: respuesta `cbind(éxitos, fracasos)` o bien `weights = m`.
- **Gaussiana o Gamma sobre medias de grupo.** Si $y_i$ es el promedio de $m_i$
  mediciones, $m_i$ es el tamaño del grupo.

No conviene confundir las dos piezas: $\phi$ es la **escala libre** de la familia
(recuadro siguiente), mientras que $m_i$ refleja la **estructura de los datos**
(réplicas o agregación), no la distribución.
:::

::: {.callout-note}
## ¿Uno o dos parámetros? El papel de $\phi$
En **Bernoulli, Poisson, exponencial y geométrica** el parámetro de dispersión es
**conocido y fijo, $\phi=1$**: no hay escala libre que estimar, y un **único
parámetro $\theta$** (equivalentemente, la media $\mu$) gobierna a la vez la media
y la varianza. Por eso $V(\mu)$ las liga de forma rígida —$\mu(1-\mu)$, $\mu$…—.
Son familias exponenciales **lineales** (un solo parámetro).

El resto de variantes —**Gaussiana, Gamma, inversa gaussiana, Tweedie**— añaden un
parámetro de **dispersión propio** que se estima de los datos: el **escalamiento se
modeliza con independencia de la media**, lo que permite describir situaciones más
ricas (varianza constante en la Gaussiana, coeficiente de variación constante en la
Gamma…). Son los **modelos de dispersión exponencial**.

Ese mando $\phi$ no es ornamental: reaparece como **sobredispersión** en los
conteos del Caso 2 (quasi-Poisson, binomial negativa) y como las **continuas
positivas** del Caso 3. En el Caso 1, al ser todo Bernoulli/Binomial, $\phi=1$ y no
hay nada que estimar.
:::

| | Bernoulli | Poisson | Gaussiana | Gamma |
|---|---|---|---|---|
| **Parametrización** | $\mathrm{Br}(\mu)$ | $\mathrm{Pois}(\mu)$ | $\mathcal{N}(\mu,\sigma^2)$ | $\mathrm{Ga}\!\left(\nu,\tfrac{\mu}{\nu}\right)$ |
| $\theta(\mu)$ | $\log\frac{\mu}{1-\mu}$ | $\log\mu$ | $\mu$ | $-1/\mu$ |
| $b(\theta)$ | $\log(1+e^\theta)$ | $e^\theta$ | $\theta^2/2$ | $-\log(-\theta)$ |
| $b'(\theta)$ | $\dfrac{e^\theta}{1+e^\theta}$ | $e^\theta$ | $\theta$ | $-1/\theta$ |
| $b''(\theta)$ | $\dfrac{e^\theta}{(1+e^\theta)^2}$ | $e^\theta$ | $1$ | $1/\theta^2$ |
| $a(\phi)$ | $1$ | $1$ | $\phi=\sigma^2$ | $\phi=\nu^{-1}$ |
| $c(y,\phi)$ | $0$ | $-\log y!$ | $-\tfrac{1}{2}\!\left(\tfrac{y^2}{\phi}+\log(2\pi\phi)\right)$ | $\tfrac{1-\phi}{\phi}\log y-\tfrac{\log\phi}{\phi}-\log\Gamma\!\left(\tfrac{1}{\phi}\right)$ |
| $V(\mu)$ | $\mu(1-\mu)$ | $\mu$ | $1$ | $\mu^2$ |
| **Enlace canónico** | logit | log | identidad | inversa |

: Familias de la exponencial: parametrización e ingredientes canónicos. {#tbl-familias}

La fila $V(\mu)$ explica de un vistazo por qué OLS (Gaussiana, $V\equiv 1$)
fracasa en binaria: para la Bernoulli, $V(\mu)=\mu(1-\mu)$ **no** es constante. El
objeto `family()` de R expone exactamente estas piezas, y cada familia permite recuperar sus ingredientes canónicos:

```{r}
#| label: u12-familia
family(fit_glm)              # familia y enlace del ajuste de 1.1
binomial()$variance          # función de varianza V(mu) = mu(1-mu)
binomial()$linkfun(0.25)     # logit(0.25): de la media al predictor
binomial()$linkinv(-1.0986)  # sigmoide: del predictor a la media
```

La varianza de una sola respuesta binaria no se observa: un 0/1 no tiene dispersión
propia. Para *verla*, agrupamos GLOW en diez tramos (deciles) según la probabilidad
ajustada $\hat\mu$; dentro de cada tramo el riesgo es parecido, así que podemos medir
allí la varianza empírica de la respuesta. Confrontamos ese valor con la parábola
$\mu(1-\mu)$: si los puntos la siguen, confirmamos —solo con datos— que en la Bernoulli
la varianza la **dicta la media** (máxima en $\mu=0{,}5$, casi nula cerca de 0 o 1), no
una constante como supondría OLS. La @fig-u12-varianza-mu muestra que los puntos siguen la parábola.

```{r}
#| label: fig-u12-varianza-mu
#| fig-cap: "Relación media–varianza en GLOW. La varianza empírica de la respuesta por tramos de probabilidad ajustada (puntos) sigue la parábola teórica $V(\\mu)=\\mu(1-\\mu)$ (línea), no una constante: justo lo que OLS no puede reproducir."

glow |>
  mutate(p_hat = fitted(fit_glm),
         tramo = ntile(p_hat, 10)) |>
  group_by(tramo) |>
  summarise(mu = mean(p_hat),
            var_emp = var(fractura01), .groups = "drop") |>
  ggplot(aes(mu, var_emp)) +
  geom_function(fun = \(m) m * (1 - m), linewidth = 1, color = "firebrick") +
  geom_point(size = 2) +
  labs(x = expression(hat(mu)), y = "Varianza empírica de Y por tramo",
       caption = expression("Curva: V(" * mu * ") = " * mu * "(1 - " * mu * ")"))
```

## 2.2 El enlace: canónico y alternativas {#sec-u12-enlace}

El **enlace** o *función link* $g$ conecta la media con el predictor lineal, $g(\mu)=\eta=\mathbf{x}^\top
\boldsymbol\beta$. Una misma familia puede utilizar cualquier enlaces $g$ que sea una función monótona y diferenciable, pero el **enlace canónico** es aquel que iguala el parámetro natural al predictor lineal, esto es, $\theta=\eta$. Este enlace canónico simplemente aporta ventajas formales en las matemáticas, como estadísticos suficientes de dimensión mínima o simplificación de las ecuaciones de verosimilitud.

En el caso binario de la familia binomial, las tres alternativas que re-escalan un predictor lineal -teóricamente definido en toda la recta real- a la escala de una probabilidad -en el rango [0,1]- son el enlace logit, el probit y el complementario log-log -cloglog-, que reproducen la forma de S invertida a la hora de relacionar el predictor lineal con la probabilidad, como se aprecia en la @fig-u12-enlaces. Logit y probit son **simétricos** (casi intercambiables tras reescalar: el
coeficiente probit ≈ logit/1,6); el **cloglog** (complementario log-log) es **asimétrico** y será el enlace
natural de la supervivencia en tiempo discreto en la [Unidad 5](#sec-u15)
[@agresti2015].

$$
\text{logit: }\log\tfrac{\pi}{1-\pi}=\eta,\qquad
\text{probit: }\Phi^{-1}(\pi)=\eta,\qquad
\text{cloglog: }\log(-\log(1-\pi))=\eta .
$$

:::: {.callout-tip}
## 🔧 En R. Especificar el enlace (logit, probit, cloglog)
En R, la forma de especificar los distintos logit se hace a través del argumento `link={'logit','probit','cloglog'}`, o bien directamente como un argumento de la familia, `family={binomial("logit"), binomial("probit"), binomial("cloglog")}`.
::::

```{r}
#| label: fig-u12-enlaces
#| fig-cap: "Funciones enlace en la familia binomial. Las tres inversas devuelven η a (0,1). logit y probit son simétricas (casi intercambiables tras reescalar); el cloglog es asimétrico."
tibble(eta = seq(-5, 5, by = 0.02)) |>
  mutate(logit   = plogis(eta),
         probit  = pnorm(eta),
         cloglog = 1 - exp(-exp(eta))) |>
  pivot_longer(c(logit, probit, cloglog), names_to = "enlace", values_to = "p") |>
  ggplot(aes(eta, p, color = enlace)) +
  geom_line(linewidth = 1) +
  geom_hline(yintercept = c(0, 1), linetype = "dotted") +
  labs(x = expression(eta), y = expression(p == g^{-1}(eta)), color = "Enlace")
```

Para ver el efecto de utilizar diferentes enlaces, ajustamos GLOW con los tres y comparamos:

```{r}
#| label: u12-enlaces
ajustes <- list(
  logit   = glm(fracture ~ age + priorfrac, binomial("logit"),   glow),
  probit  = glm(fracture ~ age + priorfrac, binomial("probit"),  glow),
  cloglog = glm(fracture ~ age + priorfrac, binomial("cloglog"), glow))

map_dfr(ajustes, \(m) glance(m) |> select(deviance, AIC), .id = "enlace")
map_dfr(ajustes, tidy, .id = "enlace") |>
  select(enlace, term, estimate) |>
  pivot_wider(names_from = enlace, values_from = estimate)
```

Los coeficientes estimados **no** son directamente comparables entre enlaces (viven en
escalas distintas y como veremos, se interpretan de forma distinta), pero el ajuste global, calibrado en términos del AIC -que ya conocemos del modelo lineal- y la deviance -que presentaremos más tarde-, es muy parecido, con lo cual en este caso concluiríamos que **la elección de enlace apenas cambia las conclusiones**. Esto es lo habitual salvo en comportamientos extremos en las colas o en datos con mucha asimetría. Por interpretabilidad solemos utilizar el logit en la familia binomial.

Esta equivalencia entre enlaces se aprecia en la escala de la
**probabilidad**, en la @fig-u12-enlaces-prob, que superpone las curvas edad–probabilidad de
los tres enlaces, separando en dos paneles las predicciones según el antecedente de fractura previa. Con el fin de ver la variación que se produce en las colas, y a pesar de que no es pertinente la extrapolación de la predicción fuera del rango observado, se han punteado las predicciones prolongadas fuera de dicho rango, para visualizar la forma de S invertida completa, y las diferencias en dichas colas.

```{r}
#| label: fig-u12-enlaces-prob
#| fig-width: 9.5
#| fig-height: 4.3
#| out-width: "100%"
#| fig-cap: "Probabilidad de fractura predicha por los tres enlaces (logit, probit, cloglog) sobre `age + priorfrac`, en paneles por antecedente de fractura previa. Trazo sólido y franja sombreada: rango observado en GLOW (55–90 años); punteado: extrapolación para apreciar la forma en S."

rango_obs <- range(glow$age)
grid <- expand_grid(
  age       = seq(40, 105, length.out = 300),
  priorfrac = factor(c("No", "Yes"), levels = levels(glow$priorfrac)))

# Predicción en probabilidad de cada uno de los tres modelos de `ajustes`
pred_enlaces <- map_dfr(ajustes, \(m) {
  grid |> mutate(p_hat = predict(m, newdata = grid, type = "response"))
}, .id = "enlace") |>
  mutate(enlace = factor(enlace, levels = c("logit", "probit", "cloglog")))
pred_obs <- filter(pred_enlaces, age >= rango_obs[1], age <= rango_obs[2])

etiq_pf <- c(No = "Sin fractura previa", Yes = "Con fractura previa")

ggplot(pred_enlaces, aes(age, p_hat, color = enlace)) +
  annotate("rect", xmin = rango_obs[1], xmax = rango_obs[2],
           ymin = -Inf, ymax = Inf, alpha = 0.05) +
  geom_line(linewidth = 0.7, linetype = "22") +          # extrapolación
  geom_line(data = pred_obs, linewidth = 1.1) +          # rango observado
  facet_wrap(~ priorfrac, labeller = labeller(priorfrac = etiq_pf)) +
  scale_y_continuous(limits = c(0, 1), labels = scales::label_percent()) +
  labs(x = "Edad (años)", y = "P(fractura)", color = "Enlace") +
  theme(legend.position = "bottom")
```

En el rango de edad **realmente observado** (zona sombreada, 55–90 años) las tres
curvas son prácticamente **indistinguibles**: la elección de enlace no mueve ni las
probabilidades predichas ni, por tanto, las conclusiones, en línea con la deviance
y el AIC casi idénticos de la tabla anterior. Las diferencias solo asoman al
**extrapolar** hacia las colas, donde el `cloglog` —asimétrico— se despega un poco
de logit y probit (simétricos, casi superpuestos entre sí). Es el comportamiento
típico: el enlace importa en las colas y bajo fuerte asimetría, no en el grueso de
los datos, lo que respalda quedarnos con el logit por interpretabilidad.

## 2.3 Interpretación: del coeficiente a la probabilidad {#sec-u12-interpretacion}


**El enlace rompió la línea recta.** En OLS, $\beta$ era el cambio en la respuesta
por cada unidad de $x$: una lectura directa. Aquí ya no. El modelo es lineal en el
**predictor** $\eta=\beta_0+\beta x$, pero la respuesta es $\mu=g^{-1}(\eta)$, una
transformación **no lineal** de $\eta$. Por eso $\beta$ ya **no** mide el cambio en
la probabilidad, sino el cambio en $\eta$: la misma diferencia en $x$ mueve mucho la
probabilidad en el centro de la curva y casi nada en las colas. Para interpretar,
leeremos los coeficientes en la escala donde el modelo **sí** es lineal —y le
pondremos nombre.

**Lo que da el modelo:** `summary()` (o `broom::tidy`) devuelve los coeficientes en
la escala del predictor lineal:

Recordemos el modelo que estamos ajustando sobre GLOW:

$$\pi_i = P(Y_i=1) \ \rightarrow \ logit(\pi_i)=\beta_0 + \beta_1 \cdot \text{age}_i + \gamma_{\text{priorfrac}}\text{priorfrac}_i, \qquad i= 1, ..., n$$  


donde $\text{age}_i$ es la edad de la paciente $i$, y $\text{priorfrac}_i=1$ si la paciente tuvo una fractura previo y 0 en caso contrario.

```{r}
#| label: u12-coef
tidy(fit_glm)   # coeficientes en escala log-odds: lo que da summary() por defecto
```

Su **signo** ya informa —positivo sube $\eta$ y con él la probabilidad; negativo la
baja—, pero su **magnitud** no es un cambio en probabilidad. ¿Qué es, entonces, esa
escala?

### Interpretación en la escala de los odds y log-odds

Aparecen en la ecuación los **odds y log-odds**, que provienen del enlace canónico de la Bernoulli. 

- El *odds* de un suceso es su **ventaja**, la probabilidad de que ocurra frente a la de que no: un odds de 2 dice que el evento es el doble de probable que su ausencia.
$$\text{odds}(x)=\frac{\pi(x)}{1-\pi(x)}$$
- Su logaritmo —el **log-odds** o *logit*— es precisamente el enlace canónico, y por tanto
**es** el predictor lineal:
$$\eta(x)=\log\frac{\pi(x)}{1-\pi(x)}=\log.\text{odds}(x)=\beta_0+\beta x.$$
La escala de los log.odds es donde viven los coeficientes y donde el modelo es una recta.
$$ \log.\text{odds}(x)=\eta(x) \rightarrow \text{odds}(x)=e^{\eta(x)}$$

**Qué dice $\beta$ ahí, y el odds ratio:** 

- Subir $x$ una unidad **suma** $\beta$ al log-odds: el efecto es **aditivo**
$$\beta=\eta(x+1)-\eta(x) \rightarrow \log.odds(x+1)= \beta +\log.odds(x)$$
- Al deshacer el logaritmo surge el **odds ratio** ($OR$), como un cociente de odds: 
$$\beta=\log\frac{\text{odds}(x+1)}{\text{odds}(x)} = log(OR) \rightarrow OR=e^{\beta}$$
de modo que el efecto en los **odds** es multiplicativo, y cada unidad de $x$ multiplica los odds por el odds ratio:
$$\text{odds}(x+1)=e^{\eta(x)+\beta} = \text{odds}(x) \cdot e^{\beta}= \text{odds}(x) \cdot OR$$


Por eso, si bien el log-odds depende de $x$, el OR es un factor **constante** que aplica a toda la curva, y de ahí su interpretabilidad: cada movimiento de una unidad en $x$, sea cual sea el $x$, multiplica los odds por el mismo factor $e^{\beta}$. La relación entre el signo de los coeficientes, el OR y el efecto que genera en las probabilidades, es ahora más patente:

- $\beta>0\Leftrightarrow\text{OR}>1$: aumenta la ventaja del evento;
- $\beta<0\Leftrightarrow\text{OR}<1$: reduce la ventaja del evento. 

La @fig-u12-tres-escalas lo resume. Utiliza los valores $\beta=1$ (en azul) y $\beta=-1$ (en rojo), y muestra el efecto de variar el predictor $x$ (en el eje X) sobre el log-odds, el odds y la probabilidad del evento. A la izquierda, el log-odds η = β₀ + βx identifica una recta cuya pendiente es β (efecto aditivo, constante). En el centro, se aprecia la curva exponencial del odds = e^η: cada unidad de x multiplica las odds por e^β. A la derecha, la relación entre el predictor x y la probabilidad = 1/(1+e^(−η)) viene representada en forma de S, creciente o decreciente, con efecto máximo en el centro y casi nulo en las colas. Esa no linealidad de la derecha es justo lo que impide leer β como un cambio de probabilidad.

```{r}
#| label: fig-u12-tres-escalas
#| fig-width: 11
#| fig-height: 3.8
#| out-width: "100%"
#| fig-cap: "Efecto de la variación en un predictor genérico x (β₀ = 0) sobre el log-odds, el odds y la probabilidad del evento. En azul un coeficiente positivo (β = +1); en rojo uno negativo (β = −1)."

betas <- c("β = +1 (positivo)" = 1, "β = −1 (negativo)" = -1)
demo <- expand_grid(x = seq(-3, 3, length.out = 300), signo = names(betas)) |>
  mutate(beta = betas[signo],
         `log-odds` = beta * x,          # β0 = 0
         odds       = exp(beta * x),
         prob       = plogis(beta * x))

col <- c("β = +1 (positivo)" = "#2c7fb8", "β = −1 (negativo)" = "#d7301f")
escala <- function(y, ylab, titulo, y0)
  ggplot(demo, aes(x, .data[[y]], color = signo)) +
    geom_hline(yintercept = y0, linetype = "dotted", color = "grey60") +
    geom_line(linewidth = 1.1) +
    scale_color_manual(values = col) +
    labs(x = "predictor x", y = ylab, color = NULL, title = titulo)

p1 <- escala("log-odds", "log-odds (η)", "η = β₀ + βx  ·  recta (pendiente β)", 0)
p2 <- escala("odds",     "odds = e^η",   "odds  ·  multiplica e^β por unidad",            1)
p3 <- escala("prob",     "P(evento)",    "probabilidad  ·  forma en S",        0.5) +
        scale_y_continuous(limits = c(0, 1), labels = scales::label_percent())

(p1 + p2 + p3) + plot_layout(guides = "collect") &
  theme(legend.position = "bottom",
        plot.title = element_text(size = 9.5, face = "bold"))
```

::: {.callout-warning}
## Odds ratio y riesgo relativo
Puntualizar que odds-ratio OR no es lo mismo que riesgo relativo RR. El riesgo relativo es útil para comparar probabilidades, luego en el contexto del modelo lineal cabría valorar el efecto, sobre la probabilidad, de añadir una unidad a la $x$
$$\text{RR}=\pi(x+1)/\pi(x)$$
En general $\text{OR}\neq\text{RR}$, si bien ambos se
relacionan por
$$
\text{OR}=\text{RR}\cdot\frac{1-\pi(x)}{1-\pi(x+1)},
$$
de modo que el OR **se aleja de 1 más que el RR** y exagera el efecto cuando el
evento es frecuente. Solo si el evento es **raro** ($\pi$ pequeña) se cumple
$1-\pi\approx 1$ y entonces $\text{OR}\approx\text{RR}$. 

Por eso decir «el riesgo
se triplica» al leer un $\text{OR}=3$ es una aproximación cómoda pero informal: la
lectura exacta es en términos de *odds* (ventaja), o bien en probabilidad mediante
los efectos marginales (más abajo).
:::

**Intervalos de confianza.** Como $\text{OR}=e^{\beta}$ y la exponencial es
**monótona creciente**, el IC del OR es la **exponencial de los extremos** del IC del
coeficiente, sin recalcular nada:
$$\text{IC}_{95\%}(\text{OR})=\big[e^{\beta_{\text{inf}}},\,e^{\beta_{\text{sup}}}\big].$$
Y como $e^{0}=1$, que el IC de $\beta$ **no contenga 0** equivale a que el del OR **no
contenga 1**: la misma señal de efecto en las dos escalas.

**En GLOW.** Aplicado a las dos predictoras:

```{r}
#| label: u12-or
exp(cbind(OR = coef(fit_glm), confint(fit_glm)))   # OR e IC: exponencial del IC de beta
```

La fractura previa multiplica las odds de fractura por $\text{OR}\approx 2{,}31$
(IC 95 %: 1,46–3,66) y cada año de edad por $\approx 1{,}04$ (IC 95 %: 1,02–1,07);
ninguno cruza 1, de modo que ambos efectos son claros. (El IC de $\beta$ procede del
perfil de verosimilitud, [Sección 2.5](#sec-u12-inferencia).)

**La misma idea en otros enlaces.** El predictor lineal $\eta$ es **idéntico** en los
tres enlaces; cambia qué *representa* y, con ello, cómo se lee el coeficiente:

| Enlace | $\eta$ es… | $\eta=g(\mu)$ | $\mu=g^{-1}(\eta)$ |
|---|---|---|---|
| **logit** | el **log-odds** | $\log\dfrac{\mu}{1-\mu}$ | $\dfrac{e^{\eta}}{1+e^{\eta}}$ |
| **probit** | el cuantil normal ($z$) | $\Phi^{-1}(\mu)$ | $\Phi(\eta)$ |
| **cloglog** | el log-riesgo (log del *hazard* acumulado) | $\log\!\big(-\log(1-\mu)\big)$ | $1-e^{-e^{\eta}}$ |

: Relación entre el predictor lineal $\eta$ —donde actúan los coeficientes— y la media $\mu$ en los tres enlaces. En el logit, $\eta$ es exactamente el log-odds; cada enlace «lee» $\eta$ en una escala distinta. {#tbl-enlaces-eta-mu}

De ahí que el mismo $\beta$ signifique cosas distintas aunque el ajuste apenas varíe:

- **logit:** $e^{\beta}$ = **odds ratio** (multiplica las odds); la lectura habitual
  en clínica y epidemiología.
- **probit:** $\beta$ **desplaza la escala latente normal** ($z$), sin lectura de odds;
  como guía, $\beta_{\text{probit}}\approx\beta_{\text{logit}}/1{,}6\text{–}1{,}8$.
- **cloglog:** $e^{\beta}$ = **razón de riesgos** (*hazard ratio*); lectura natural en
  supervivencia ([Unidad 5](#sec-u15)).

```{r}
#| label: u12-coef-enlaces
# Mismos datos, distinta escala del coeficiente según el enlace
map_dfr(ajustes, \(m) tidy(m) |> select(term, estimate), .id = "enlace") |>
  pivot_wider(names_from = enlace, values_from = estimate) |>
  mutate(razon_logit_probit = logit / probit)   # ~1,6-1,8 (salvo intercepto)
```

### Interpretación en la escala de la probabilidad: efectos marginales

Cuando queremos interpretar un ajuste directamente en términos de las probabilidades, acudimos a los **efectos marginales**. El odds ratio es constante, pero el cambio en **probabilidad** no: un año más de `age` apenas mueve nada en las colas y
mucho en el centro de la S. Para resumirlo en un número, el **efecto marginal medio
(AME)** responde a la pregunta que de verdad importa:

>"¿Cuántos puntos de probabilidad sube el evento, en promedio, por una unidad más de la covariable?

En GLOW, el AME de `age` es ≈ **0,007**: unos **0,7 puntos porcentuales de riesgo de fractura por año**.

**¿De dónde sale ese número?** 

Para calcular el AME o efecto medio marginal asociado a un predictor continuo $x_k$ (con coeficiente $\beta_k$ en el predictor lineal), se considera el efecto de cada individuo a través de la pendiente de su propia curva, y dado que la derivada del logit es $\mu(1-\mu)$, esta pendiente es:
$$\frac{\partial\mu_i}{\partial x_k}=\hat\pi_i(1-\hat\pi_i)\,\beta_k.$$
El AME es el promedio sobre las pendientes para las $n$ personas observadas: literalmente, el
coeficiente por la pendiente media de la sigmoide.
$$\text{AME}_k=\beta_k\cdot\frac{1}{n}\sum_i \hat\pi_i(1-\hat\pi_i)$$ 

```{r}
#| label: u12-ame
# AME de cada covariable, en puntos de probabilidad
avg_slopes(fit_glm)                       

# Reproducción "a mano" del cálculo del AME para 'age':
p <- fitted(fit_glm)                      # pi_hat de cada individuo
mean(p * (1 - p)) * coef(fit_glm)["age"]  # = AME de age
```

Cuando se trata de evaluar el AME para un factor como `priorfrac`, el coeficiente no es una pendiente (derivada) sino una **diferencia contrafactual**. El AME entonces se calcula con el efecto diferencial sobre la categoría de referencia (en `priorfrac`, $\hat\pi$ con `Yes` menos con `No`), promediada entre individuos, es decir:
$$
\text{AME}_{\text{priorfrac}}=\frac{1}{n}\sum_{i=1}^{n}\Big[\hat\pi\big(\texttt{Yes},\,x_i\big)-\hat\pi\big(\texttt{No},\,x_i\big)\Big],
$$
donde $\hat\pi(\texttt{nivel},x_i)$ se evalúa para todos los sujetos en la muestra en todos los niveles (en `priorfrac`, Yes y No).
$$
\hat\pi(\text{nivel},x_i)=\operatorname{logit}^{-1}\!\big(\hat\beta_0+\hat\beta_{\text{age}}\,\text{age}_i+\hat\beta_{\text{pf}}^{\,\text{nivel}}\big),\qquad \hat\beta_{\text{pf}}^{\,\text{ref}}=0.
$$

```{r}
#| label: u12-ame-factor
# Reproducción "a mano" del cálculo de AME para 'priorfrac'
g_yes <- transform(glow, priorfrac = factor("Yes", levels = levels(glow$priorfrac)))
g_no  <- transform(glow, priorfrac = factor("No",  levels = levels(glow$priorfrac)))

mean(predict(fit_glm, g_yes, type = "response") -
     predict(fit_glm, g_no,  type = "response"))   # = AME de priorfrac
```

El AME en probabilidad es, además, **casi invariante al enlace** (los tres ajustes lo dan
parecido pese a coeficientes distintos) y se prefiere frente a otros indicadore por promediar sobre la población observada [@arelbundock2024]. En una frase: 
>El **OR** comunica la fuerza del efecto; el **AME**, su impacto absoluto en probabilidad.


En la @fig-u12-dos-escalas se visualiza el efecto de la edad en dos escalas. A la izquierda, en escala log-odds, 
los dos perfiles son **rectas paralelas**: la edad entra de forma lineal y `priorfrac` solo desplaza la recta una
cantidad **constante**, $\log(\text{OR})\approx 0{,}84$ —ahí vive el odds ratio—. A
la derecha, en probabilidad, esas mismas rectas se vuelven **curvas en S**: el
efecto de `priorfrac` (la distancia entre curvas) es máximo en el centro y se
desvanece en los extremos, **aunque el OR sea constante**. Claramente se aprecia la diferencia constante en
los odds y variable en las probabilidades. 

```{r}
#| label: fig-u12-dos-escalas
#| fig-width: 11
#| fig-height: 4.8
#| out-width: "100%"
#| fig-cap: "El mismo modelo en dos escalas. Izquierda (log-odds): rectas paralelas, separación constante ≈ 0,84 (logaritmo del OR). Derecha (probabilidad): curvas en S, con separación variable. Trazo sólido y franja sombreada: rango de edad observado en GLOW (55–90 años); fuera, extrapolación (punteado). Bandas: IC 95 %."

rango_obs <- range(glow$age)        # ~55–90: edad observada en GLOW
grid <- expand_grid(
  age       = seq(40, 105, length.out = 300),
  priorfrac = factor(c("No", "Yes"), levels = levels(glow$priorfrac)))
pr   <- predict(fit_glm, newdata = grid, type = "link", se.fit = TRUE)
grid <- grid |>
  mutate(eta = pr$fit, se = pr$se.fit,
         lo_eta = eta - 1.96 * se, hi_eta = eta + 1.96 * se,   # IC 95 % en log-odds
         p_hat  = plogis(eta),
         lo_p   = plogis(lo_eta), hi_p = plogis(hi_eta))       # IC 95 % en probabilidad
grid_obs <- filter(grid, age >= rango_obs[1], age <= rango_obs[2])

pA <- ggplot(grid, aes(age, eta, color = priorfrac, fill = priorfrac)) +
  annotate("rect", xmin = rango_obs[1], xmax = rango_obs[2],
           ymin = -Inf, ymax = Inf, alpha = 0.06) +
  geom_ribbon(aes(ymin = lo_eta, ymax = hi_eta), alpha = 0.15, color = NA) +
  geom_line(linewidth = 0.7, linetype = "22") +          # extrapolación
  geom_line(data = grid_obs, linewidth = 1.2) +          # rango observado
  labs(x = "Edad (años)", y = "log-odds (η)",
       color = "Fractura previa", fill = "Fractura previa",
       title = "Escala log-odds", subtitle = "rectas paralelas (separación ≈ 0,84)")

pB <- ggplot(grid, aes(age, p_hat, color = priorfrac, fill = priorfrac)) +
  annotate("rect", xmin = rango_obs[1], xmax = rango_obs[2],
           ymin = -Inf, ymax = Inf, alpha = 0.06) +
  geom_ribbon(aes(ymin = lo_p, ymax = hi_p), alpha = 0.15, color = NA) +
  geom_line(linewidth = 0.7, linetype = "22") +
  geom_line(data = grid_obs, linewidth = 1.2) +
  scale_y_continuous(limits = c(0, 1), labels = scales::label_percent()) +
  labs(x = "Edad (años)", y = "P(fractura)",
       color = "Fractura previa", fill = "Fractura previa",
       title = "Escala probabilidad", subtitle = "curvas en S (separación variable)")

(pA + pB) +
  plot_layout(guides = "collect") &
  theme(legend.position = "bottom",
        plot.title    = element_text(size = 11, face = "bold"),
        plot.subtitle = element_text(size = 8.5))
```

## 2.4 Estimación: máxima verosimilitud e IWLS {#sec-u12-iwls}

A diferencia de OLS, **no hay fórmula cerrada** para $\hat{\boldsymbol\beta}$: se maximiza
la log-verosimilitud numéricamente. Pero ese "numéricamente" no es una caja negra; al
abrirlo aparece algo muy reconocible —una sucesión de regresiones lineales ponderadas—.
Veámoslo en tres pasos: las ecuaciones que hay que resolver, el algoritmo que las
resuelve y su reproducción a mano sobre GLOW.

**Las ecuaciones de score: ¿dónde están los $\boldsymbol\beta$?** 

La estimación de los coeficientes $\beta$ del predictor lineal pasa por maximizar la verosimilitud, o lo que es lo mismo, la log-verosimilitud $l(\beta)$. Maximizar
$\ell(\boldsymbol\beta)=\sum_i \ell_i$ exige anular su gradiente. Como los
coeficientes entran en la log-verosimilitud a través del predictor lineal, $\eta_i=\mathbf{x}_i^\top\boldsymbol\beta$, aplicando la regla de
la cadena en la derivación, $\ell_i\to\mu_i\to\eta_i\to\boldsymbol\beta$, y dado que que $\partial\eta_i/\partial\boldsymbol\beta=\mathbf{x}_i$, llegamos a:

$$
\frac{\partial \ell}{\partial\boldsymbol\beta}
=\sum_{i=1}^{n}
\underbrace{\frac{\partial \ell_i}{\partial\mu_i}}_{\textstyle \frac{y_i-\mu_i}{a(\phi)V(\mu_i)}}\;
\underbrace{\frac{\partial\mu_i}{\partial\eta_i}}_{\text{enlace}}\;
\underbrace{\frac{\partial\eta_i}{\partial\boldsymbol\beta}}_{\textstyle \mathbf{x}_i}
=\sum_{i=1}^{n}\frac{(y_i-\mu_i)}{a(\phi)\,V(\mu_i)}\,
\frac{\mathrm{d}\mu_i}{\mathrm{d}\eta_i}\,\mathbf{x}_i=\mathbf{0}.
$$

Resulta pues un sistema que iguala a cero una **suma ponderada de residuos** $(y_i-\mu_i)$, que no tiene solución cerrada y requiere de un procedimiento iterativo de optimización. 

**Del sistema al algoritmo: Fisher scoring = IWLS (*Iterative Weighted Least Squares*)** 

El método de **Newton–Raphson**,
usando la **información esperada** (*Fisher scoring*), resuelve ese sistema, y tiene una
lectura "interesante": **cada iteración equivale a una regresión por mínimos cuadrados
ponderados** (IWLS) [@nelder1972; @mccullagh1989]. En cada paso se fabrican: una
**respuesta de trabajo** $z_i$ (una linealización del enlace alrededor del ajuste actual)
y unos **pesos** $w_i$ (que dan más voz a las observaciones más precisas):

$$
z_i=\eta_i+(y_i-\mu_i)\frac{\mathrm{d}\eta_i}{\mathrm{d}\mu_i},
\qquad
w_i=\frac{(\mathrm{d}\mu_i/\mathrm{d}\eta_i)^2}{V(\mu_i)},
$$

y se actualiza $\boldsymbol\beta\leftarrow(\mathbf{X}^\top\mathbf{W}\mathbf{X})^{-1}
\mathbf{X}^\top\mathbf{W}\mathbf{z}$ —la fórmula de WLS— hasta que deja de cambiar. Así,
**estimar un GLM es resolver una sucesión de regresiones lineales ponderadas**.

::: {.callout-note}
## Recordatorio: Newton–Raphson y Fisher scoring
Para hallar la raíz de la ecuación de score $\partial\ell/\partial\boldsymbol\beta=\mathbf{0}$,
**Newton–Raphson** itera $\boldsymbol\beta\leftarrow\boldsymbol\beta-\mathbf{H}^{-1}\mathbf{s}$,
con $\mathbf{s}$ el score (gradiente) y $\mathbf{H}$ el hessiano (segunda derivada).
**Fisher scoring** sustituye $\mathbf{H}$ por su valor esperado, la **información esperada**
$\mathcal{I}=\mathbb{E}[-\mathbf{H}]$: más estable y, en un GLM, lo que convierte cada paso
en una regresión ponderada.
:::

::: {.callout-note}
## Recordatorio: mínimos cuadrados ponderados (WLS)
OLS minimiza $\sum_i (z_i-\mathbf{x}_i^\top\boldsymbol\beta)^2$ tratando todas las
observaciones por igual. **WLS** asigna a cada una un peso $w_i$ y minimiza
$\sum_i w_i\,(z_i-\mathbf{x}_i^\top\boldsymbol\beta)^2$, dando más influencia a las de
menor varianza; su solución es $\hat{\boldsymbol\beta}=(\mathbf{X}^\top\mathbf{W}\mathbf{X})^{-1}
\mathbf{X}^\top\mathbf{W}\mathbf{z}$, con $\mathbf{W}=\operatorname{diag}(w_i)$. El IWLS no
es más que aplicarla una y otra vez, recalculando $z$ y $w$ en cada vuelta.
:::

**Reproducción a mano sobre GLOW.** 

Reproducimos ahora el bucle con las piezas que el propio objeto
`family()` proporciona (`linkinv`, `mu.eta`, `variance`) y comprobamos que coincide con la solución de
`glm()`:

```{r}
#| label: u12-iwls
X   <- model.matrix(fit_glm)          # matriz de diseño: columnas (Intercept), age, priorfracYes
y   <- glow$fractura01                # respuesta binaria 0/1
fam <- binomial()                     # la familia expone las piezas: linkinv, mu.eta, variance

beta  <- rep(0, ncol(X))              # arranque: todos los coeficientes a 0
traza <- list()                       # aquí guardaremos beta de cada iteración (para la figura)

for (it in 1:25) {
  eta <- as.vector(X %*% beta)        # predictor lineal      eta = X beta
  mu  <- fam$linkinv(eta)             # media ajustada        mu  = g^{-1}(eta) = plogis(eta)
  mep <- fam$mu.eta(eta)              # derivada del enlace    dmu/deta = mu(1-mu)
  z   <- eta + (y - mu) / mep         # respuesta de trabajo  (linealización de un paso)
  w   <- mep^2 / fam$variance(mu)     # pesos IWLS             w = (dmu/deta)^2 / V(mu)
  WX  <- X * w                        # cada fila de X escalada por su peso
  # paso de mínimos cuadrados ponderados: beta = (X'WX)^{-1} X'W z
  beta_new <- as.vector(solve(crossprod(WX, X), crossprod(WX, z)))
  traza[[it]] <- beta_new             # registramos el beta de esta vuelta
  if (max(abs(beta_new - beta)) < 1e-10) { beta <- beta_new; break }  # ¿convergió?
  beta <- beta_new                    # si no, otra iteración partiendo del nuevo beta
}
cat("Convergió en", it, "iteraciones\n")
cbind(IWLS_a_mano = beta, glm = coef(fit_glm))   # deben coincidir con glm()
```

**La trayectoria hasta la convergencia.** 

En cada vuelta hemos ido guardando el vector de
coeficientes en la lista `traza`; ahora `do.call(rbind, traza)` **apila** esos vectores en
una **matriz** —una fila por iteración, una columna por coeficiente—, que pasamos a formato
largo para dibujar cómo evoluciona cada coeficiente. La @fig-u12-iwls-convergencia lo
muestra: partiendo de cero, los tres coeficientes alcanzan su valor de máxima verosimilitud
—las líneas punteadas de `glm()`— en apenas un puñado de iteraciones. Esa convergencia casi
inmediata es la marca del Fisher scoring cerca del óptimo.

```{r}
#| label: fig-u12-iwls-convergencia
#| fig-cap: "Convergencia del IWLS «a mano». Cada coeficiente se estabiliza en su valor de máxima verosimilitud (líneas punteadas, las de `glm()`) en muy pocas iteraciones."

do.call(rbind, traza) |>          # apila la lista en matriz: filas = iteraciones, columnas = coeficientes
  as.data.frame() |>
  setNames(colnames(X)) |>        # nombra las columnas como los coeficientes
  mutate(iter = row_number()) |>  # índice de iteración
  pivot_longer(-iter, names_to = "coef", values_to = "valor") |>   # formato largo para ggplot
  ggplot(aes(iter, valor, color = coef)) +
  geom_line() + geom_point(size = 1.5) +
  geom_hline(data = enframe(coef(fit_glm), "coef", "obj"),         # valores de glm() como referencia
             aes(yintercept = obj, color = coef), linetype = "dashed") +
  labs(x = "Iteración IWLS", y = "Coeficiente", color = NULL)
```


## 2.5 Inferencia y selección de modelos {#sec-u12-inferencia}

Una vez tenemos los coeficientes estimados, $\hat{\boldsymbol\beta}$, toca interrogar al modelo en tres niveles, de
lo particular a lo global:
1. ¿es real el efecto de cada coeficiente? 
1. ¿aporta conocimiento un bloque de variables? y 
1. ¿qué modelo elijo entre varias especificaciones? 

Las tres preguntas se responden con la misma moneda —la verosimilitud— y su sombra operativa, la **deviance**.

### La Deviance

Para medir cuánto se aleja un ajuste del mejor posible, se compara con el **modelo saturado**, que es el que tiene un parámetro por
observación, y por lo tanto reproduce los datos exactamente ($\hat\mu_i=y_i$) y proporciona el valor de
log-verosimilitud máxima $\ell_{\text{sat}}$. La **deviance** es esa distancia, en
unidades de log-verosimilitud, convertido en un valor positivo:
$$
D=-2\bigl[\ell(\hat{\boldsymbol\beta})-\ell_{\text{sat}}\bigr].
$$
La deviance viene a ser el análogo GLM de la suma de cuadrados residual: cuanto menor valor toma, mejor ajuste. 

La **deviance nula** $D_0$ es la del modelo solo con interceptación (el más simple que no asume ningún efecto de los predictores); la **deviance residual** $D$, representa la deviance del modelo ajustado.

:::: {.callout-tip}
## 🔧 En R. Deviances y grados de libertad de un `glm`
En un modelo `glm` ajustado, `fit_glm`, las deviances nula, del modelo saturado y del
modelo ajustado, junto con sus grados de libertad, se obtienen así:

```r
fit_glm$null.deviance     # deviance NULA (modelo solo con intercepto)
fit_glm$df.null           #   sus g.l.: n - 1

deviance(fit_glm)         # deviance del modelo AJUSTADO (= residual); igual a fit_glm$deviance
df.residual(fit_glm)      #   sus g.l.: n - p; igual a fit_glm$df.residual
```

La deviance del modelo **saturado es 0 por definición**: es el modelo de referencia —un
parámetro por observación, con g.l. residuales = 0— frente al que se mide *toda* deviance, de
modo que no hay una función que la devuelva (la "residual" ya es la distancia del modelo
ajustado al saturado).

Para mostrarlas todas conjuntamente, podemos utilizar `glance()`, de la librería `broom`, que
devuelve en una sola fila la `null.deviance` con `df.null`, la `deviance` residual con
`df.residual`, y además `logLik`, `AIC`, `BIC` y `nobs`:

```r
glance(fit_glm)
```
::::


::: {.callout-tip}
Detalle útil: con datos binarios **individuales**,
$\ell_{\text{sat}}=0$, de modo que $D=-2\,\ell(\hat{\boldsymbol\beta})$. 
:::

Mostramos las deviances y otros indicadores como el AIC y BIC, que nos serán útiles más adelante para la comparación de modelos:

```{r}
#| label: u12-deviance
glance(fit_glm) |>
  select(null.deviance, df.null, deviance, df.residual, logLik, AIC, BIC)
```

En GLOW, la deviance baja de **562,3** (nula) a **528,5** (residual) al añadir `age` y
`priorfrac`: esa reducción de ≈ 33,8 puntos es la materia prima de los contrastes que
siguen. 

La deviance residual **no** sirve como test *absoluto* de ajuste con datos
individuales (lo veremos en la [Sección 2.6](#sec-u12-diagnostico)). De momento la usaremos solo
para **comparar** modelos.


### Inferencia sobre los coeficientes 

Para contestar la pregunta "¿es real el efecto de cada coeficiente?", se utilizan los intervalos de confianza de Wald y de perfil.

**Test de Wald**

El test de Wald es el análogo directo de los tests $t$ del modelo lineal. Se construye con el cociente entre el estimador del coeficiente  y su error estándar:

$$
z_k=\frac{\hat\beta_k}{\widehat{\text{EE}}(\hat\beta_k)}.
$$
Este estadístico de Wald se compara con la normal asintótica (de ahí $z$ y no $t$), y es el que imprime
`summary()`/`tidy()`. Es un test cómodo, pero **poco fiable** con muestras pequeñas o coeficientes
grandes, y **engañoso bajo separación** (da por no significativo un efecto que en realidad es muy fuerte, debido a un inflado del error estándar -efecto [@hauckdonner1977]). 

**Intervalos de confianza: Wald frente a perfil.** 

Por la misma razón, los **intervalos de
confianza** se prefieren por **perfil de verosimilitud** antes que por Wald [@agresti2015]. El de **Wald**,
$\hat\beta_k\pm1{,}96\,\widehat{\text{EE}}$, aproxima la log-verosimilitud por una **parábola**
y es siempre simétrico. El de **perfil** se obtiene **invirtiendo la razón de verosimilitudes**:
es el conjunto de valores $b$ que el LRT no rechazaría, es decir, aquellos en los que la
**log-verosimilitud perfil**
$$
\ell_p(b)=\max_{\boldsymbol\beta_{-k}}\ \ell(b,\boldsymbol\beta_{-k})
$$
—la verosimilitud maximizada sobre el **resto** de parámetros con $\beta_k=b$ fijo— cae como
mucho $1{,}92=\tfrac12\chi^2_{1,\,0{,}95}$ por debajo de su máximo:
$$
\text{IC}_{95\%}(\beta_k)=\Bigl\{\,b:\ \ell(\hat{\boldsymbol\beta})-\ell_p(b)\le 1{,}92\,\Bigr\}.
$$
Geométricamente, se corta la curva de verosimilitud perfil a $1{,}92$ del pico y se toman las
dos abscisas del cruce. Como usa la **forma real** —a menudo **asimétrica**— de la
verosimilitud, no asume normalidad de $\hat\beta$ y **no tiene por qué ser simétrico**: es más
fiable justo cuando la parábola de Wald aproxima mal (muestras pequeñas, coeficientes grandes,
cerca de la separación). 

:::: {.callout-tip}
## 🔧 En R. Intervalos de confianza sobre los coeficientes
En R,

- `confint()` devuelve el intervalo de confianza del perfil de verosimilitud —el método de perfilado implementado en `MASS`
[@venablesripley2002]—
- `confint.default()`, el de Wald.
::::


Vemos que en GLOW, que se comporta bien, casi coinciden ambos intervalos:

```{r}
#| label: u12-confint
list(perfil = confint(fit_glm),
     wald   = confint.default(fit_glm)) |>
  map(\(m) round(m, 4))
```


### Inferencia sobre bloques: el contraste de razón de verosimilitudes (LRT)

Toda la inferencia que **compara modelos** se apoya en un mismo motor: el **contraste de razón de
verosimilitudes** (*likelihood ratio test*, LRT). Mide **cuánto mejora la verosimilitud** al pasar de
un modelo **pequeño** a otro **más grande que lo contiene** (anidado). El estadístico es
$$
\Lambda=2\big(\ell_{\text{grande}}-\ell_{\text{pequeño}}\big)=D_{\text{pequeño}}-D_{\text{grande}}\;\sim\;\chi^2_{\Delta\text{gl}},
$$
es decir, **la diferencia de deviances** —recuerda que $D=2(\ell_{\text{sat}}-\ell_{\text{modelo}})$,
así que el término del saturado se cancela—. Bajo $H_0$ ("el modelo pequeño basta; los parámetros
extra no aportan"), $\Lambda$ sigue una $\chi^2$ con tantos grados de libertad como **parámetros
añadidos**. Un $\Lambda$ **grande** ($p$ bajo) indica que esos parámetros **sí** explican señal real:
nos quedamos con el modelo grande.

Es **un único contraste** que, según qué par anidado compares, responde a preguntas distintas:

- **pequeño = nulo, grande = ajustado** → *«¿aporta algo el modelo?»* (el **ómnibus**, justo abajo).
- **pequeño = reducido, grande = completo** → *«¿aporta este bloque de variables?»* (la escalera de
  modelos que recorreremos enseguida).
- **grande = saturado** → *bondad de ajuste*: la **deviance residual** es el LRT contra el ajuste
  perfecto ([Sección 2.6](#sec-u12-diagnostico)).

Dos requisitos: los modelos han de estar **anidados** y ajustados sobre los **mismos datos**. Para
especificaciones **no anidadas** no hay LRT y se recurre a **AIC/BIC** (más abajo).

**El ómnibus** —"¿aporta *algo* el modelo?"— es el caso *nulo vs ajustado*, análogo al **test $F$
global** del modelo lineal:
$$D_0-D\sim\chi^2_{p}.$$
Para valorar el aporte de *cada término ajustando por el resto*, se echa mano del `Anova` de tipo II:

```{r}
#| label: u12-inferencia
# Wald en escala log-odds: aporte de cada término independientemente
tidy(fit_glm)  # proporcionado por summary(fit_glm)

# Anova tipo II: aporte de cada término ajustando por el resto
car::Anova(fit_glm, type = "II")

# LRT ómnibus: aporte del bloque completo frente al modelo nulo
anova(glm(fracture ~ 1, binomial, glow), fit_glm, test = "LRT")
```

:::: {.callout-tip}
## 🔧 En R. Contrastes de razón de verosimilitudes (LRT)
El LRT compara dos modelos **anidados** (ajustados sobre los **mismos datos**) por su diferencia de
deviances, con `anova(..., test = "LRT")` (equivale a `test = "Chisq"` en `glm`):

```r
# Ómnibus: ¿aporta algo el modelo frente al nulo (solo intercepto)?
anova(glm(fracture ~ 1, family = binomial, data = glow), fit_glm, test = "LRT")

# LRT entre dos modelos anidados cualesquiera (reducido vs completo)
anova(mod_reducido, mod_completo, test = "LRT")

# Aporte de CADA término ajustando por el resto (LRT por término)
car::Anova(fit_glm, type = "II")     # type = "III" si hay interacciones
```

`summary(fit_glm)` / `tidy(fit_glm)` dan el contraste **de Wald** por coeficiente (aproximado); el
**LRT** suele ser más fiable. Recuerda: el LRT **solo** vale para modelos **anidados**.
::::

### Comparar modelos no anidados: AIC y BIC

El LRT solo vale para comparar modelos anidados.
Para comparar especificaciones cualesquiera se usan criterios que penalizan la
verosimilitud por complejidad, como el AIC y el BIC [@akaike1974; @schwarz1978], que tienen en cuenta el número $k$ parámetros y $n$ observaciones:

$$
\text{AIC}=-2\ell+2k,\qquad \text{BIC}=-2\ell+k\log n,
$$

Menor es mejor, si bien el **BIC penaliza más** y por tanto **favorece la parsimonia**. Es importante remarcar que son medidas **relativas**, útiles para ordenar modelos, no para concluir si alguno ajusta bien en términos absolutos.

:::: {.callout-tip}
## 🔧 En R. AIC y BIC de un modelo
Sobre un modelo ajustado `fit_glm`:

```r
AIC(fit_glm)              # AIC
BIC(fit_glm)              # BIC
AIC(m_1, m_2, m_3)        # tabla con los AIC de varios modelos (BIC(...) para el BIC)
glance(fit_glm)           # broom: AIC, BIC, deviance, logLik, gl... en una fila
```

Menor es mejor; recuerda que son **relativos** (sirven para ordenar, no para juzgar el ajuste absoluto).
::::

**La escalera de modelos.**

Seleccionar el mejor modelo no es elegir entre dos concretos, sino **recorrer una escalera o
jerarquía** de modelos, de menos a más compleja (o viceversa), decidiendo en cada peldaño si la
complejidad añadida (o excluida) se paga "de modo suficiente" con ajuste. Para realizar esta
selección el analista propone un criterio y procede; los más habituales son LRT, AIC y BIC.  La
decisión final es **del analista**, y debe argumentarse.


A la hora de ajustar un mejor modelo sobre GLOW, consideramos diversos modelos de menos a más
complejos, con las variables `age`, `priorfrac`, `momfrac` (fractura de cadera en la madre) y
`bmi` (índice de masa corporal), teniendo en cuenta posibles interacciones entre factores o
entre factores y covariables (los dos factores son `priorfrac` y `momfrac`).


```{r}
#| label: u12-escalera
mod_0 <- glm(fracture ~ age + priorfrac,                     family = binomial, data = glow)
mod_1 <- glm(fracture ~ age + priorfrac + momfrac + bmi,     family = binomial, data = glow)
mod_2 <- glm(fracture ~ age * priorfrac + bmi + momfrac, family = binomial, data = glow)
mod_3 <- glm(fracture ~ (age + bmi) * priorfrac + momfrac, family = binomial, data = glow)
mod_4 <- glm(fracture ~ (age + bmi) * (priorfrac + momfrac), family = binomial, data = glow)
tibble(modelo = paste0("mod_", 1:4)) |>
  bind_cols(map_dfr(list(mod_1, mod_2, mod_3, mod_4),
                    \(m) glance(m) |> dplyr::select(df = df.residual, deviance, AIC, BIC)))
```

Los efectos principales **bajan la deviance con claridad**; las interacciones, menos. Lo
contrastamos peldaño a peldaño con LRT:

```{r}
#| label: u12-lrt-escalera
anova(mod_4, mod_3,  test = "LRT")   
anova(mod_3, mod_2, test = "LRT")   
anova(mod_2, mod_1, test = "LRT") 
anova(mod_1, mod_0, test = "LRT") 
```

El primer contraste contesta si son significativas (en bloque) las interacciones de edad y bmi con `momfrac`, y la respuesta es que no (p-valor 0.8554), por lo que cabe descartarlas para simplificar el modelo. La siguiente tabla Anova resuelve si tiene sentido la interacción de `bmi` con `priorfrac`, y la respuesta es no (p-valor 0.1595). A continuación se comparan los modelos `mod_2` y `mod_1`, esto es, si es significativa la interacción `age:priorfrac` y el resultado es sí, con un p-valor de 0.0162. Llegado a este punto, pararíamos y nos quedaríamos con el `mod_2`. La última comparación valora si merece la pena incluir las variables `momfrac` y `bmi` en el modelo, y la respuesta es nuevamente que sí (p-valor 0.036), pues amplían la información que dan exclusivamente `age` y `priorfrac`. 


Interpretar una interacción exige cuidado: el coeficiente de `priorfrac` ya **no** es "el efecto
de la fractura previa", sino su efecto **a edad 0** (sin sentido sin centrar). Por eso, con
interacciones, la lectura se hace en **efectos marginales o predicciones** (ya vista en la
[Sección 2.3](#sec-u12-interpretacion)), no en coeficientes crudos:

```{r}
#| label: u12-interaccion-coef
tidy(mod_2) 
```

:::: {.callout-tip}
## 🔧 En R. Automatizar la escalera: `dredge` (exhaustiva) y `stepAIC` (paso a paso)
Con varias variables e interacciones el número de modelos crece deprisa. Dos funciones lo
automatizan, con filosofías opuestas; ambas parten de un **modelo global** ajustado con
`na.action = na.fail` (requisito de `dredge`):

```r
global <- glm(fracture ~ (age + bmi) * (priorfrac + momfrac),
              family = binomial, data = glow, na.action = na.fail)
```

**`MuMIn::dredge` — exhaustiva** [@mumin]: ajusta **todos** los submodelos (respetando la
marginalidad) y los ordena por el criterio elegido:

```r
sel   <- dredge(global, rank = "AIC")        # rank = "BIC" para favorecer la parsimonia
head(sel, 3)                                 # mejores modelos
mejor <- get.models(sel, subset = 1)[[1]]    # el modelo ganador, ya ajustado
formula(mejor)                               # solo su fórmula
```

**`MASS::stepAIC` — voraz / paso a paso** [@venablesripley2002]: añade o quita un término cada vez
siguiendo el AIC, sin recorrer todo el espacio (más rápida, pero puede quedarse en un óptimo local):

```r
m_step <- MASS::stepAIC(global, direction = "both", trace = FALSE)  # 'both'/'forward'/'backward'; devuelve YA el modelo ganador
formula(m_step)                                                     # solo su fórmula
```

`dredge` recorre **todo** el espacio; `stepAIC`, **un solo camino**: si coinciden, refuerza la
elección; si discrepan, hay varios modelos casi indistinguibles.
::::

Las comparamos entre sí:

```{r}
#| label: u12-auto
#library(MuMIn)
global <- glm(fracture ~ (age + bmi) * (priorfrac + momfrac),
              family = binomial, data = glow, na.action = na.fail)  # na.fail: requisito de dredge

sel <- dredge(global, rank = "AIC")       # todos los submodelos, ordenados por AIC (respeta marginalidad)
head(sel, 3)                              # los cinco mejores
dredge(global, rank = "BIC") |> head(3)   # el BIC, más parsimonioso, suele elegir otro

# Mejor por AIC (exhaustivo) frente a la búsqueda paso a paso:
formula(get.models(sel, subset = 1)[[1]])                          # ganador de dredge
formula(MASS::stepAIC(global, direction = "both", trace = FALSE))  # ganador de stepAIC
```


En este caso, ambos métodos han llegado al mismo óptimo. 

:::: {.callout-tip}
## 🔧 En R. Generar modelos anidados con `MuMIn` (`dredge`)
En cualquier caso, cabe tener como referencia que a partir de un modelo más complejo, podemos extraer todos los posibles modelos (anidados) viables con la librería `MuMIn`, para compararlos por cualquier criterio.

```
library(MuMIn)
# Definimos el modelo más complejo de partida
global <- glm(fracture ~ (age + bmi )* (priorfrac + momfrac ) + priorfrac:momfrac,
              family = binomial, data = glow, na.action = na.fail)  # na.fail: requisito de dredge

calls <- dredge(global, evaluate = FALSE)     # NO ajusta: solo genera las llamadas/fórmulas
length(calls)                                 # cuántos modelos hay
calls[[10]]                                     # una llamada concreta
formulas = lapply(calls, function(cl) formula(eval(cl))) # extrae las fórmulas, si las quieres como tales
formulas[[10]]
```
::::

Conviene leer las tres herramientas **juntas**, no como árbitros que se sustituyan: el **LRT**
dice qué términos aportan señal real (aquí, `age:priorfrac` sí; `priorfrac:momfrac` no);
**dredge** y **stepAIC**, al optimizar AIC, tienden a un modelo de buena predicción, pero pueden
discrepar entre sí —dredge ve todo el espacio; `step`, solo un camino— y, sobre todo, del **BIC**,
más parsimonioso. Que dos algoritmos converjan o no es ya información: si discrepan, varios modelos
son casi indistinguibles y la elección no debe delegarse al criterio.

::: {.callout-warning}
## Sobre la selección automática (`step`, `dredge`)
La búsqueda automática —paso a paso (`MASS::stepAIC`) o exhaustiva (`MuMIn::dredge`)— es útil
como **exploración**, pero no como árbitro. Ambas invalidan los $p$-valores posteriores
(inferencia **post-selección**) y favorecen el sobreajuste; la exhaustiva, además, **agrava la
multiplicidad** al comparar muchísimos modelos. En este curso las usamos para mapear el espacio,
y la decisión final se justifica con LRT/AIC/BIC, validación y conocimiento del problema
[@harrell2015].
:::

::: {.callout-tip}
## Protocolo de inferencia y selección
1. **¿Aporta algo el modelo?** Contraste ómnibus (LRT nulo vs completo).
2. **¿Aporta cada término?** LRT / `Anova` tipo II por término, con Wald e IC (mejor por
   perfil) como lectura individual.
3. **¿Qué especificación?** LRT entre anidados; AIC/BIC entre cualesquiera (AIC → predicción,
   BIC → parsimonia).
4. **Selección de variables.** Recorre la escalera **guiada por el problema**, no a ciegas;
   combina LRT/AIC/BIC; evita la búsqueda automática como árbitro; **valida y argumenta** la
   elección.
:::

## 2.6 Bondad de ajuste y diagnóstico {#sec-u12-diagnostico}

En 2.5 preguntamos si el modelo **aporta** y **cuál** elegir. Ahora la pregunta es
otra: el modelo elegido, **¿ajusta bien los datos?** Es la **bondad de ajuste**, y la
bibliografía la trata de la mano del **análisis de residuos** [@hosmer2013; @faraway2016;
@agresti2015], porque ambos responden a lo mismo desde dos ángulos: un **contraste
global** y una **mirada caso a caso**.

### El contraste global: ¿aportar o ajustar? (y por qué falla en binaria)

Conviene situar tres modelos en una **escalera**, del peor al mejor ajuste posible: el **nulo**
(solo intercepto, deviance $D_0$); el **ajustado**, el nuestro (deviance residual $D$); y el
**saturado**, con un parámetro por observación, que **ajusta los datos a la perfección**
(deviance $=0$). Con ellos se responden **dos preguntas distintas que no hay que mezclar**:

- **¿Aporta el modelo?** (utilidad, la de la [Sección 2.5](#sec-u12-inferencia)). Se compara
  **ajustado vs nulo**: $D_0-D\sim\chi^2_{p}$. Un $p$ bajo **rechaza el nulo** a favor del nuestro
  —las covariables explican algo—. Es la pregunta *«¿mejor que nada?»*.
- **¿Ajusta bien?** (bondad de ajuste, la de aquí). Se compara **ajustado vs saturado**: la propia
  **deviance residual** $D=2(\ell_{\text{sat}}-\ell_{\text{ajustado}})$ **es** el LRT frente al mejor
  ajuste posible y, bajo un modelo correcto, se distribuiría $\chi^2_{n-p}$. Mide *«¿cuánto queda sin
  explicar respecto a un ajuste perfecto?»*; un $p$ bajo señala **falta de ajuste**.

¿Por qué el **saturado** y no el nulo es la referencia de la *bondad de ajuste*? Porque "ajustar
bien" es **acercarse al mejor ajuste alcanzable**, no solo superar a "no ajustar nada": un modelo
puede **batir al nulo** y, a la vez, quedar **lejos del saturado** (forma funcional pobre, enlace
equivocado, no linealidad ignorada). Aportaría, pero no ajustaría. Por eso son contrastes
**complementarios**, no intercambiables.

**Y aquí está el problema en binaria.** El $\chi^2_{n-p}$ exige un régimen asintótico concreto: que
el **número de celdas** —los parámetros del saturado— quede **fijo** mientras la **información
dentro de cada celda crece**. Con datos **agrupados** (binomial, $m_i$ ensayos por celda) se
cumple: hay un número fijo de celdas y, al aumentar $m_i$, cada proporción $\hat\pi_i$ se estima con
precisión. Con respuesta **binaria individual** ($m_i=1$, una fila por sujeto) ocurre lo contrario:
cada "celda" es **una sola** observación, así que al crecer $n$ **crece también el saturado** —un
parámetro por dato, estimado siempre con un único $0/1$—; nunca se acumula información por
parámetro, $D$ **no** sigue $\chi^2_{n-p}$ y el contraste **carece de valor**. De hecho, en GLOW
$\ell_{\text{sat}}=0$ y $D=-2\ell(\hat{\boldsymbol\beta})$ depende **solo** de las probabilidades
ajustadas, sin informar del ajuste absoluto [@agresti2015; @hosmer2013].

**Cómo se resuelve, entonces.** Dos vías: (i) **agrupar** —si las covariables son categóricas, se
pasa a binomial: tanto $D$ como el **estadístico de Pearson** $X^2$ **recuperan su sentido** como
contraste $\chi^2$, y allí los definimos con detalle ([Sección 3.2](#sec-u13-gof-binomial))—; (ii) cuando no se
puede (covariables continuas, como en GLOW), se sustituye el contraste global por herramientas que
**agrupan implícitamente** o **simulan**: el **test de Hosmer–Lemeshow** (deciles de riesgo), la
**curva de calibración** y los **residuos por tramos**, que vemos a continuación.

### Residuos

Para diagnosticar un GLM **observación a observación** se examinan los **residuos**. Los estándar de
un GLM son los de **Pearson** y los de **deviance** [@mccullagh1989];
son informativos cuando hay **suficiente información por observación** —datos agrupados, conteos
altos—, y los desarrollamos en propiedad (fórmulas, gráfico y variantes —estandarizados, leverage,
distancia de Cook—) en la [Sección 3.2](#sec-u13-gof-binomial), donde **sí** funcionan.

**El problema en binaria.** Con respuesta **binaria individual**, $y_i\in\{0,1\}$: para cada
$\hat\mu_i$ el residuo crudo solo toma dos valores y se ordena en **dos bandas**, y ni Pearson ni
deviance llegan a ser $\approx N(0,1)$. Hacen falta herramientas que **rescaten** el diagnóstico, y
conviene elegirlas **según para qué**:

- **Diagnóstico general** (mala especificación de cualquier tipo: forma funcional, dispersión,
  *outliers*) → los **residuos simulados / de cuantil** (DHARMa) [@dunnsmyth1996; @hartig2022]. Se
  simula muchas veces del modelo ajustado y se sitúa cada observación en su distribución simulada;
  bajo un modelo correcto son **uniformes** $(0,1)$, comparables con un Q–Q y un test de
  Kolmogorov–Smirnov. No son "de binaria": valen para **cualquier** GLM —son, simplemente, el
  rescate cuando los estándar fallan—.
- **Diagnóstico de calibración** (¿son fiables las probabilidades, y en qué tramo fallan?) → los
  **residuos por tramos** (*binned*), que promedian observado − predicho por tramos de riesgo. Como
  responden a la pregunta de la calibración, los tratamos **donde se usan**: en la subsección
  siguiente.

:::: {.callout-tip}
## 🔧 En R. Residuos simulados (DHARMa)

```r
sim <- DHARMa::simulateResiduals(fit_glm)   # plot = TRUE por defecto
plot(sim)                                   # Q–Q (con test KS) + residuos vs predicción
DHARMa::testResiduals(sim)                  # tests formales: uniformidad, dispersión, outliers
```

Los de Pearson y deviance, con `residuals(fit_glm, type = "pearson" | "deviance")`.
::::

```{r}
#| label: fig-u12-dharma
#| fig-width: 9
#| fig-height: 4
#| fig-cap: "Residuos simulados (DHARMa) del modelo logístico en GLOW. Izquierda: Q–Q de los residuos, que deberían seguir la diagonal uniforme. Derecha: residuos frente a predicción, sin patrón si el ajuste es adecuado."
sim <- DHARMa::simulateResiduals(fit_glm, plot = FALSE)
plot(sim)
```

En GLOW, los residuos simulados **no muestran ningún problema**: el Q–Q sigue la diagonal uniforme
(test de Kolmogorov–Smirnov $p\approx0{,}81$) y no hay señal de **sobredispersión** ($p\approx0{,}87$)
ni de **outliers** ($p\approx1$); a la derecha, los residuos se reparten **sin patrón** frente a la
predicción. Ninguna evidencia, por tanto, de mala especificación —ni en la forma, ni en la
dispersión, ni en casos atípicos—.

### Calibración: ¿son fiables las probabilidades?

Confronta la probabilidad **predicha** con la frecuencia **observada**. El contraste de referencia
es el **test de Hosmer–Lemeshow** (HL) [@hosmer1980]: se ordenan las $n$ observaciones por su
probabilidad predicha $\hat\pi_i$ y se reparten en $G$ grupos de igual tamaño (habitualmente $G=10$,
deciles de riesgo). En cada grupo $g$ se cuentan los eventos **observados** $O_g=\sum_{i\in g}y_i$ y
los **esperados** $E_g=\sum_{i\in g}\hat\pi_i=n_g\bar\pi_g$ (con $\bar\pi_g$ la probabilidad media
predicha del grupo), y el estadístico los confronta:
$$
\hat C=\sum_{g=1}^{G}\frac{(O_g-E_g)^2}{n_g\,\bar\pi_g(1-\bar\pi_g)}.
$$
Bajo $H_0$ («el modelo calibra bien»), $\hat C$ se distribuye aproximadamente $\chi^2_{G-2}$ —los
$G-2$ grados de libertad, establecidos por simulación, descuentan dos por el agrupamiento y la
estimación—. Un $\hat C$ **grande** ($p$ bajo) indica que observado y esperado **discrepan** → mala
calibración; un $p$ **alto** señala **ausencia de evidencia** de mala calibración (no «prueba» que
calibre bien).

```{r}
#| label: u12-hosmer
performance::performance_hosmer(fit_glm, n_bins = 10)
```

En GLOW, $p\approx 0{,}21$: sin evidencia de mala calibración. Pero el HL tiene
**limitaciones conocidas** —sensible al número de grupos y poco potente—, así que la
práctica moderna lo **acompaña de un gráfico de calibración** y de los residuos por tramos
[@agresti2015; @harrell2015]:

```{r}
#| label: fig-u12-calibracion
#| fig-width: 10
#| fig-height: 4.5
#| out-width: "100%"
#| fig-cap: "Diagnóstico visual. Izquierda: residuos por tramos (deberían caer dentro de las bandas de ±2 EE). Derecha: curva de calibración por deciles de riesgo (debería seguir la diagonal)."

diag <- tibble(p_hat = fitted(fit_glm), y = glow$fractura01)

# --- Panel A: residuos por tramos (estilo binnedplot, en ggplot) ---
binned <- diag |>
  mutate(tramo = ntile(p_hat, 20)) |>
  group_by(tramo) |>
  summarise(p_med = mean(p_hat),
            r_med = mean(y - p_hat),
            se    = sqrt(mean(p_hat * (1 - p_hat)) / n()), .groups = "drop")

pA <- ggplot(binned, aes(p_med, r_med)) +
  geom_ribbon(aes(ymin = -2 * se, ymax = 2 * se), alpha = 0.15) +
  geom_hline(yintercept = 0, linetype = "dashed") +
  geom_point(size = 1.8) +
  labs(x = "Probabilidad ajustada", y = "Residuo medio por tramo",
       title = "Residuos por tramos")

# --- Panel B: calibración por deciles ---
calib <- diag |>
  mutate(decil = ntile(p_hat, 10)) |>
  group_by(decil) |>
  summarise(pred = mean(p_hat), obs = mean(y), .groups = "drop")

pB <- ggplot(calib, aes(pred, obs)) +
  geom_abline(slope = 1, intercept = 0, linetype = "dashed") +   # calibración perfecta
  geom_line() + geom_point(size = 1.8) +
  coord_equal(xlim = c(0, 1), ylim = c(0, 1)) +
  labs(x = "Riesgo predicho (media por decil)", y = "Frecuencia observada",
       title = "Calibración")

pA + pB        # patchwork: lado a lado
```

Ambos paneles convierten el "pasa / no pasa" del HL en un **diagnóstico localizado**: dicen
no solo *si* hay problema, sino *dónde*, *en qué sentido* y *qué* podría corregirlo.

::: {.callout-tip}
## Cómo leer los dos gráficos
**Curva de calibración** (probabilidad predicha en X, frecuencia observada en Y; la referencia
es la diagonal de 45°):

- **Sobre la diagonal** → bien calibrado: entre los casos con riesgo predicho $p$, una
  fracción $\approx p$ tiene el evento.
- **Por debajo de la diagonal** (sobre todo en riesgo alto) → el modelo **sobreestima** el
  riesgo en esa zona.
- **Por encima de la diagonal** (sobre todo en riesgo bajo) → **subestima**.
- **Forma en S** alrededor de la diagonal → exceso o defecto de confianza en los extremos.
- Lo informativo es **en qué tramo** de riesgo aparece la desviación, no solo que exista.

**Residuos por tramos** (residuo medio por tramo en Y, probabilidad media en X, con bandas de
$\pm 2$ EE $\approx 95\,\%$):

- **Dispersos en torno a 0 y dentro de las bandas** → sin falta de ajuste sistemática.
- **Patrón sistemático** (p. ej. negativos en riesgo bajo y positivos en alto, o una curva)
  → el modelo **yerra la media** en ese rango.
- **Más de ~5 % de puntos fuera de las bandas** → señal de mal ajuste.
- Si se representa el residuo medio por tramos frente a una **covariable** (la edad, por
  ejemplo) en lugar de frente a la probabilidad ajustada, un patrón indica que **esa variable
  pide un término no lineal** (un cuadrático) o una interacción: orienta sobre **qué** cambiar.
:::


En GLOW (@fig-u12-calibracion), coherente con el HL ($p\approx0{,}21$): los residuos por tramos
quedan dentro de las bandas y la calibración se ajusta a la diagonal, sin señales de descalibración
ni de falta de ajuste en el rango observado.

:::: {.callout-tip}
## 🔧 En R. Hosmer–Lemeshow y curva de calibración
**Test de Hosmer–Lemeshow:**

```r
performance::performance_hosmer(fit_glm, n_bins = 10)
ResourceSelection::hoslem.test(glow$fractura01, fitted(fit_glm), g = 10)   # alternativa
```

**Residuos por tramos (binned):**

```r
plot(performance::binned_residuals(fit_glm))   # observado − predicho promediado por tramo, ±2 EE
```

**Curva de calibración** (riesgo predicho vs frecuencia observada, por deciles):

```r
tibble(p = fitted(fit_glm), y = glow$fractura01) |>
  mutate(grupo = ntile(p, 10)) |>
  group_by(grupo) |> summarise(pred = mean(p), obs = mean(y)) |>
  ggplot(aes(pred, obs)) +
  geom_abline(linetype = "dashed") + geom_point() + geom_line()
```

(Curva suavizada e índices: `rms::val.prob()`, `performance::check_model()`.)
::::



### Separación: cuando el ajuste "perfecto" rompe la inferencia. 

Es el patológico que vuelve inútil al Wald. Hablamos de "separación " cuando una combinación de covariables **predice perfectamente** el resultado en algún subgrupo: la verosimilitud sigue creciendo al alejar
el coeficiente hacia $\pm\infty$, así que `glm()` devuelve estimaciones **enormes con
errores estándar gigantescos** [@albert1984]. 

GLOW lo exhibe de forma natural: entre las
fumadoras que autoperciben su riesgo como «menor» (`smoke = Yes`, `raterisk = Less`) **no
hay ninguna fractura** (0 de 12), lo que separa ese grupo.

```{r}
#| label: u12-separacion
glow |> count(smoke, raterisk, fracture) |>
  filter(smoke == "Yes", raterisk == "Less")        # celda con 0 fracturas

fit_sep <- glm(fracture ~ smoke * raterisk, binomial, glow)
tidy(fit_sep) |> filter(str_detect(term, "smoke"))  # coef enorme, EE descomunal

```

El coeficiente de `smokeYes` ronda **−15 con un EE de varios cientos**: síntoma inequívoco
de cuasi-separación, no un efecto «protector» real. El Wald carece de sentido aquí.

La solución es detectarla (`detectseparation::detect_separation`) y, si aparece, estimar por
**verosimilitud penalizada de Firth** [@firth1993; @heinze2002], que produce estimaciones
finitas:

```{r}
#| label: u12-firth
# install.packages("logistf")  # o brglm2::brglm_fit
# La verosimilitud penalizada de Firth devuelve estimaciones FINITAS e inferencia con sentido:
logistf::logistf(fracture ~ smoke * raterisk, data = glow)
```

Se ve inmediatamente la corrección: el término de `smoke` pasa de $\approx-15$ con EE de cientos y
$p\approx1$ (Wald, roto) a una estimación **finita**, con IC y $p$ razonables (Firth).


:::: {.callout-tip}
## 🔧 En R. Detectar la separación y estimar por verosimilitud penalizada (Firth)
**Detección** (antes de fiarte de un `glm` con coeficientes enormes y EE descomunales; también
avisa el warning *"fitted probabilities numerically 0 or 1 occurred"*):

```r
detectseparation::detect_separation(fracture ~ smoke * raterisk,
                                    family = binomial, data = glow)   # marca los términos separados
```

**Estimación penalizada de Firth** —penaliza la verosimilitud (corrección de Jeffreys), de modo que
los coeficientes quedan **finitos** incluso bajo separación, con inferencia por verosimilitud de
perfil—:

```r
logistf::logistf(fracture ~ smoke * raterisk, data = glow)            # coef, IC y p finitos
glm(fracture ~ smoke * raterisk, family = binomial, data = glow,
    method = brglm2::brglm_fit)                                       # alternativa (brglm2)
```
::::

## 2.7 Evaluación como clasificador {#sec-u12-evaluacion}

El modelo entrega **probabilidades**; convertirlo en un **clasificador** (predecir sí/no)
exige un **umbral** $c$ y abre un tercer eje de evaluación del modelo: la **discriminación** —¿separa
eventos de no-eventos?—. Este enfoque de evaluación es distinta al que se realiza sobre la bondad del ajuste (en la [Sección 2.6](#sec-u12-diagnostico) y sobre la utilidad inferencial (en la [Sección 2.5](#sec-u12-inferencia)).

### Del umbral a la matriz de confusión.

Fijado un umbral $c \in [0,1]$,  por defecto $c=0{,}5$,  clasificamos con una observación en función de su predicción $\hat\pi_i$ con:

$$\hat y_i=\mathbf{1}[\hat\pi_i\ge c]$$  

Para evaluar la calidad de la clasificación, tenemos fácil la comparación entre lo observado (los datos iniciales 0/1) y lo predicho al clasificar. El cruce de lo predicho con lo observado define la **matriz de confusión**, punto de partida de todas las métricas.

**Las cuatro celdas.** 

Con un umbral dado $c$, cada caso cae en una de cuatro celdas según coincidan predicción y realidad:
**verdadero positivo** (TP, *true positive*), **falso positivo** (FP, *false positive*),
**falso negativo** (FN, *false negative*) y **verdadero negativo** (TN, *true negative*).
De ahí salen las métricas más habituales (nombradas en inglés porque es así como las nombra casi
todo el software):

- **Exactitud** — *accuracy*: $\dfrac{TP+TN}{\text{total}}$. Proporción global de aciertos.
- **Sensibilidad** o *recall* — *sensitivity / true positive rate*: $\dfrac{TP}{TP+FN}$. De los
  eventos reales, cuántos detecta.
- **Especificidad** — *specificity / true negative rate*: $\dfrac{TN}{TN+FP}$. De los no-eventos
  reales, cuántos descarta.
- **Precisión** o valor predictivo positivo — *precision / positive predictive value (PPV)*:
  $\dfrac{TP}{TP+FP}$. De los que predice como eventos, cuántos lo son de verdad.

Sensibilidad y precisión suelen estar **en tensión** (bajar el umbral detecta más eventos pero
genera más falsas alarmas); cuando ambas importan se utiliza el 

- **F1** (*F1-score*), la media armónica de sensibilidad y precisión,
$2\,\dfrac{\text{precisión}\cdot\text{recall}}{\text{precisión}+\text{recall}}$.

**Cuál mirar.** 

A la hora de valorar la calidad de la clasificación, cabe tener en cuenta estas consideraciones:

- La **exactitud engaña con clases desbalanceadas**: si una clase es rara,
"clasificar todo como la mayoritaria" logra una exactitud alta y es inútil. Conviene entonces
mirar **sensibilidad, especificidad y precisión por separado** y elegir según el **coste de cada
error**: la sensibilidad prima cuando un falso negativo es caro (no detectar un evento); la
precisión, cuando lo caro es la falsa alarma. 
- Además, sensibilidad y especificidad son
**invariantes a la prevalencia** (son propiedades del clasificador dada la clase real), mientras
que exactitud y precisión **sí dependen** de ella; por eso, y para resumir todos los umbrales a la
vez, conviene complementarlas con el **AUC**, también invariante a la prevalencia.

Calculamos a mano la matriz de confusión para GLOW.

```{r}
#| label: u12-confusion
# --- A mano (para ver qué es cada cosa) ---
umbral <- 0.5
pred   <- factor(ifelse(fitted(fit_glm) >= umbral, "Yes", "No"),
                 levels = c("No", "Yes"))
cm <- table(Predicho = pred, Observado = glow$fracture)
cm

TP <- cm["Yes", "Yes"]; FP <- cm["Yes", "No"]
FN <- cm["No",  "Yes"]; TN <- cm["No",  "No"]
c(exactitud     = (TP + TN) / sum(cm),   # accuracy
  sensibilidad  = TP / (TP + FN),        # sensitivity / recall
  especificidad = TN / (TN + FP),        # specificity
  precision     = TP / (TP + FP)) |> round(3)   # precision / PPV
```

En la práctica no se calcula a mano: `yardstick` [@yardstick] devuelve la matriz y todas las métricas en una
llamada. El único cuidado es declarar la **clase positiva**; como aquí `Yes` es el **segundo**
nivel del factor, se indica con `event_level = "second"` (si no, sensibilidad y especificidad
saldrían intercambiadas):

```{r}
#| label: u12-confusion-yardstick
library(yardstick)
eval_df <- tibble(obs = glow$fracture, pred = pred)

conf_mat(eval_df, truth = obs, estimate = pred)        # misma matriz de confusión

metric_set(accuracy, sensitivity, specificity, precision, recall, f_meas)(
  eval_df, truth = obs, estimate = pred, event_level = "second")
```


:::: {.callout-tip}
## 🔧 En R. Matriz de confusión y métricas
A partir de la respuesta observada y la predicha (factores con los **mismos niveles**), `yardstick`
da todo en una llamada; el único cuidado es declarar la **clase positiva** con `event_level`:

```r
library(yardstick)
pred    <- factor(ifelse(fitted(fit_glm) >= 0.5, "Yes", "No"), levels = c("No", "Yes"))
eval_df <- tibble(obs = glow$fracture, pred = pred)

conf_mat(eval_df, truth = obs, estimate = pred)                       # matriz de confusión
metric_set(accuracy, sensitivity, specificity, precision, recall, f_meas)(
  eval_df, truth = obs, estimate = pred, event_level = "second")      # 'second' = positivo en el 2º nivel
```

Alternativa: `caret::confusionMatrix(pred, obs, positive = "Yes")`.
::::

### ROC y AUC

Todas las métricas anteriores dependen de un **umbral fijo** para clasificar a los sujetos en Yes/No ($c=0{,}5$ por
defecto): cambiar el corte cambia sensibilidad, especificidad y precisión. Para evaluar el
clasificador **con independencia del umbral** —y, como acabamos de ver, de la prevalencia— se
recorre el corte de 0 a 1 y se resume el compromiso entre acertar eventos y evitar falsas
alarmas. Eso es la curva **ROC** y su área, el **AUC** [@robin2011].

La **curva ROC** (*receiver operating characteristic*) recorre **todos los umbrales** $c\in[0,1]$ y,
para cada uno, dibuja la **sensibilidad** (tasa de verdaderos positivos, TPR) frente a
**1 − especificidad** (tasa de falsos positivos, FPR):

\begin{eqnarray}
\text{TPR}(c)&=&\frac{TP(c)}{TP(c)+FN(c)}=\Pr(\hat\pi\ge c\mid Y=1),\\
\text{FPR}(c)&=&\frac{FP(c)}{FP(c)+TN(c)}=\Pr(\hat\pi\ge c\mid Y=0).
\end{eqnarray}

La curva es el conjunto de puntos $\{(\text{FPR}(c),\,\text{TPR}(c)):c\in[0,1]\}$; cada punto es un
clasificador posible. La **diagonal** es el azar (TPR = FPR), y cuanto más se arrima la curva a la
**esquina superior izquierda**, mejor separa (ver @fig-u12-roc).

El **AUC** (*area under the curve*) es el área bajo esa curva,
$$
\text{AUC}=\int_0^1 \text{TPR}\,\mathrm{d}(\text{FPR}),
$$
con $0{,}5$ = azar y $1$ = perfecto. Tiene una lectura **probabilística** elegante (equivale al
estadístico de Mann–Whitney): es la **probabilidad de que el modelo asigne más riesgo a un evento
que a un no-evento** tomados al azar,
$$
\text{AUC}=\Pr(\hat\pi_i>\hat\pi_j\mid Y_i=1,\,Y_j=0),
$$
que se estima como la **proporción de pares (evento, no-evento) bien ordenados** (los empates cuentan
media). Al promediar sobre todos los umbrales, mide la **discriminación** con independencia del corte
y de la prevalencia.

```{r}
#| label: fig-u12-roc
#| fig-cap: "Curva ROC del modelo logístico en GLOW. El AUC resume la capacidad de discriminación agregada sobre todos los umbrales."
roc_obj <- pROC::roc(glow$fracture, fitted(fit_glm), quiet = TRUE)
pROC::auc(roc_obj)
pROC::ggroc(roc_obj) +
  geom_abline(slope = 1, intercept = 1, linetype = "dashed") +
  labs(x = "Especificidad", y = "Sensibilidad")
```

El AUC ≈ **0,68**: discriminación **modesta**, coherente con que la fractura depende de
muchos factores no incluidos. Un AUC alto no garantiza buenas probabilidades: eso es
**calibración**, ya vista en la [Sección 2.6](#sec-u12-diagnostico).

**Elegir el umbral es una decisión, no un dato.** 

El $0{,}5$ por defecto rara vez es óptimo:
depende del **coste relativo** de un falso positivo frente a un falso negativo. Un criterio habitual
y **simétrico** (trata ambos errores por igual) es el **índice de Youden**,
$$
J(c)=\text{sensibilidad}(c)+\text{especificidad}(c)-1=\text{TPR}(c)-\text{FPR}(c),
$$
con $J\in[0,1]$ ($0$ = no discrimina, sobre la diagonal; $1$ = perfecto). El umbral elegido es el que
**maximiza** $J$,
$$
c^\star=\arg\max_{c}\big[\text{TPR}(c)-\text{FPR}(c)\big],
$$
que geométricamente es el punto de la curva ROC **más alejado verticalmente de la diagonal**. Cuando
los costes de los dos errores **no** son simétricos, Youden deja de ser apropiado y se elige el
umbral que **minimiza el coste esperado** (ponderando FP y FN por su coste).

```{r}
#| label: u12-umbral
pROC::coords(roc_obj, "best", best.method = "youden",
             ret = c("threshold", "sensitivity", "specificity"))
```

:::: {.callout-tip}
## 🔧 En R. Curva ROC, AUC y umbral de Youden
Con `pROC`, a partir de la respuesta observada y las probabilidades predichas:

```r
library(pROC)
roc_obj <- roc(glow$fracture, fitted(fit_glm))     # (respuesta, predictor)
auc(roc_obj)                                        # AUC ; ci.auc(roc_obj) para su IC 95%
ggroc(roc_obj)                                      # la curva ROC (ggplot)

coords(roc_obj, "best", best.method = "youden",     # umbral óptimo de Youden
       ret = c("threshold", "sensitivity", "specificity"))
```

Alternativa con `yardstick`: `roc_auc(eval_df, truth, prob, event_level = "second")` y `roc_curve(...)`.
::::

::: {.callout-note}
## Tres preguntas, tres herramientas
Con el fin de resumir lo trabajado en esta unidad al respecto de cómo evaluar y analizar qué nos proporciona un ajuste, hemos respondido una serie de preguntas, y proporcionado una serie de herramientas con las que contestarlas, y son:

- **¿Aporta? ¿qué modelo?** (utilidad e inferencia) → Wald, LRT ómnibus, AIC/BIC ([2.5](#sec-u12-inferencia)).
- **¿Ajusta bien?** (bondad de ajuste) → residuos, calibración, Hosmer–Lemeshow ([2.6](#sec-u12-diagnostico)).
- **¿Discrimina bien?** (clasificación) → matriz de confusión, ROC/AUC, umbral ([2.7](#sec-u12-evaluacion)).

Un modelo puede ser **significativo** y aun así **calibrar mal** o **discriminar poco**:
son preguntas independientes.
:::

::: {.callout-important}
## Tarea del estudiante (1.2) — alineada con RA3–RA5
1. **Interpretación (RA3).** Elige **dos** covariables del modelo e interpreta su
   efecto en **dos escalas**: odds ratio (con IC) y efecto marginal medio (AME),
   en lenguaje clínico. Explica por qué el coeficiente crudo no es un efecto sobre
   la probabilidad.
2. **Comparación escalonada (RA5).** Construye una escalera de al menos cuatro
   modelos que incluya **una interacción** (factor×continua o factor×factor).
   Compáralos por **LRT, AIC y BIC**, documenta dónde discrepan AIC y BIC y
   **justifica tu modelo final** (no basta con el AIC mínimo).
3. **Evaluación (RA4).** Para tu modelo elegido, reporta **AUC** y evalúa la
   **calibración** (gráfico + Hosmer–Lemeshow, con su salvedad). Comprueba además
   si hay **separación** en algún subgrupo y, si la hubiera, explica cómo la
   tratarías.

*Formato:* fragmento reproducible en el `.qmd` del grupo. Alimenta los apartados
«Diagnóstico y validación», «Selección y comparación» e «Interpretación» de la
rúbrica del informe (ver [Unidad 6](#sec-u16)).
:::

## Puente a la Unidad 3 {#sec-u12-puente}

Hasta aquí, una respuesta **binaria** modelada con **efectos fijos**. Las próximas
unidades generalizan en dos frentes. La [Unidad 3](#sec-u13) amplía el **tipo de
respuesta**: datos **agrupados** (binomial) y **más de dos categorías** (politómica
nominal y ordinal). La [Unidad 4](#sec-u14) introduce los **efectos aleatorios**
(GLMM) para el agrupamiento por centro —GLOW tiene pacientes anidadas en 6 centros y
la cohorte simulada, en 24—, donde las observaciones de un mismo centro se parecen
más de lo que el azar explicaría. Y por último la [Unidad 5](#sec-u15) aborda la 
supervivencia desde la perspectiva de los datos binarios.

<!-- Partial: incluir con {{< include _unidad_1_3.qmd >}}. Sin YAML.
     Chunks prefijados u13-. Binomial agrupado y politómicas (nominal/ordinal, raterisk) sobre GLOW. -->

# 3 · Extensión de la respuesta binaria: politómica y binomial {#sec-u13}

La respuesta binaria de las unidades anteriores admite **dos extensiones naturales**, y esta
unidad recorre ambas.

La primera es **agrupar**. Cuando las predictoras son **categóricas**, dos sujetos con el mismo
perfil de covariables son **intercambiables** —para el modelo, ensayos idénticos—, así que podemos
**colapsar** cada perfil en una fila que cuente cuántos casos hay en el grupo. Con respuesta
**binaria**, agrupar convierte la Bernoulli individual ($y_i\in\{0,1\}$) en una **binomial**,
$Y_i\sim\mathrm{Bin}(m_i,\pi_i)$ con $m_i$ el tamaño del grupo: es **el mismo modelo logístico**,
solo cambia la disposición del dato. Lo vemos en las Secciones [3.1](#sec-u13-binomial) y
[3.2](#sec-u13-gof-binomial), donde agrupar además **recupera los diagnósticos de ajuste**.

La segunda es admitir **más de dos categorías**. Si la respuesta es **politómica** —categórica con
tres o más niveles—, el logit binario se generaliza al **logit de categoría base** cuando no hay
orden ([Sección 3.3](#sec-u13-nominal)) y al de **odds proporcionales** cuando sí lo hay
([Sección 3.4](#sec-u13-ordinal)). Aquí las tratamos sobre datos **individuales** de GLOW
(`raterisk`). 

::: {.callout-tip}
Las respuestas politómicas también podrían agruparse en recuentos, generando una **multinomial**,
$Y_i\sim\mathrm{Mult}(m_i,\boldsymbol{\pi}_i)$, pero su ajuste se apoya en la equivalencia con un modelo
**log-lineal de Poisson**, que desarrollamos en el Caso 2, donde se introduce la Poisson, 
de modo que lo estudiaremos allí.
:::

::: {.callout-note}
## Sobre esta unidad
**RA implicados:** RA1–RA5 sobre un nuevo tipo de respuesta.
:::


## 3.1 Del dato individual al agrupado: respuesta binomial {#sec-u13-binomial}

Para ejemplificar la implementación de esta versión del modelo binomial, appliquemos una agrupación a GLOW, considerando ahora como predictoras las variables `priorfrac`, `momfrac` y `raterisk`. Como las tres predictoras son **factores**, cada combinación de niveles reúne a mujeres intercambiables. Al colapsarlas, la respuesta pasa de un $0/1$ por sujeto a un **recuento de fracturas sobre el total del grupo** —una binomial—. Lo llamativo es que **el ajuste no cambia** respecto a la logística individual; lo que sí cambia —y es la ganancia de agrupar— lo veremos en la bondad de ajuste ([Sección 3.2](#sec-u13-gof-binomial)).

Tendremos ahora datos $y_i \sim Bi(m_i,\pi_i), i=1,...,k$, con $y_i$ el número de éxitos en $m_i$ registros en cada una de las $k$ celdas o perfiles de covariables, con $\pi_i$ la probabilidad de éxito (fractura). El modelo que se plantea para GLOW propone como respuesta el combinado de éxitos y fracasos, $(y_i,m_i-y_i)$ y predictor lineal:

$$\pi_i=P(Y_i=1) \rightarrow \eta_i=logit(\pi_i) = \theta + \gamma^{\text{momfrac}}\text{momfrac}_i + \delta_k^{\text{raterisk}}\text{raterisk}_{ik}$$

donde $\text{momfrac}_i=1$ si la madre de la paciente $i$ tuvo fractura y $\text{raterisk}_{ik}=1$ si su autopercepción del riesgo de fractura es $k$, con $k \in \{\text{Less, Same, Greater}\}$.

:::: {.callout-tip}
## 🔧 En R. El `glm()` con datos binomiales

El ajuste de datos binomiales es similar al de datos binarios, con la salvedad de cómo se especifica la respuesta aquí, a partir de una matriz con dos columnas: éxitos y fracasos:
```
glm(cind(exitos,fracasos) ~ x1+x2+x1:x2+...,family=binomial(link = "logit"),data)
```
::::

Agrupamos entonces GLOW por tres factores (`priorfrac`, `momfrac`, `raterisk`) y contamos fracturas:

```{r}
#| label: u13-agrupar
glow_agg <- glow |>
  count(priorfrac, momfrac, raterisk, fracture) |>
  tidyr::pivot_wider(names_from = fracture, values_from = n, values_fill = 0) |>
  mutate(total = No + Yes)            # ensayos por celda; Yes = nº de fracturas
glow_agg
```

Antes de modelar conviene **ver** los datos agrupados. La @fig-u13-barras muestra, para cada perfil
de covariables, la **proporción observada de fractura** $(y_i/m_i)$; la cifra sobre cada barra es el
número de mujeres de la celda ($m_i$), que recuerda cuánta información sostiene cada proporción.

```{r}
#| label: fig-u13-barras
#| fig-width: 8
#| fig-height: 4
#| fig-cap: "Proporción observada de fractura por perfil de covariables en GLOW. Cada panel es un nivel de riesgo autopercibido (raterisk); dentro de cada panel, las barras cruzan fractura previa (priorfrac) y antecedente materno (momfrac). La cifra sobre la barra es el nº de mujeres de la celda."
glow_agg |>
  mutate(prop = Yes / total) |>
  ggplot(aes(x = priorfrac, y = prop, fill = momfrac)) +
  geom_col(position = position_dodge(width = 0.8), width = 0.7) +
  geom_text(aes(label = total), position = position_dodge(width = 0.8),
            vjust = -0.3, size = 3) +
  facet_wrap(~ raterisk, labeller = label_both) +
  labs(x = "Fractura previa (priorfrac)", y = "Proporción de fractura (Yes/total)",
       fill = "Antecedente materno\n(momfrac)") +
  ylim(0, NA)
```

En la @fig-u13-barras se aprecian ya las tendencias que el modelo recogerá: la proporción de fractura
**sube con la fractura previa** (`priorfrac`), **con el antecedente materno** (`momfrac`) y al pasar
de riesgo autopercibido `Less` a `Same` y `Greater` —el orden esperado—, lo que respalda incluir las
tres covariables. Conviene, eso sí, leer cada barra junto al **tamaño de su celda** (la cifra encima):
las tres celdas basales (`priorfrac = No`, `momfrac = No`) concentran la mayor parte de los datos
($n = 127, 124, 76$), mientras que las que acumulan ambos antecedentes son **diminutas**
($n = 2, 6, 10$). Sus proporciones son muy inestables —la celda `Less` con ambos antecedentes y $n=2$
cae a 0, y su análoga en `Same` con $n=6$ salta a 0,5—: es **ruido de muestreo** que no conviene
sobreinterpretar y que, además, es justo el que degrada la aproximación $\chi^2$ del contraste de
bondad de ajuste cuando hay celdas pequeñas (la cautela de la [Sección 3.2](#sec-u13-gof-binomial)).

Ahora ajustamos el binomial con la sintaxis `cbind(éxitos, fracasos)` y comprobamos que
**recupera exactamente** los mismos coeficientes que la logística individual con esas
predictoras —misma verosimilitud, distinta presentación—:

```{r}
#| label: u13-binomial-fit
fit_bin <- glm(cbind(Yes, No) ~ priorfrac + momfrac + raterisk,
               family = binomial, data = glow_agg)
fit_ind <- glm(fracture ~ priorfrac + momfrac + raterisk,
               family = binomial, data = glow)

cbind(agrupado = coef(fit_bin), individual = coef(fit_ind))   # idénticos
```

La interpretación de los coeficientes (odds ratios, [Sección 2.3](#sec-u12-interpretacion))
y la selección de modelos (LRT/AIC/BIC, [Sección 2.5](#sec-u12-inferencia)) son **idénticas**
a las del caso individual. Lo que cambia —y es la ganancia de agrupar— es la bondad de ajuste.

## 3.2 Bondad de ajuste y diagnóstico en datos binomiales {#sec-u13-gof-binomial}

En la [Sección 2.6](#sec-u12-diagnostico) vimos que, con datos **individuales**, ni la deviance ni el
estadístico de Pearson sirven como test absoluto de ajuste: el saturado crece con $n$ y la asintótica
$\chi^2$ no se sostiene. Con datos **agrupados** (y $m_i$ suficientemente grandes) esa asintótica
**se recupera** [@agresti2015; @faraway2016]. También cobran relevancia ciertos residuos comunes en los GLM que no funcionaban en el caso binario. Presentamos en primer lugar estos residuos y construimos, a partir de ellos y con su **suma de cuadrados**, los estadísticos globales de bondad de ajuste.

### Residuos

Al agrupar los datos, cada celda reúne $m_i$ ensayos, de modo que los residuos
**estándar** ya son informativos —aproximadamente $N(0,1)$ con $m_i$ grande
[@mccullagh1989]—. Estos residuos, genéricos en los GLM, son:

- **Pearson:**, la diferencia entre lo observado y lo esperado en la celda, tipificada por su desviación estándar,
  $$r_{P,i}=\dfrac{y_i-m_i\hat\pi_i}{\sqrt{m_i\hat\pi_i(1-\hat\pi_i)}}$$
- **Deviance:**, la raíz con signo de la contribución $d_i$ de cada celda a la deviance,
    $$r_{D,i}=\operatorname{sign}(y_i-m_i\hat\pi_i)\sqrt{d_i}$$

- Para compararlos en igualdad de condiciones se usan sus versiones **estandarizadas** (o
*estudentizadas*), que dividen por $\sqrt{1-h_{ii}}$,
$$
r^{\,s}_{P,i}=\frac{r_{P,i}}{\sqrt{1-h_{ii}}}, \qquad r^{\,s}_{D,i}=\frac{r_{D,i}}{\sqrt{1-h_{ii}}}
$$
donde $h_{ii}$ es el **leverage** (apalancamiento) de la celda —su influencia *potencial*, dada por el
elemento diagonal de la matriz gorro $\hat H$, con $\sum_i h_{ii}=p$—: un $h_{ii}$ alto marca una celda con
un perfil de covariables extremo, esto es, sujetos con características alejadas de la mayoría de los datos.


::: {.callout-note}
## La matriz sombrero en un GLM

En regresión lineal $\hat H = X(X^{\top}X)^{-1}X^{\top}$ proyecta $\mathbf y$ sobre los
valores ajustados. En un GLM el ajuste es por mínimos cuadrados **ponderados** (IWLS), así
que la matriz sombrero incorpora los pesos de la última iteración $W=\operatorname{diag}(w_i)$:
$$
\hat H = W^{1/2}X\,(X^{\top}WX)^{-1}X^{\top}W^{1/2}.
$$
Es **simétrica e idempotente**, sus diagonales son los **leverages** $h_{ii}$ y suman el
número de parámetros, $\sum_i h_{ii}=\operatorname{tr}(\hat H)=p$. En el binomial/logístico
los pesos de trabajo son $w_i=m_i\hat\pi_i(1-\hat\pi_i)$, de modo que $h_{ii}$ combina lo
extremo del perfil de covariables con su varianza. Es el $h_{ii}$ que aparece en
$r^{\,s}_{P,i}=r_{P,i}/\sqrt{1-h_{ii}}$ y en la distancia de Cook.
:::

- La **distancia de Cook** combina residuo y leverage, y mide **cuánto cambiarían los coeficientes** si se eliminara esa celda; valores grandes señalan **celdas influyentes** que conviene revisar.
$$
D_i=\frac{(r^{\,s}_{P,i})^2}{p}\,\frac{h_{ii}}{1-h_{ii}}
$$
Calculamos y representamos los residuos estandarizados y las distancias de Cook para GLOW en la @fig-u13-residuos.

```{r}
#| label: fig-u13-residuos
#| fig-width: 10
#| fig-height: 4
#| fig-cap: "Diagnósticos por celda (el tamaño del punto es el nº de ensayos). Izquierda: residuos de Pearson estandarizados frente a la probabilidad ajustada; con un buen ajuste se reparten en torno a 0 dentro de la banda de ±2. Derecha: distancia de Cook por celda, con línea de referencia orientativa en 4/n."
#library(patchwork)

diag_bin <- glow_agg |>
  mutate(pred  = fitted(fit_bin),
         r_std = rstandard(fit_bin, type = "pearson"),
         cook  = cooks.distance(fit_bin),
         cell  = row_number())

p_res <- ggplot(diag_bin, aes(pred, r_std, size = total)) +
  geom_hline(yintercept = c(-2, 0, 2), linetype = c("dotted", "dashed", "dotted")) +
  geom_point(alpha = 0.7) +
  labs(x = "Probabilidad ajustada", y = "Residuo de Pearson estandarizado",
       size = "n por celda")

p_cook <- ggplot(diag_bin, aes(cell, cook, size = total)) +
  geom_hline(yintercept = 4 / nrow(diag_bin), linetype = "dotted") +
  geom_segment(aes(xend = cell, yend = 0), linewidth = 0.3) +
  geom_point(alpha = 0.7) +
  scale_x_continuous(breaks = diag_bin$cell) +
  labs(x = "Celda", y = "Distancia de Cook", size = "n por celda")

p_res + p_cook + plot_layout(guides = "collect")
```

En GLOW, vemos, a la izquierda en la @fig-u13-residuos, que los **12 perfiles** (celdas) dejan todos los residuos estandarizados (de Pearson)  dentro de la banda de ±2, y se reparten sin
patrón alrededor de 0 (rango ≈ −1,3 a +1,4); las celdas con más ensayos —las de menor riesgo— caen
muy cerca de 0. No hay, pues, **celdas mal ajustadas ni influyentes**, en línea con el χ² de bondad
de ajuste y la calibración. En el panel derecho, las distancias de Cook son pequeñas en términos absolutos —la mayor (celda 9)
ronda 0,33 y apenas roza la referencia orientativa de $4/n$—, muy por debajo del umbral clásico
$D\gtrsim 1$. Las celdas con más ensayos (1–3) presentan un Cook moderado por su peso en el ajuste,
no por desajuste. Junto con la ausencia de residuos fuera de la banda de ±2, se confirma que ninguna
celda resulta influyente.

:::: {.callout-tip}
## 🔧 En R. Residuos e influencia

```r
residuals(fit_bin, type = "deviance")     # o "deviance", "response"
rstandard(fit_bin, type = "pearson")     # estandarizados (ajustan por el leverage)
hatvalues(fit_bin)                        # leverage h_ii de cada celda
cooks.distance(fit_bin)                   # influencia de cada celda
```
::::

### Bondad de ajuste

Sumando los cuadrados de los residuos deviance y Pearson se
obtienen los **dos estadísticos globales**, que comparan el modelo **ajustado con el saturado** —el
mejor ajuste posible—:

- **Pearson:** $X^2=\sum_i r_{P,i}^2$, la suma directa de los residuos de Pearson al cuadrado.
- **Deviance:** $D=\sum_i r_{D,i}^2=2(\ell_{\text{sat}}-\ell_{\text{ajustado}})$, que es además el
  **LRT frente al saturado**.

Bajo un modelo correcto y con celdas no demasiado pequeñas, ambos se distribuyen $\chi^2_{n-p}$
[@agresti2015; @faraway2016], con lo que **ya podemos contrastar el ajuste de forma absoluta** contra el modelo saturado y responder a la pregunta **¿ajusta bien nuestro modelo?**.


```{r}
#| label: u13-gof
gl <- df.residual(fit_bin)                       # n - p = nº de celdas - nº de parámetros
c(deviance = deviance(fit_bin),
  pearson  = sum(residuals(fit_bin, type = "pearson")^2),
  gl       = gl)

# p-valores del contraste de bondad de ajuste (H0: el modelo ajusta)
c(p_deviance = pchisq(deviance(fit_bin), gl, lower.tail = FALSE),
  p_pearson  = pchisq(sum(residuals(fit_bin, "pearson")^2), gl, lower.tail = FALSE))
```

Al conseguir un p-valor **alto** (muy superior a la significatividad 0.05) no evidenciamos falta de ajuste (no podemos rechazar el modelo ajustado frente al saturado), y así concluimos que el modelo reproduce
razonablemente las proporciones por celda. Es exactamente el
contraste que en la [Sección 2.6](#sec-u12-diagnostico) no podíamos usar y que ahora, 
al agrupar, recupera su sentido.

::: {.callout-important}
**Cautela:** la aproximación exige celdas no
demasiado pequeñas; con muchos $m_i$ pequeños vuelve a degradarse. 
:::

:::: {.callout-tip}
## 🔧 En R. El contraste χ² de bondad de ajuste (deviance y Pearson)
Con el modelo agrupado ajustado, los grados de libertad son **celdas menos parámetros**
(`df.residual`) y cada estadístico se compara con una $\chi^2_{n-p}$ mediante
`pchisq(..., lower.tail = FALSE)`:

```r
gl <- df.residual(fit_bin)                       # n - p = nº de celdas - nº de parámetros

# Deviance
pchisq(deviance(fit_bin), gl, lower.tail = FALSE)

# Pearson
X2 <- sum(residuals(fit_bin, type = "pearson")^2)
pchisq(X2, gl, lower.tail = FALSE)
```

Un $p$ **alto** = no hay evidencia de falta de ajuste. ¿Cuál usar?

- **Pearson ($X^2$)** para el **test absoluto** de bondad de ajuste: aproxima mejor la $\chi^2$ con
  recuentos moderados y es, además, la base de la **sobredispersión** ($\hat\phi = X^2/\text{gl}$).
- **Deviance** para **comparar modelos anidados** (su diferencia es el LRT) y como medida ligada a la
  verosimilitud; como test **absoluto** es algo menos fiable que Pearson cuando los recuentos son
  pequeños.
- **Ambos** exigen celdas **no pequeñas** (regla práctica: frecuencias esperadas $\gtrsim 5$); con
  muchos $m_i$ pequeños la aproximación $\chi^2$ se degrada —y la deviance, antes—.
::::

### Calibración 

Al agrupar, la **calibración** es inmediata —algo que el caso individual no permitía sin recurrir a
deciles—: cada celda aporta su **probabilidad predicha** y su **proporción observada**, y basta
enfrentarlas. Si el modelo está bien calibrado, los puntos se alinean sobre la diagonal:

```{r}
#| fig-width: 6
#| fig-height: 4
#| label: fig-u13-calibracion
#| fig-cap: "Curva de calibración por celda en GLOW: probabilidad predicha frente a proporción observada de fractura (el tamaño del punto es el nº de mujeres de la celda). La diagonal discontinua marca la calibración perfecta; los puntos próximos a ella, sin sesgo sistemático, indican un modelo bien calibrado."
# Calibración DIRECTA: cada celda = (probabilidad predicha, proporción observada)
calib_bin <- glow_agg |>
  mutate(pred = fitted(fit_bin), obs = Yes / total)

ggplot(calib_bin, aes(pred, obs, size = total)) +
  geom_abline(linetype = "dashed") +                 # calibración perfecta
  geom_point(alpha = 0.7) +
  labs(x = "Probabilidad predicha", y = "Proporción observada (Yes/total)",
       size = "n por celda")
```

Como se aprecia en @fig-u13-calibracion, en GLOW los 12 puntos caen cerca de la diagonal, sin sesgo sistemático por tramos de riesgo: el modelo está **bien calibrado**, en línea con el χ² de bondad de ajuste y los residuos.

:::: {.callout-tip}
## 🔧 En R. El data frame de observados vs predichos para la calibración

La calibración solo necesita, por celda, la probabilidad **predicha** y la proporción **observada**.
La forma directa es añadirlas con `mutate()`:

```r
calib_bin <- glow_agg |>
  mutate(pred = fitted(fit_bin),     # probabilidad predicha por el modelo
         obs  = Yes / total)         # proporción observada en la celda
```

Con `broom` se obtiene lo mismo de forma compacta —`type.predict = "response"` devuelve la
predicción en escala de probabilidad en la columna `.fitted`—:

```r
library(broom)
calib_bin <- augment(fit_bin, data = glow_agg, type.predict = "response") |>
  mutate(obs = Yes / total)
```

Sobre ese data frame, `geom_point(aes(pred, obs))` dibuja la calibración por celda y `geom_abline()`
la diagonal de referencia; un `geom_smooth(aes(pred, obs))` traza además la **curva de calibración**
suavizada.
::::



### Sobredispersión

El modelo binomial $y_i \sim Bi(m_i,\pi_i)$, impone que la varianza de cada celda sea exactamente
$m_i\hat\pi_i(1-\hat\pi_i)=Var(y_i)$. Cuando la varianza real en los datos **excede** ese valor hay **sobredispersión**, 
y al agrupar pasa a ser **estimable** mediante el cociente de Pearson:

$$
\hat\phi=\frac{X^2}{n-p}=\frac{1}{n-p}\sum_i r_{P,i}^2 .
$$

Un $\hat\phi\approx 1$ indica que el binomial basta; $\hat\phi>1$ indica sobredispersión (variación
*extra-binomial*); $\hat\phi<1$ indica infradispersión (es menos frecuente).

La sobredispersión puede ser debida a cierta **correlación entre los ensayos de una misma celda** (sujetos agrupados que no son del todo independientes), a **heterogeneidad no modelada** o por una **covariable omitida**. Lo que
importa es su efecto: los coeficientes $\hat\beta$ siguen siendo **consistentes**, pero los **errores estándar se subestiman**, de modo que los contrastes de Wald y LRT resultan **demasiado liberales**, y
los intervalos **demasiado estrechos**.

El remedio inmediato es el modelo **quasi-binomial**: no cambia las estimaciones puntuales, pero
**estima $\phi$ de los datos** e **infla los errores estándar** por $\sqrt{\hat\phi}$ (y sustituye los
contrastes $\chi^2$ por contrastes **F**). Cuando la sobredispersión procede de un efecto de agrupamiento 
identificable, la alternativa estructural pasa por incluir **efectos aleatorios** ([Unidad 4](#sec-u14)).

```{r}
#| label: u13-sobredisp
# Índice de sobredispersión (cociente de Pearson)
sum(residuals(fit_bin, type = "pearson")^2) / df.residual(fit_bin)
```

En GLOW, $\hat\phi\approx 0{,}58$: **por debajo de 1**, así que **no hay rastro de sobredispersión**
—el binomial es de sobra suficiente— y el quasi-binomial no haría falta. Lo mostramos igualmente como
herramienta para cuando $\hat\phi$ supere con claridad la unidad.

:::: {.callout-tip}
## 🔧 En R. Sobredispersión y quasi-binomial

```r
# Índice de sobredispersión (cociente de Pearson): ~1 si basta el binomial
sum(residuals(fit_bin, type = "pearson")^2) / df.residual(fit_bin)

# Ajuste robusto a sobredispersión: mismas estimaciones, SE inflados por sqrt(phi)
fit_q <- glm(cbind(Yes, No) ~ priorfrac + momfrac + raterisk,
             family = quasibinomial, data = glow_agg)
summary(fit_q)             # 'Dispersion parameter' = phi estimado; contrastes tipo t/F
drop1(fit_q, test = "F")   # comparación de modelos con test F (no chi^2)
```
::::


::: {.callout-note}
## ¿Y tratar la sobredispersión binomial con quasi-Poisson?

Hay una excepción a tratar la sobredispersión en el modelo binomial con un **quasi-Poisson**, y es el
régimen de **sucesos raros** ($m_i$ grande, $\pi_i$ pequeña y $m_i\pi_i$ moderado), donde la binomial
se aproxima a una Poisson. Ahí sí tiene sentido modelar el **conteo** con una Poisson de enlace log y
un *offset* $\log(m_i)$ —es decir, modelar una **tasa**—, y un quasi-Poisson o una binomial negativa
absorberían la variación extra. Pero eso es porque has entrado **deliberadamente** en el dominio
Poisson, no porque el quasi-Poisson arregle una binomial. Estos modelos los veremos en el Caso 2.
:::

En **todo lo demás**, el binomial agrupado se ajusta e interpreta **exactamente igual** que el
logístico individual de la [Unidad 2](#sec-u12): misma estimación (máxima verosimilitud / IWLS), misma
inferencia (Wald, LRT, IC por perfil) y misma selección de modelos (deviance, AIC/BIC, la
escalera de la [Sección 2.5](#sec-u12-inferencia)). Y la **lectura de los resultados no cambia**:
los coeficientes siguen siendo log-odds, $e^{\beta}$ es un odds ratio y las predicciones se
interpretan en probabilidad igual que en la [Sección 2.3](#sec-u12-interpretacion). La única
diferencia es la que acabamos de explotar: al agrupar, recuperamos los **diagnósticos de ajuste**
(el $\chi^2$ de bondad de ajuste, los residuos, la calibración y la sobredispersión por celda) que en
el caso individual no eran válidos.



## 3.3 Más de dos categorías sin orden: politómica nominal {#sec-u13-nominal}

Cuando seguimos teniendo respuestas individualizadas y la respuesta tiene **más de dos categorías sin orden**, el logit binario se generaliza al **logit de categoría base**: se fija una categoría de referencia y se modela, frente a ella, el
log-odds de cada una de las demás. 

Para ilustrar este modelo con GLOW, usamos `raterisk` —el riesgo de fractura **autopercibido** como variable respuesta $Y$, observada en tres niveles `Less`/`Same`/`Greater`—, y usamos la categoría `Same` como referencia. 


\begin{eqnarray}
y_i \in \{\text{Same},\text{Less},\text{Greater}\} & \rightarrow &\ \log\frac{\Pr(y_i=k)}{\Pr(y_i=\text{Same})}=\beta_{0k} + \beta_{1k} \cdot \text{age}_i + \gamma^{\text{priorfrac}}_{k}\text{priorfrac}_i,\\
&& \qquad i= 1, ..., n; \ k\in\{\text{Less},\text{Greater}\}
\end{eqnarray}

donde $\text{age}_i$ es la edad de la paciente $i$, y $\text{priorfrac}_i=1$ si la paciente tuvo una fractura previa y 0 en caso contrario.


En el logit de categoría base cada coeficiente lleva **dos índices** que conviene no confundir:
el del sujeto, $i$, y el de la **categoría de respuesta** $k$ (con cuál de las categorías distintas a la de referencia comparamos; aquí $k\in\{\text{Less},\text{Greater}\}$). Así, para el sujeto $i$,
$\beta_{1k}$ es el efecto del regresor `age`, y $\gamma^{\text{priorfrac}}_{k}$ el efecto de haber tenido una fractura previa sobre el **log-odds de la categoría $k$ frente a la referencia `Same`**. Hay, por tanto, un **vector de
coeficientes por cada categoría no-referencia** —uno para `Less` vs `Same` y otro para `Greater`
vs `Same`—, cada uno con su interceptación y sus pendientes, $\{(\beta_{0k},\beta_{1k},\gamma_k^{\text{priorfrac}}), k \in \{\text{Less, Greater}\} \}$.


Al exponenciar, $e^{\beta_{1k}}$ es un **odds ratio relativo**: el factor por el que se
multiplican las **odds de pertenecer a la categoría $k$ en lugar de a `Same`** cuando `age`
aumenta una unidad, manteniendo el resto constante. Por ejemplo, $e^{\beta_{1,\text{Greater}}}$
es cuánto cambia la ventaja de percibirse en **mayor** riesgo (`Greater`) frente a **igual**
(`Same`) por cada año más de edad. Cuando se estiman los coeficientes del modelo, su
**signo** ya orienta: $\beta_{1k}>0$ aumenta el riesgo de $k$ sobre `Same` al aumentar la edad,  y $\beta_{1k}<0$, disminuye el riesgo en $k$ respecto de `Same`.

Es siempre una comparación **por pares contra la referencia**: el OR de `Less` frente a `Greater` no
aparece directamente —se obtiene combinando ambos juegos ($e^{\beta_{1k}-\beta_{1k'}}$) o
reeligiendo la categoría base—. Cambiar la referencia cambia los coeficientes, pero **no** las
probabilidades ajustadas.

::: {.callout-note}
## Individual o agrupado: qué cambia y qué no
Tanto si la respuesta categórica es **individual** (una fila por sujeto, como aquí con
`raterisk`) como **agrupada** (recuentos por perfil, multinomial), el **modelo y sus estimaciones
son los mismos**: misma verosimilitud, misma interpretación de coeficientes, y la **selección de
modelos** (LRT, AIC/BIC) funciona igual. Lo que **no** vale con datos individuales es el contraste
**$\chi^2$ de bondad de ajuste** ni el análisis de **residuos**: como en el paso Bernoulli→binomial
([Sección 3.2](#sec-u13-gof-binomial)), solo recuperan su sentido al **agrupar**. Por eso aquí
valoramos el ajuste mirando las **probabilidades predichas**, no un $\chi^2$.
:::

**Un cambio de motor: esto ya no se ajusta con `glm()`.** La función `glm()` está programada para
respuestas de la familia exponencial **univariante** —un predictor lineal para una respuesta
escalar—; el multinomial tiene respuesta **vectorial** ($K$ probabilidades que suman 1) y necesita
**$K-1$ predictores lineales a la vez**, de modo que se sale de lo que cubre `glm()` (aunque sigue
siendo un GLM en sentido amplio). En R se ajusta con `nnet::multinom()` —lo que usamos aquí— o con
`VGAM::vglm(..., family = multinomial)`; ambas estiman por máxima verosimilitud el **mismo** logit de
categoría base.

:::: {.callout-tip}
## 🔧 En R. Ajustar e inspeccionar el multinomial nominal

La respuesta debe ser un **factor** en el que hay que identificar la categoría de referencia.

**Ajuste (dos motores equivalentes).**

```r
library(nnet)
m_nom  <- multinom(relevel(raterisk, ref = "Same") ~ age + priorfrac, data = glow, trace = FALSE)

library(VGAM)
m_vglm <- vglm(raterisk ~ age + priorfrac,
               family = multinomial(refLevel = "Same"), data = glow)
```

**Resúmenes.** 

En `nnet`:

- `summary(m_nom)` da un bloque de coeficientes por categoría no-referencia
(`Less|Same`, `Greater|Same`) con sus errores estándar; 
- `multinom` no imprime $p$-valores, así que se sacan con `broom::tidy(m_nom)` (añade $z$ y $p$), y 
- `exp(coef(m_nom))` da los **odds ratios relativos**. 

En `vglm`: 

- `summary(m_vglm)` y 
- `coef(m_vglm, matrix = TRUE)`.

**Bondad de ajuste.** Con datos individuales no hay $\chi^2$ de bondad de ajuste
([Sección 2.6](#sec-u12-diagnostico)); se usan la verosimilitud y los criterios de información: `logLik()`, `AIC()`, `BIC()`,
`deviance()`. 

**Comparación de modelos.** Modelos **anidados** con el LRT —`lmtest::lrtest(m0, m_nom)` (o `anova()` en `vglm`)—;
**no anidados**, con AIC/BIC.

**Ajustes y predicciones.** 

En `nnet`:
```
fitted(m_nom) # matriz de probabilidades** (una columna por nivel) 
predict(m_nom, type = "probs") #  predice probabilidades para datos nuevos
predict(m_nom, type ="class") # da la categoría más probable. 
ggeffects::ggpredict(m_nom, terms = "age [all]") # para visualizarlas frente a una covariable
```

En `vglm`:
```
fitted(m_vglm)` # equivale a `predict(m_vglm, type = "response")`
nd <- data.frame(age = 60:90, priorfrac = "No") # datos nuevos
predict(m_vglm, newdata = nd, type = "response")   # predicción de probabilidades para datos nuevos, matriz n x K
predict(m_vglm, newdata = nd, type = "link") # predicción de logit frente a referencia
colnames(p)[max.col(p, ties.method = "first")] # da la categoría más probable
```
::::


Ajustamos a continuación este modelo sobre GLOW, utilizando `nnet`.

```{r}
#| label: u13-nominal
library(nnet)
m_nom <- multinom(relevel(raterisk, ref = "Same") ~ age + priorfrac,
                  data = glow, trace = FALSE)
summary(m_nom) # dos bloques de coeficientes: Less|Same y Greater|Same
tidy(m_nom) # visualización ordenada
exp(coef(m_nom))      # odds ratios relativos
```

`multinom` devuelve **dos bloques** de odds ratios relativos, `Less|Same` y `Greater|Same`. El
motor es la **fractura previa**: tener una multiplica por **2,23** las odds de percibirse en
**mayor** riesgo (`Greater`) frente a «igual» (`Same`) y, a la vez, las reduce a **0,65** (−35 %)
para percibirse en **menor** riesgo (`Less`); es decir, empuja la percepción fuera de «Same» hacia
«Greater» —clínicamente coherente y buena señal de que el modelo capta algo real—. La **edad**, en
cambio, apenas influye (OR ≈ 1: 1,01 para `Less`, 0,98 para `Greater`). Dos matices: son odds
**relativas a `Same`**, no frente a todas las categorías; y los interceptos (OR de `Less` ≈ 0,49 y
`Greater` ≈ 2,93) corresponden a `age = 0`, una extrapolación sin sentido sustantivo —para leer en
probabilidad por edades, la @fig-u13-pred-nominal—.

Con los coeficientes estimados, la **probabilidad predicha** sale de combinar los dos predictores
lineales (con $\text{priorfrac}=1$ si hay fractura previa, y $\text{age}$ en años):
$$
\eta_{\text{Less}}    = -0{,}71 + 0{,}010\,\text{age} - 0{,}42\,\text{priorfrac},\qquad
\eta_{\text{Greater}} =  1{,}08 - 0{,}023\,\text{age} + 0{,}80\,\text{priorfrac}.
$$
Como `Same` es la referencia y las tres probabilidades suman 1,
$$
\Pr(Y=\text{Less})=\frac{e^{\eta_{\text{Less}}}}{1+e^{\eta_{\text{Less}}}+e^{\eta_{\text{Greater}}}},
\qquad
\Pr(Y=\text{Greater})=\frac{e^{\eta_{\text{Greater}}}}{1+e^{\eta_{\text{Less}}}+e^{\eta_{\text{Greater}}}},
$$
y $\Pr(Y=\text{Same})=1\big/\!\left(1+e^{\eta_{\text{Less}}}+e^{\eta_{\text{Greater}}}\right)$.

Lo más comunicable es la **probabilidad predicha** de cada nivel. La @fig-u13-pred-nominal la
traza frente a la edad:

```{r}
#| label: fig-u13-pred-nominal
#| fig-width: 6.5
#| fig-height: 4
#| fig-cap: "Probabilidad predicha de cada nivel de raterisk según la edad (multinomial nominal, priorfrac = No). Cada categoría sigue su propia curva, sin restricción entre ellas."
library(ggeffects)
plot(ggpredict(m_nom, terms = "age [all]"))
```

La @fig-u13-pred-nominal traduce los coeficientes a **probabilidad** (con `priorfrac = No`) y revela
el patrón con la edad: la de percibirse en **menor** riesgo (`Less`) **crece** de ≈32 % a los 55 años
a ≈47 % a los 90, mientras que la de **mayor** riesgo (`Greater`) **cae** de ≈31 % a ≈14 %; la
referencia `Same` se mantiene casi plana en torno al 38 % (con una leve curvatura: en la escala de
probabilidad las curvas no tienen por qué ser monótonas aunque los logit sean lineales). Las tres se
mueven **con independencia** —sin restricción de orden, propio del modelo nominal— pero **suman 1** a
cada edad. El desplazamiento concuerda con los OR por año (`Less` 1,01; `Greater` 0,98): minúsculos
uno a uno, pero apreciables **acumulados** sobre 35 años. Sustantivamente llama la atención —y es
clínicamente relevante— que las mujeres mayores tiendan a **autopercibir menos riesgo** justo cuando
el objetivo es mayor, una brecha de percepción que el modelo capta. (Con `priorfrac = Yes` el perfil
se desplazaría hacia `Greater`, acorde con su OR de 2,23.)

**Bondad de ajuste, diagnóstico y comparación.** 

- Con la respuesta **individual** no disponemos del
$\chi^2$ de bondad de ajuste ni de los residuos estándar ([Sección 3.2](#sec-u13-gof-binomial)): el
ajuste se valora con la **verosimilitud**. 
- El contraste **global** —¿aporta el modelo frente al
nulo?— es un LRT, $D_0-D\sim\chi^2$ con $(K-1)\,p$ grados de libertad (aquí, los dos bloques de
pendientes a la vez). 
- El **diagnóstico** se apoya en las **probabilidades predichas**
- La **calibración** se examina **por categoría** —probabilidad media predicha frente a frecuencia
observada en grupos de riesgo, una *reliability* por nivel—. 
- El acierto clasificatorio se concluye con la
**matriz de confusión** (`predict(m_nom, type = "class")`) o el *Brier score* multiclase.
- Para residuos formales hay que recurrir a residuos **simulados/surrogate**, no a los clásicos. 
- La **comparación de modelos** sigue la lógica habitual: 
  - **anidados** por LRT (`lmtest::lrtest`, `anova`) —por ejemplo, contrastar si `priorfrac` aporta— y 
  - **no anidados** por **AIC/BIC**. 
  
Una comparación natural es con el modelo **ordinal** de la [Sección 3.4](#sec-u13-ordinal), que aprovecha
el orden de las categorías y gana parsimonia: un solo $\beta$ por covariable en vez de uno por
categoría.

El *Brier score* multiclase, que acabamos de mencionar, merece su definición. Es el **error
cuadrático medio** entre el vector de probabilidades predichas y la clase observada codificada como
*one-hot*:
$$
\text{BS}=\frac{1}{n}\sum_{i=1}^{n}\sum_{k=1}^{K}\bigl(\hat p_{ik}-y_{ik}\bigr)^2,
\qquad y_{ik}=\mathbf 1\{\text{la clase observada de }i\text{ es }k\},
$$
donde $\hat p_{ik}$ es la probabilidad predicha de que la observación $i$ pertenezca a la categoría
$k$. Es un *scoring rule* **propio** —penaliza a la vez la mala **calibración** y la falta de
**nitidez**—: vale **0** con predicción perfecta y, sumando sobre las $K$ clases, como máximo **2**
(el caso binario es el conocido $\tfrac1n\sum_i(\hat p_i-y_i)^2\in[0,1]$). Cuanto **menor**, mejor;
sirve para **comparar modelos** sobre las mismas observaciones.

:::: {.callout-tip}
## 🔧 En R. Brier score multiclase

A partir de la matriz de probabilidades predichas (`fitted`) y la clase observada en *one-hot*:

```r
P   <- fitted(m_nom)                         # n x K; columnas = niveles de la respuesta
obs <- factor(glow$raterisk, levels = colnames(P))
Y   <- model.matrix(~ 0 + obs)               # one-hot
colnames(Y) <- levels(obs)                   # alinea columnas con las de P
brier <- mean(rowSums((P - Y)^2))            # 0 = perfecto; máximo 2
brier
```

Con `yardstick` se calcula directamente —la respuesta y las columnas de probabilidad deben llevar el
nombre de cada clase—:

```r
library(yardstick); library(tibble)
prob <- as_tibble(fitted(m_nom))             # columnas = niveles de la respuesta
prob$obs <- factor(glow$raterisk, levels = names(prob))
brier_class(prob, truth = obs, Same, Less, Greater)
```
::::

## 3.4 Categorías ordenadas: el modelo de odds proporcionales {#sec-u13-ordinal}

Consideramos ahora la respuesta individualizada `raterisk`, que **tiene orden** (`Less < Same < Greater`), y el modelo politómico anterior lo ignoraba.
Cuando el orden existe, conviene aprovecharlo con el modelo de **odds proporcionales**
(*cumulative logit*), que modela los **logits acumulados** con umbrales ordenados y **un único**
vector de efectos:
$$
\operatorname{logit}\Pr(Y_i\le k)=\theta_k-\bigl(\beta_{\text{age}}\,\text{age}_i+\gamma_{\text{priorfrac}}\,\text{priorfrac}_i\bigr),
\qquad k\in\{\text{Less},\,\text{Same}\},\quad \theta_{\text{Less}}<\theta_{\text{Same}}
$$
donde $\text{age}_i$ es la edad de la paciente $i$, y $\text{priorfrac}_i=1$ si la paciente tuvo una fractura previa y 0 en caso contrario.

Frente al nominal de la [Sección 3.3](#sec-u13-nominal) —que estimaba **un vector de efectos por
categoría**, $\{(\beta_{1k},\gamma_k)\}$— aquí hay dos piezas bien diferenciadas:

- los **umbrales** (interceptos ordenados), uno por cada corte entre categorías contiguas: con tres
  niveles hay **dos**, $\theta_{\text{Less}}$ (corte `Less|Same`, es decir $\Pr(Y\le\text{Less})$) y
  $\theta_{\text{Same}}$ (corte `Same|Greater`, $\Pr(Y\le\text{Same})$), ordenados
  $\theta_{\text{Less}}<\theta_{\text{Same}}$;
- un **único vector de efectos** $(\beta_{\text{age}},\gamma_{\text{priorfrac}})$ **compartido por
  los dos cortes** —de ahí «odds proporcionales»—, de modo que $e^{\beta_{\text{age}}}$ y
  $e^{\gamma_{\text{priorfrac}}}$ son odds ratios **acumulados** comunes a todos ellos.

Con los datos de GLOW, los umbrales estimados son $\theta_{\text{Less}}\approx-2{,}06$ y
$\theta_{\text{Same}}\approx-0{,}44$, y el vector de efectos $(\beta_{\text{age}},\gamma_{\text{priorfrac}})\approx(-0{,}023,\ 0{,}90)$.
Se ajusta con `MASS::polr`, que **exige** una respuesta de tipo **factor ordenado**:

```{r}
#| label: u13-ordinal
glow_ord <- glow |>
  mutate(raterisk = ordered(raterisk, levels = c("Less", "Same", "Greater")))
m_ord <- MASS::polr(raterisk ~ age + priorfrac, data = glow_ord, Hess = TRUE)
tidy(m_ord)        # dos umbrales (theta) y los efectos (beta), comunes a ambos cortes
exp(coef(m_ord))      # odds ratios acumulados
```

Frente al nominal de 3.3, el ordinal **gana parsimonia**: un solo $\beta$ por covariable en vez de
uno por categoría, a cambio del **supuesto de proporcionalidad de odds** (el mismo efecto en todos
los cortes), que conviene contrastar (un test de líneas paralelas).

:::: {.callout-tip}
## 🔧 En R. Ajustar e inspeccionar el ordinal (odds proporcionales)

**Respuesta.** Debe ser un **factor ordenado** (`ordered()` con los niveles en su orden natural):

```r
glow$raterisk <- ordered(glow$raterisk, levels = c("Less", "Same", "Greater"))
```

**Ajuste (motores equivalentes).**

```r
m_ord  <- MASS::polr(raterisk ~ age + priorfrac, data = glow, Hess = TRUE)
m_clm  <- ordinal::clm(raterisk ~ age + priorfrac, data = glow)
m_vglm <- VGAM::vglm(raterisk ~ age + priorfrac,
                     family = cumulative(parallel = TRUE), data = glow)  # parallel = odds proporcionales
```

**Resúmenes.** 

- `summary(m_ord)` da **un único bloque de pendientes** (común a los cortes) y los
**umbrales** (`Intercepts` / `zeta`); 
- `broom::tidy(m_ord)` los ordena (marca `coef.type` =`coefficient` / `scale`)  
- `exp(coef(m_ord))` da los **odds ratios acumulados**. 

`polr` no imprime $p$-valores; se sacan del valor $t$ o con `ordinal::clm` (que sí los da).

**Bondad de ajuste y comparación.** 

- Verosimilitud y criterios: `logLik()`, `AIC()`, `BIC()`, `deviance()`. 
- **Modelos anidados** con el LRT (`anova`, `lmtest::lrtest`). 
-  Y clave aquí, el **supuesto de proporcionalidad de odds** (líneas paralelas) se contrasta con `brant::brant(m_ord)` o
`ordinal::nominal_test(m_clm)`.
- Comparar con el **nominal** por AIC/BIC indica si compensa la parsimonia.

**Ajustes y predicciones.** 

- `predict(m_ord, type = "probs")` da la **matriz de probabilidades**,
- `predict(m_ord, type = "class")` la categoría modal, y 
- `ggeffects::ggpredict(m_ord, terms = "age[all]")` las dibuja.
::::

```{r}
#| label: fig-u13-pred-ordinal
#| fig-width: 6.5
#| fig-height: 4
#| fig-cap: "Probabilidad predicha de cada nivel de raterisk según la edad (odds proporcionales, priorfrac = No). Las curvas están ligadas por un único efecto de la edad común a todos los cortes."
library(ggeffects)
plot(ggpredict(m_ord, terms = "age [all]"))
```

La @fig-u13-pred-ordinal lo deja ver: frente a las **curvas libres** del nominal
(@fig-u13-pred-nominal), aquí están **ligadas** por un único efecto de la edad —la categoría
central (`Same`) forma una joroba y las extremas son monótonas—. Si ambas imágenes se parecen, el
supuesto de proporcionalidad es razonable y ganamos parsimonia; si difieren mucho, el ordinal
estaría forzando.

Con ambos modelos ajustados, la comparación numérica resume las **discrepancias** entre nominal y
ordinal:

```{r}
#| label: u13-comparacion-nom-ord
# Ajuste y parsimonia (el ordinal usa menos parámetros)
ajuste <- tibble::tibble(
  Modelo     = c("Nominal (multinom)", "Ordinal (polr)"),
  Parametros = c(length(coef(m_nom)), length(coef(m_ord)) + length(m_ord$zeta)),
  logLik     = c(as.numeric(logLik(m_nom)), as.numeric(logLik(m_ord))),
  AIC        = c(AIC(m_nom), AIC(m_ord)),
  BIC        = c(BIC(m_nom), BIC(m_ord))
)

# Odds ratios: el nominal da uno por categoría; el ordinal, uno (acumulado) común
or_nom <- exp(coef(m_nom))      # filas Less/Greater x (Intercept, age, priorfracYes)
or_ord <- exp(coef(m_ord))      # age, priorfracYes (acumulado)
discrepancias <- tibble::tibble(
  Covariable              = c("age", "priorfracYes"),
  `Nominal Less|Same`     = or_nom["Less",    c("age", "priorfracYes")],
  `Nominal Greater|Same`  = or_nom["Greater", c("age", "priorfracYes")],
  `Ordinal (acumulado)`   = or_ord[c("age", "priorfracYes")]
)

ajuste
discrepancias
```

La tabla resume el contraste. En **ajuste**, el ordinal emplea **4 parámetros** (dos umbrales + dos
pendientes) frente a los **6** del nominal (dos bloques de tres); si la proporcionalidad de odds se
sostiene, su `AIC`/`BIC` salen **competitivos o mejores** pese a algo menos de verosimilitud. En
**efectos**, la diferencia estructural es nítida: el nominal estima **un OR por categoría** y el
ordinal los **colapsa en uno solo, acumulado**. Para `priorfrac`, el nominal da **0,65** sobre `Less`
y **2,23** sobre `Greater` (frente a `Same`): leídos en el orden, ambos empujan **hacia arriba** (más
`Greater`, menos `Less`), justo lo que resume el OR acumulado **2,46**. Para `age` ocurre lo simétrico
(**1,01** y **0,98** → empuje **hacia abajo**, hacia `Less`; acumulado **0,98**). Como las dos
lecturas nominales son **coherentes con el orden** en ambas covariables, el resumen ordinal es fiel y
la @fig-u13-pred-ordinal se parece a la @fig-u13-pred-nominal; si un OR claramente $>1$ conviviera con
otro $<1$ **incoherentes** con el orden, el ordinal estaría forzando —y el **test de líneas
paralelas** lo delataría—.

::: {.callout-note}
## Puente a la Unidad 4

En la [Unidad 4](#sec-u14) reactivamos los **efectos aleatorios** y ajustamos un **ordinal mixto**
(intercepto aleatorio por grupo, `ordinal::clmm`) —allí sobre la cohorte simulada, cuya verdad
conocida deja ver cómo el mixto recupera mejor umbrales y efectos que `polr` cuando hay
agrupamiento—.
:::


::: {.callout-important}
## Tarea del estudiante (1.3)
1. **Binomial agrupado (RA2, RA4).** Agrupa GLOW por un conjunto de factores y ajusta el
   binomial; comprueba que recupera la logística individual y aplica el **contraste χ² de
   bondad de ajuste** (deviance y Pearson), interpretándolo. Discute el efecto del tamaño de
   celda.
2. **Multinomial (RA3).** Interpreta un **odds ratio relativo** del multinomial nominal y un
   **odds ratio acumulado** del ordinal (odds proporcionales), en lenguaje del problema.
:::

<!-- Partial: incluir con {{< include _unidad_1_4.qmd >}}. Sin YAML.
     Chunks prefijados u14-. Efectos aleatorios (intercepto y pendiente) sobre la
     cohorte simulada (ever, sever_ord, centro) con verdad en attr(cohorte, "verdad"). -->

# 4 · Efectos aleatorios {#sec-u14}

Hasta aquí, cada coeficiente ha sido un **efecto fijo**: un único número válido para toda la
población. Pero los datos rara vez son una masa homogénea —los pacientes se agrupan en centros,
los alumnos en aulas, las medidas repetidas en cada sujeto—, y los grupos **difieren entre sí**,
tanto en su nivel de partida como en cómo responden a las covariables. Ignorar ese agrupamiento
(o gastarse un parámetro por grupo) lleva a problemas conocidos; los **efectos aleatorios** son la
vía intermedia: modelar la **variación entre grupos** como una distribución de la que cada grupo
es una muestra. Es el primero de los dos hilos en espiral del curso —los efectos mixtos—, que
abrimos aquí sobre el GLMM logístico y reactivaremos en cada caso.

En nuestros datos ese agrupamiento tiene nombre: la variable **`centro`**. Tanto en **GLOW** como en
la **cohorte** simulada, cada paciente pertenece a un centro hospitalario, y los centros **no son
intercambiables**: difieren en **criterios diagnósticos** —cómo se define y registra una fractura,
qué umbrales se aplican— y en **factores de entorno** ligados a su **ubicación** —perfil de las
pacientes, exposición, hábitos, recursos del área—. Dos mujeres con la misma edad y los mismos
antecedentes pueden, por tanto, tener un riesgo algo distinto según **dónde** se las atienda.

Ahora bien, el centro **no nos interesa como predictor**: no queremos —ni tendría sentido clínico—
predecir si una paciente se fracturará **según a qué hospital pertenezca**, ni los seis centros de
GLOW (o los veinticuatro de la cohorte) son un fin en sí mismos; son una **muestra** de los muchos
centros posibles. Lo que **sí** debemos reconocer es que el centro induce **agrupamiento**: las
pacientes de un mismo centro se **parecen más entre sí** de lo que explicaría el azar —no son del
todo independientes— y, a la vez, hay **diferencias sistemáticas entre centros** —unos con un riesgo
basal más alto que otros—. Gastar un coeficiente **fijo** por centro malgastaría parámetros y no
generalizaría a centros nuevos; ignorarlo falsearía la precisión. La vía adecuada es modelar esa
variación **entre centros** como **efectos aleatorios** —el objeto de esta unidad—: estimamos
**cuánto varían** los centros (una varianza), no el valor concreto de cada uno. Trabajamos sobre la
**cohorte** simulada, cuya verdad conocida permite comprobar si lo recuperamos bien.

::: {.callout-note}
## Sobre esta unidad
Primer contacto con los **efectos mixtos** (hilo en espiral: se reactiva en el Caso 2).
Introducimos los **efectos aleatorios** —de intercepto y de pendiente— sobre el GLMM logístico
binario de la cohorte y, al cierre, reactivamos la respuesta **ordinal** de la
[Unidad 3](#sec-u13) en su versión **mixta**. Trabajamos sobre la cohorte simulada, cuya
**verdad conocida** permite contrastar lo estimado.
**RA implicados:** RA6 (efectos mixtos).
[@bates2015; @agresti2015]
:::

## 4.1 Qué son y para qué sirven {#sec-u14-que-son}

La idea central es un cambio de pregunta. Un efecto **fijo** responde *«¿cuál es el efecto?»* con
**un número para toda la población**. Un efecto **aleatorio** no estima un número por grupo, sino
la **distribución** de la que esos números proceden: en lugar de *«¿cuál es la interceptación del
centro 7?»*, pregunta *«¿cuánto varían los interceptos entre centros?»* —su **varianza**—. Se
modela la **dispersión** entre grupos, no cada grupo por separado.

**Dos cosas pueden variar de un grupo a otro:**

- El **nivel de base** → un **intercepto aleatorio** $u_{0j}$: cada centro parte de un riesgo
  basal distinto (la curva se **desplaza** arriba o abajo).
- La **sensibilidad a una covariable** → una **pendiente aleatoria** $u_{1j}$: en cada centro, el
  efecto de (digamos) la edad es distinto (la curva se **inclina** de forma diferente).

**¿Para qué sirven?** Cuatro cosas concretas:

1. **Respetar la no independencia.** Las observaciones de un mismo grupo se parecen entre sí;
   ignorarlo da errores estándar **demasiado pequeños** (falsa precisión). El efecto aleatorio
   modela esa correlación intragrupo correctamente.
2. **Partial pooling (tomar prestado).** Las estimaciones de cada grupo se **encogen** hacia la
   media global, tanto más cuanto menos datos aporta el grupo: estabiliza los grupos pequeños sin
   imponer a todos un único valor rígido.
3. **Cuantificar la heterogeneidad.** Las **varianzas** $\sigma^2$ son, ellas mismas, la respuesta
   a una pregunta sustantiva: ¿cuánto difieren los grupos en su nivel, o en el efecto de una
   covariable?
4. **Generalizar.** Al tratar los grupos como una **muestra** de una población de grupos, las
   conclusiones valen para grupos **no observados**, no solo para los de la muestra.

En una frase: el efecto fijo dice *cuánto*; el efecto aleatorio dice *cuánto varía ese cuánto entre
grupos* —y lo resume en una varianza—.

## 4.2 Intercepto aleatorio {#sec-u14-intercepto}

Empecemos por dejar variar el **nivel de base**. Para tratar el centro hay tres estrategias:

- **Ignorarlo** (*complete pooling*): un solo intercepto para todos. Subestima la incertidumbre y
  puede sesgar los efectos.
- **Un parámetro por centro** (*no pooling*): tantos interceptos como centros. Gasta muchos grados
  de libertad y no generaliza a centros nuevos.
- **Intercepto aleatorio** (*partial pooling*): los interceptos de centro son **extracciones de una
  distribución**, $u_j\sim\mathcal{N}(0,\sigma_u^2)$; no estimamos cada $u_j$, sino su **varianza**
  $\sigma_u^2$.

El modelo, para la binaria de la cohorte, es un **GLMM logístico**:
$$
\operatorname{logit}\Pr(\text{ever}_{ij}=1)=\beta_0+\beta_1 x_{1ij}+\beta_2 x_{2ij}+u_j,
\qquad u_j\sim\mathcal{N}(0,\sigma_u^2).
$$

En **forma matricial** —la notación habitual de los modelos mixtos, que conviene reconocer desde
ya—, el predictor lineal de todas las observaciones se escribe
$$
\boldsymbol\eta=\mathbf{X}\boldsymbol\beta+\mathbf{Z}\mathbf{u},\qquad
\mathbf{u}\sim\mathcal{N}(\mathbf{0},\mathbf{G}),\qquad g(\boldsymbol\mu)=\boldsymbol\eta,
$$
donde $\mathbf{X}$ ($n\times p$) y $\boldsymbol\beta$ son el diseño y los coeficientes **fijos**;
$\mathbf{Z}$ ($n\times q$) y $\mathbf{u}$ son el diseño y los efectos **aleatorios**, con $\mathbf{G}$
su matriz de covarianzas; y $g$ es el enlace (aquí, logit). Para el **intercepto aleatorio**,
$\mathbf{u}=(u_1,\dots,u_J)^\top$ son los $J$ interceptos de centro, $\mathbf{Z}$ es la matriz
**indicadora** que asigna cada observación a su centro, y $\mathbf{G}=\sigma_u^2\,\mathbf{I}_J$. La
parte $\mathbf{X}\boldsymbol\beta$ es lo de siempre; lo nuevo es el término $\mathbf{Z}\mathbf{u}$.

Se interpreta así:

- **Componentes de varianza.** $\sigma_u^2$ resume cuánto varían los grupos. En la escala latente
  logística, el **coeficiente de correlación intraclase** $\text{ICC}=\sigma_u^2/(\sigma_u^2+\pi^2/3)$
  mide qué fracción de la variación total es **entre** centros (en la cohorte, $\approx 0{,}13$).
- **Condicional vs marginal.** En un GLMM **no lineal**, los efectos fijos son **condicionales**
  (específicos de grupo: «para un centro dado») y **difieren** de los **marginales** (poblacionales,
  promediados sobre los centros). Por eso un ajuste *pooled* —que estima algo marginal— **atenúa**
  los coeficientes frente a la verdad condicional. No es un error: son dos cantidades distintas.
- **Shrinkage.** Los interceptos de centro estimados (`ranef`) están **encogidos** hacia 0; los
  centros con menos datos se encogen más.

## 4.3 Pendiente aleatoria: cuando el efecto varía por grupo {#sec-u14-pendiente}

El intercepto aleatorio deja que el **nivel** cambie por centro, pero asume que el **efecto** de
cada covariable es el mismo en todos. A menudo no lo es: la edad, por ejemplo, puede pesar más en
unos centros que en otros. Eso se modela con una **pendiente aleatoria**, añadiendo una desviación
de grupo al coeficiente:
$$
\operatorname{logit}\Pr(\text{ever}_{ij}=1)=\beta_0+\beta_1 x_{1ij}+\beta_2 x_{2ij}+u_{0j}+u_{1j}\,x_{1ij},
\qquad
\begin{pmatrix}u_{0j}\\ u_{1j}\end{pmatrix}\sim\mathcal{N}(\mathbf{0},\boldsymbol\Sigma).
$$

Cómo se lee:

- $\beta_1$ es el efecto **medio** de $x_1$ en la población de centros; el efecto **en el centro
  $j$** es $\beta_1+u_{1j}$.
- $\sigma_{u_1}^2$ (la varianza de las pendientes) dice **cuánto varía** ese efecto entre centros:
  si es $\approx 0$, $x_1$ actúa casi igual en todos; si es grande, en unos centros pesa mucho y en
  otros poco.
- La **correlación** $\rho$ entre $u_{0j}$ y $u_{1j}$ liga nivel y sensibilidad: $\rho>0$ indica que
  los centros con mayor riesgo basal tienen, además, un efecto de $x_1$ más fuerte.

Las pendientes aleatorias son **más exigentes** con los datos (necesitan suficientes grupos y
variación de $x_1$ dentro de cada uno) y es frecuente que el ajuste no converja o resulte
**singular**; por eso se empieza por el intercepto y se añade la pendiente solo si está justificada.

En forma matricial, una pendiente aleatoria solo **añade columnas a $\mathbf{Z}$** (una por efecto
aleatorio y grupo) y agranda $\mathbf{G}$, que pasa a tener un bloque $\boldsymbol\Sigma$ de
$2\times2$ por grupo.




## 4.4 Laboratorio: GLMM logístico {#sec-u14-glmm}

Replicamos la idea: cómo se distribuye la fragilidad (`x1`) entre
quienes fracturan y quienes no, separando por tratamiento (`x2`) y por periodo de revisión
(`tiempo`):

```{r}
#| label: fig-cohorte-eda-box
#| fig-width: 10
#| fig-height: 5
#| out-width: "100%"
#| fig-cap: "Fragilidad ósea (x1) según el desenlace, por brazo de tratamiento (x2) y con facetas por periodo de revisión (tiempo)."
cohorte |>
  mutate(Desenlace   = factor(evento, levels = c(0, 1), labels = c("Sin fractura", "Fractura")),
         Tratamiento = factor(x2,     levels = c(0, 1), labels = c("No", "Sí"))) |>
  ggplot(aes(x = Desenlace, y = x1, fill = Tratamiento)) +
  geom_boxplot(alpha = 0.75, outlier.alpha = 0.25) +
  facet_wrap(~ tiempo, nrow = 1, labeller = label_both) +
  labs(x = "Desenlace", y = "Fragilidad ósea (x1, en z)", fill = "Tratamiento")
```

El patrón es nítido y **coherente en todos los periodos**: dentro de cada faceta, quienes
**fracturan** tienen una **fragilidad mayor** (las cajas «Fractura» se desplazan hacia arriba) —el
efecto de `x1` sobre el riesgo—. Y entre las **tratadas** (`x2` = Sí), las que llegan a fracturar
son típicamente **aún más frágiles**: como el tratamiento protege, solo «caen» las de fragilidad más
alta —el desplazamiento de umbral propio de un factor protector—. A lo largo de los periodos el
cuadro se mantiene, en línea con la proporcionalidad del riesgo.

**Sobre `centro`.** No lo he metido en este gráfico: con **24 niveles** no cabe de forma legible (ni
como color ni como faceta adicional, que multiplicaría los paneles). La variación entre clínicas se
ve mejor donde le corresponde —el **intercepto aleatorio** (la *fragilidad* del modelo mixto)—, que
es justo lo que dibujamos más abajo, ya **modelizado**, en el *caterpillar* de los 24 centros.


:::: {.callout-tip}
## 🔧 En R. Ajustar un GLMM con `glmer` y la sintaxis `( | )`
Un GLMM binomial se ajusta con **`lme4::glmer`** (o `glmmTMB::glmmTMB`, más rápido y flexible),
con `family = binomial`, **igual para datos individuales y agrupados** —solo cambia la respuesta,
como en `glm`—:

- **Individual** (0/1 por sujeto): `glmer(y ~ x1 + x2 + (1 | g), family = binomial, data = d)`.
- **Agrupado** (éxitos/ensayos por fila): `glmer(cbind(exitos, fracasos) ~ x1 + x2 + (1 | g), family = binomial, data = d)` (o la proporción con `weights = m`).

La parte `( ... | grupo )` declara **qué** varía por grupo:

- `(1 | g)` — **intercepto** aleatorio por `g`.
- `(1 + x | g)` (o `(x | g)`) — intercepto **y** pendiente de `x`, **correlados**.
- `(0 + x | g)` — **solo** pendiente de `x` (sin intercepto aleatorio).
- `(1 + x || g)` — intercepto y pendiente, **sin** correlación.
- `(1 | g1) + (1 | g2)` — dos agrupamientos **cruzados**; `(1 | g1/g2)` para **anidados**.
::::

```{r}
#| label: u14-glmm
library(lme4)
m_pool  <- glm  (ever ~ x1 + x2,                    family = binomial, data = cohorte)  # complete pooling
m_int   <- glmer(ever ~ x1 + x2 + (1 | centro),      family = binomial, data = cohorte)  # intercepto aleatorio
m_slope <- glmer(ever ~ x1 + x2 + (1 + x1 | centro), family = binomial, data = cohorte)  # + pendiente de x1

# Resumen de los modelos
summary(m_pool)
summary(m_int)
summary(m_slope)

# Contraste con la verdad conocida del DGP:
attr(cohorte, "verdad")$binaria$beta   # b1 = 0.85, b2 = -0.65 (condicional)
fixef(m_int)                           # se acerca más a la verdad que coef(m_pool)
VarCorr(m_int)                         # estima sigma_u (verdad: 0.70)

VarCorr(m_slope)                       # sigma_u0, sigma_u1 (pendientes de x1) y su correlación
ranef(m_slope)$centro                  # desviaciones u_0j, u_1j por centro
```

**Cómo se lee la salida de `glmer` (y en qué se distingue de `glm`).** El bloque de **efectos
fijos** es idéntico al de `glm` —coeficientes con su error estándar, $z$ y $p$, que se interpretan
igual (log-odds, $e^{\beta}$ = OR)—. La novedad es el bloque **`Random effects`**, encima: ahí están
las **varianzas** y desviaciones típicas de los efectos aleatorios —en `m_int`, una sola fila,
`centro (Intercept)` con $\hat\sigma_u=0{,}44$— y el número de grupos. `glm` no tiene ese bloque:
estima un único intercepto y nada más. En `m_slope` el bloque crece a dos filas (`(Intercept)` y
`x1`) más su **correlación**.

**Efectos fijos: condicional vs marginal, y los errores estándar.** Los coeficientes de `m_int`
($x_1=1{,}08$, $x_2=-0{,}66$) son algo **mayores** en magnitud que los del *pooled* (`m_pool`:
$x_1=1{,}02$, $x_2=-0{,}63$): es la atenuación —leve aquí, porque $\sigma_u$ es pequeño— del efecto
**marginal** frente al **condicional**. Y, sobre todo, los **errores estándar crecen** en el mixto
(el del intercepto pasa de $0{,}106$ a $0{,}142$), porque `m_int` **tiene en cuenta** el agrupamiento
por centro que el *pooled* ignoraba: el *pooled* daba una precisión **falsamente alta**. Frente a la
verdad del DGP ($x_1=0{,}85$, $x_2=-0{,}65$, $\sigma_u=0{,}70$), `m_int` recupera bien `x2`, deja `x1`
algo alto y **subestima** $\sigma_u$ (ICC latente $\approx0{,}06$ frente a $0{,}13$), algo habitual
con respuesta binaria y pocos grupos.

**El efecto de centro, dibujado.** El modelo no estima un número por centro, sino su *distribución*;
pero los interceptos **predichos** (`ranef`, ya **encogidos** hacia 0) hacen visible esa
heterogeneidad entre clínicas —la *fragilidad* del modelo mixto—:

```{r}
#| label: fig-u14-cohorte-caterpillar
#| fig-width: 7
#| fig-height: 6
#| out-width: "85%"
#| fig-cap: "Efecto de centro modelizado en la cohorte: interceptos aleatorios de los 24 centros (desviación en log-odds respecto al riesgo medio) con IC 95%, ordenados. La dispersión del conjunto es lo que el mixto resume en sigma_u; los centros cuyo intervalo no cruza el 0 se separan del promedio."
re_coh <- as.data.frame(ranef(m_int, condVar = TRUE))   # columnas: grp, condval, condsd
ggplot(re_coh, aes(reorder(grp, condval), condval)) +
  geom_hline(yintercept = 0, linetype = "dashed") +
  geom_pointrange(aes(ymin = condval - 1.96 * condsd,
                      ymax = condval + 1.96 * condsd), linewidth = 0.3) +
  coord_flip() +
  labs(x = "Centro", y = "Intercepto aleatorio (log-odds)")
```

Cada punto es la desviación estimada de un centro respecto al riesgo medio; el **shrinkage** acerca a
0 a los centros con menos datos, y la **amplitud** del conjunto es la traducción visual de
$\hat\sigma_u$.

**¿Hace falta la pendiente aleatoria?** En `m_slope`, `VarCorr` da $\hat\sigma_{u_1}=0{,}40$ con una
correlación casi extrema, $-0{,}80$; pero el **AIC apenas cambia** ($899{,}0\to898{,}1$, despreciable)
y el LRT frente a `m_int` no es significativo ($p\approx0{,}08$, sobre 2 g.l.). Como el DGP **no**
incluye pendiente aleatoria, ese $0{,}40$ es **ruido sobreajustado**: los datos dicen que no se
justifica. Es exactamente cómo se decide —se añade, se compara por AIC/LRT y se descarta si no
aporta—.

## 4.5 El modelo mixto sobre datos reales: GLOW por centro {#sec-u14-glmm-glow}

La cohorte tiene la ventaja de la **verdad conocida**, pero 24 centros son muchos para *verlos*.
GLOW ofrece el contrapunto **real e intuitivo**: 500 mujeres seguidas en **6 centros** (`site_id`).
Las pacientes de un mismo centro comparten contexto asistencial, así que cabe el **mismo GLMM
logístico** con intercepto aleatorio por centro, ahora sobre la respuesta real `fracture`:

```{r}
#| label: u14-glow-mixto
library(lme4)
glow_m <- glow |> mutate(site_id = factor(site_id))

m_glow_pool <- glm  (fracture ~ age + priorfrac,                 family = binomial, data = glow_m)
m_glow_mix  <- glmer(fracture ~ age + priorfrac + (1 | site_id), family = binomial, data = glow_m)

summary(m_glow_mix)

# Varianza entre centros e ICC latente
vc <- as.data.frame(VarCorr(m_glow_mix))
c(sigma_u = sqrt(vc$vcov[1]), ICC = vc$vcov[1] / (vc$vcov[1] + pi^2 / 3))

# Efectos fijos: agrupado (glm) vs mixto (glmer)
cbind(pooled = coef(m_glow_pool), mixto = fixef(m_glow_mix))
```

```{r}
#| label: fig-u14-glow-caterpillar
#| fig-width: 6.5
#| fig-height: 3.5
#| out-width: "75%"
#| fig-cap: "Efecto de centro modelizado en GLOW: intercepto aleatorio de cada uno de los 6 centros (desviación en log-odds respecto al riesgo medio) con IC 95%. Los seis se apiñan en torno a 0 y todos los intervalos lo cruzan: la variación entre centros es mínima (ICC latente ≈ 0,7%)."
re_glow <- as.data.frame(ranef(m_glow_mix, condVar = TRUE))   # grp, condval, condsd
ggplot(re_glow, aes(reorder(grp, condval), condval)) +
  geom_hline(yintercept = 0, linetype = "dashed") +
  geom_pointrange(aes(ymin = condval - 1.96 * condsd,
                      ymax = condval + 1.96 * condsd)) +
  coord_flip() +
  labs(x = "Centro (site_id)", y = "Intercepto aleatorio (log-odds)")
```

Y el resultado es **claro**: la variación entre centros es **mínima**. El modelo estima
$\hat\sigma_u=0{,}15$ (log-odds) y un **ICC latente de apenas $0{,}7\,\%$** —casi todo el riesgo está
**dentro** de los centros, no entre ellos—. El *caterpillar* lo confirma: los **seis** interceptos se
apiñan en torno a $0$ (de $-0{,}13$ a $+0{,}09$) y **todos** los intervalos lo cruzan, así que ninguno
se separa del riesgo medio. Y como el efecto aleatorio es tan pequeño, los efectos fijos del mixto son
**prácticamente idénticos** a los del *pooled* (edad $\approx+0{,}04$ por año; fractura previa
$\widehat{\text{OR}}\approx e^{0{,}85}\!\approx\!2{,}3$). En GLOW, **agrupar por centro apenas
aporta**: un hallazgo legítimo —no toda jerarquía esconde heterogeneidad— y el contraste perfecto con
la cohorte, donde el centro **sí** estructuraba el riesgo ($\hat\sigma_u=0{,}44$, ICC $\approx 6\,\%$).

Esta base hace explícitos dos **límites** que la cohorte disimulaba. Con **solo 6 grupos**, la
varianza $\sigma_u$ se estima con **mucha incertidumbre** —por eso la cohorte, con 24 centros y
verdad conocida, sigue siendo el banco de pruebas riguroso—; y un $\hat\sigma_u$ tan pequeño (y, en
otros casos, **pegado al borde**) es el tipo de situación de **frontera** que el enfoque bayesiano
maneja con más suavidad (callout al cierre). GLOW gana en **intuición** (pocos centros, reales,
dibujables); la cohorte, en **rigor**.

## 4.6 Reactivación: el ordinal mixto {#sec-u14-ordinal-mixto}

En la [Sección 3.4](#sec-u13-ordinal) ajustamos la respuesta ordinal `sever_ord` con **efectos
fijos** (`MASS::polr`). Como la cohorte está agrupada por centro, ahora añadimos un **intercepto
aleatorio** y cerramos el modelo de odds proporcionales en su versión **mixta**:
$$
\operatorname{logit}\Pr(S_{ij}\le k)=\theta_k-(\beta_1 x_{1ij}+\beta_2 x_{2ij}+w_j),
\qquad w_j\sim\mathcal{N}(0,\sigma_w^2),
$$
con umbrales ordenados $\theta_1<\theta_2$ y un **único** vector de efectos (proporcionalidad de
odds). Se ajusta con `ordinal::clmm`:

```{r}
#| label: u14-ordinal-mixto
library(ordinal)
m_ord <- clmm(sever_ord ~ x1 + x2 + (1 | centro), data = cohorte)
summary(m_ord)
tidy(m_ord)
attr(cohorte, "verdad")$ordinal          # theta, beta y sigma_w verdaderos
# El mixto recupera mejor los umbrales/efectos que la versión de efectos fijos
# (polr de 1.3), que ignora w_j y atenúa: ese contraste es la lección del hilo mixto.
```

El contraste con la verdad es **excelente**: `clmm` recupera los **umbrales** ($\hat\theta_1=-0{,}64$,
$\hat\theta_2=0{,}86$; verdad $-0{,}6$ y $0{,}9$), los **efectos** ($\hat\beta_{x_1}=0{,}91$,
$\hat\beta_{x_2}=-0{,}50$; verdad $0{,}8$ y $-0{,}5$) y la **varianza entre centros**
($\hat\sigma_w=0{,}62$; verdad $0{,}6$). Como el DGP **sí** incluye un intercepto aleatorio en el
ordinal, el mixto estima cantidades **condicionales** próximas a la verdad; la versión de efectos
fijos (`polr`), que ignora $w_j$, las **atenuaría** —ese es justo el contraste que se pide en la
tarea—.

::: {.callout-note}
## El multinomial *nominal* mixto: una frontera
El ordinal mixto ha sido directo porque el modelo de odds proporcionales tiene **un solo**
predictor lineal, y añadirle un efecto aleatorio cabe en `ordinal::clmm`. El **nominal** no: el
logit de categoría base tiene **$K-1$ predictores lineales a la vez**, y la pila habitual no lo
cubre —`nnet`/`VGAM` son de efectos fijos, y `glmer`/`glmmTMB` no ajustan respuesta nominal—. Para
hacerlo hacen falta herramientas especializadas: `mclogit::mblogit` (frecuentista) o, en enfoque
bayesiano, `brms` (`family = categorical()`) o `MCMCglmm`. Queda fuera del alcance del curso; lo
señalamos como extensión.
:::

::: {.callout-note}
## Los efectos aleatorios encajan mejor en el enfoque bayesiano
La distinción **fijo vs aleatorio** es, en parte, una incomodidad **frecuentista**: los aleatorios
se tratan como variables latentes que hay que **integrar** de la verosimilitud (con aproximaciones
—Laplace, cuadratura—), y su varianza puede quedar **pegada al borde** —los ajustes *singulares* o
el $\hat\sigma_u$ subestimado que vimos—. En el marco **bayesiano** no hay tal dicotomía: **todos**
los parámetros tienen distribución, y un efecto aleatorio es solo un parámetro con un **prior
jerárquico** ($u_j\sim\mathcal{N}(0,\sigma_u^2)$, con su propio prior para $\sigma_u^2$). El
*partial pooling* **emerge solo** de esa jerarquía, la incertidumbre de la varianza se propaga
entera, los problemas de frontera se manejan con suavidad y estructuras complejas (como el nominal
mixto) encajan sin forzar. Herramientas: `brms`, `rstanarm`, `MCMCglmm`.
:::

::: {.callout-important}
## Tarea del estudiante (1.4)
1. **Pooled vs GLMM (RA6).** Ajusta `ever ~ x1 + x2` en dos versiones: *pooled* (`glm`) y con
   intercepto aleatorio (`glmer`, `(1 | centro)`). Compara `coef(m_pool)` con `fixef(m_int)`,
   contrasta ambos con la **verdad** del DGP y explica por qué el *pooled* **atenúa** los efectos
   (condicional vs marginal). Interpreta el **ICC**.
2. **Pendiente aleatoria.** Añade `(1 + x1 | centro)` y examina `VarCorr`: interpreta $\sigma_{u_1}$
   y su correlación con el intercepto, y razona —a la vista de la varianza estimada, la correlación y la
   convergencia— si los datos justifican la pendiente aleatoria.
3. **Ordinal mixto.** Ajusta el ordinal con `clmm` y compáralo con la versión de efectos fijos
   (`polr`) de la [Sección 3.4](#sec-u13-ordinal): comenta qué cambia en umbrales y efectos, y por
   qué.
:::

<!-- Partial: incluir con {{< include _unidad_1_5.qmd >}}. Sin YAML.
     Chunks prefijados u15-. Usa la cohorte simulada y su expansión pp.
     Esta unidad absorbe la fundamentación que antes vivía en el apéndice. -->

# 5 · Supervivencia: del hazard al GLM en tiempo discreto {#sec-u15}


Las unidades anteriores respondían a *«¿ocurre el evento?»*: una fractura, sí o no, modelada con la
logística. Pero a esa pregunta le falta otra —*«¿y **cuándo**?»*—. Pasar de la ocurrencia al
**tiempo hasta el evento** abre el **análisis de supervivencia**, el segundo hilo en espiral del
curso. Sin salir del marco GLM, el tiempo-a-evento en **tiempo discreto** se reescribe como una
regresión binomial con enlace `cloglog`, y nos permitirá integrar efectos de agrupación con modelos mixtos como los que ya vimos en la [Unidad 4](#sec-u14)—.

**Los datos.** 

GLOW se queda corto aquí: registra si hubo fractura en el primer año, pero no
**cuándo** ni a quién se dejó de observar. Por eso trabajamos con una
[**cohorte simulada**](#sec-datos-cohorte) —descrita al detalle, con todas sus variables y su
proceso generador, en la Presentación del caso— inspirada en el mismo contexto clínico (fracturas
en mujeres seguidas en varias clínicas), que
añade justo lo que falta: un **seguimiento por periodos** (revisiones anuales > tiempo discreto), el **tiempo hasta la fractura** (`tiempo`)
y si llegamos a **observar el evento** o no (`evento`: 1 = fractura observada; 0 = **censura**, p. ej.
fin administrativo del seguimiento). Las covariables tienen lectura clínica: `x1` es un marcador de
**fragilidad ósea** (baja densidad mineral, estandarizada en puntuación z), un **factor de riesgo
continuo** —a mayor valor, hueso más frágil y **más** riesgo ($\beta_1>0$)—; `x2` es un **factor
binario** y **protector** (recibir o no un **tratamiento** preventivo, $\beta_2<0$); y `centro` es la
**clínica** a la que pertenece cada paciente —el agrupamiento que trataremos como **fragilidad**
(intercepto aleatorio)—. Al ser
simulada, conocemos la **verdad** del proceso generador y podremos comprobar que el ajuste la recupera.

**Qué cambia respecto a antes.** 

Hasta ahora estimábamos la **probabilidad** de un desenlace binario. Ahora el objeto es **dinámico**, e interesa:
- el **riesgo a lo largo del tiempo** —el *hazard*: ¿qué
riesgo de fracturar hay en cada periodo, entre quienes llegan sin fractura?—, 
- la **curva de  supervivencia** $S(t)$ —¿qué fracción sigue sin fractura pasado $t$?— 
- y cómo las covariables **aceleran o retrasan** el evento (los *hazard ratios*). 

Y además, aparece un ingrediente nuevo, la
**censura**: hay que aprovechar la información de las pacientes a las que **no** vimos fracturar en
la ventana de observación, sin sesgar. Todo ello, como veremos, cabe dentro del GLM.

::: {.callout-note}
## Sobre esta unidad
**RA implicados:**
RA7 (censura y tiempo-a-evento como casos del marco GLM).
[@dobson2018, cap. de supervivencia; @prentice1978; @allison1982; @cox1972;
@aitkin1980]
:::

## 5.1 La función de supervivencia y la función de riesgo {#sec-u15-hazard}

Sea $T\ge 0$ el **tiempo hasta que ocurre el evento**. En **tiempo continuo**, toda la información
sobre $T$ se resume en **tres representaciones equivalentes** —conocer una determina las otras dos—:

- **Función de supervivencia.** $S(t)=\Pr(T>t)$, la probabilidad de "seguir sin evento" pasado $t$.
Se interpreta directamente ($S(5)=0{,}8$: el 80 % llega sin evento más allá de 5), cumple $S(0)=1$,
es **no creciente** y $S(t)\to 0$.

- **Función de riesgo (hazard).** Riesgo **instantáneo** de que el evento ocurra en $t$, **condicionado
a no haber ocurrido antes**:
$$
h(t)=\frac{f(t)}{S(t)}=-\frac{\mathrm{d}}{\mathrm{d}t}\log S(t).
$$
Es una **tasa** (intensidad por unidad de tiempo, **no** acotada por 1), no una probabilidad.

- **Hazard acumulado.** Se lee como el **riesgo total acumulado** hasta $t$ —la suma de los riesgos instantáneos— o, de forma equivalente, como el **número esperado de eventos** que ocurrirían hasta $t$ si el evento
pudiera repetirse; de ahí que **no** sea una probabilidad y pueda **superar 1**. 
$$H(t)=\int_0^t h(u)\,\mathrm{d}u=-\log S(t)$$
Como $S(t)=e^{-H(t)}$, cada unidad de hazard acumulado multiplica la supervivencia por $e^{-1}\approx0{,}37$
(así, $H(t)=1$ equivale a $S(t)\approx0{,}37$).

Cuando el tiempo se observa **por periodos** $t=1,2,\dots$ —como en nuestra cohorte—, estas
funciones tienen su **contrapartida en tiempo discreto**:

- **Hazard discreto.** De los sujetos que llegan al periodo $t$ sin evento (el **conjunto de riesgo**
en $t$), $h_t$ representa la **fracción que falla durante ese periodo**:
$$
h_t=\Pr(T=t\mid T\ge t)\in[0,1].
$$
A diferencia del continuo, $h_t$ es una **probabilidad** (acotada por 1); en lenguaje de tablas de vida es
la **probabilidad condicional de fallo** $q_t$. Se relaciona con el hazard continuo agrupando el
riesgo del intervalo:
$$
h_t=1-\frac{S(t)}{S(t-1)}=1-\exp\!\Big(-\!\int_{t-1}^{t} h(u)\,\mathrm{d}u\Big),
$$
de modo que **coincide con la tasa continua cuando el riesgo es pequeño** y se satura en 1 cuando es
alto.

- **Supervivencia discreta.** Se reconstruye periodo a periodo, $S_t=\prod_{k=1}^{t}(1-h_k)$ —el
análogo discreto de $S(t)=e^{-H(t)}$—.

::: {.callout-warning}
## Tasa vs probabilidad
El hazard **continuo** $h(t)$ es una **tasa** (puede superar 1); el hazard **discreto** $h_t$ es una
**probabilidad** ($\in[0,1]$). Comparten la idea —riesgo de fallar ahora, dado que aún no ha
fallado— pero no la escala.
:::

**La conexión con Bernoulli (y con el GLM).**

En nuestros datos, cada periodo en riesgo es un **ensayo de Bernoulli** con probabilidad $h_t$: el
sujeto "falla" (evento) o "sobrevive" ese periodo. Una paciente **censurada** es aquella que sale del
seguimiento sin haber tenido una fractura (se acaba el periodo de observación); la censura no
está vinculada al riesgo de fractura.

Disponer los datos con **una fila por sujeto y periodo en riesgo** (persona-periodo) convierte el
problema en una **secuencia de ensayos de Bernoulli** con probabilidad $h_t$. La verosimilitud de
supervivencia se **factoriza** exactamente como un producto de Bernoulli sobre los periodos,

$$ P(T=t)=\begin{cases}
h_t \prod_{s<t} (1-h_s) & \text{ si evento en } t\\
\prod_{s \leq c} (1-h_s) & \text{ si censura}
\end{cases}$$

así que un **GLM binomial ordinario** (con enlace cloglog, [Sección 5.3](#sec-u15-glm))
maximiza la verosimilitud correcta, sin maquinaria extra.

::: {.callout-note}
## ¿Hace falta un efecto aleatorio por paciente?
Aunque cada sujeto aporte **varias filas** en el formato persona-periodo, **no**. Las filas de un
mismo sujeto son **condicionalmente independientes** dadas las covariables y los interceptos por
periodo —es la factorización de la verosimilitud de arriba, no una aproximación—, de modo que un GLM
ordinario ya maximiza la verosimilitud correcta. Los efectos aleatorios entran en **otro nivel**: el
de la **clínica** (`centro`), como *fragilidad compartida* (la ajustamos en la [Sección 5.3](#sec-u15-glm)). Una
*fragilidad individual* por paciente sería una extensión posible —no un requisito— y, además, difícil
de identificar con tan pocos periodos por sujeto.
:::


## 5.2 Precedentes en clave de supervivencia: Kaplan–Meier y Cox {#sec-u15-ph}

El análisis de supervivencia tiene un instrumental **propio**, anterior al GLM, que conviene
conocer antes de construir el nuestro: aporta el contexto, el vocabulario y una **vara de
medir**. Lo recorremos en tres pasos, de lo más descriptivo a lo más estructurado.

Primero, **Kaplan–Meier**: la forma **no paramétrica** de *dejar hablar a los datos*, que dibuja
la curva de supervivencia y compara grupos sin suponer nada sobre el riesgo. Después, los
**modelos de riesgos proporcionales**, el **marco** común que permite pasar de *describir* a
*explicar* —cómo las covariables modulan el riesgo— y del que cuelgan tanto Cox como nuestro
GLM. Y por último el **modelo de Cox**, la regresión **semiparamétrica** estándar, que pone ese
marco a trabajar sobre la cohorte.

Estas técnicas son el **precedente** y el **banco de pruebas** del modelo que construiremos en la
[Sección 5.3](#sec-u15-glm): que el GLM **recupere lo mismo** que Cox y que las curvas de
Kaplan–Meier será la **validación** de que vamos bien. Pero el GLM no se queda en reproducirlas:
aporta lo que estas aproximaciones **no** dan —**predecir** el riesgo base y la supervivencia
periodo a periodo, incorporar la **fragilidad** entre clínicas como efecto aleatorio y **relajar
la proporcionalidad** cuando haga falta—, y todo **dentro de un único marco**, el de los GLM que
ya manejamos. La validación es el punto de partida; su **alcance**, la razón de usarlo.

### Kaplan–Meier: dejar hablar a los datos {#sec-u15-km}

En supervivencia, lo primero es **mirar los datos**, sin comprometerse todavía con ningún
modelo. Antes de imponer una fórmula al riesgo o un efecto a las covariables, interesa **ver**
la forma de la curva de supervivencia —cómo y cuándo cae— y si dos grupos se separan. Ese es el
terreno de los métodos **no paramétricos**: en lugar de estimar los parámetros de un modelo,
**leen la curva directamente de los datos**, con el mínimo de supuestos. Su herramienta de
referencia —y casi siempre el primer paso de cualquier análisis de supervivencia— es el
estimador de **Kaplan–Meier**.

El estimador de **Kaplan–Meier** [@kaplan1958] reconstruye la curva de supervivencia **sin
imponer ninguna forma funcional**: en cada instante con eventos multiplica la fracción que
"sobrevive",
$$
\widehat S(t)=\prod_{t_k\le t}\Bigl(1-\frac{d_k}{n_k}\Bigr),
$$
con $d_k$ eventos y $n_k$ sujetos en riesgo en $t_k$. Fíjate en que es **exactamente** el
producto $\prod(1-h_k)$ de la [Sección 5.1](#sec-u15-hazard), con el hazard estimado de forma empírica,
$\widehat h_k=d_k/n_k$: Kaplan–Meier es el **hermano no paramétrico** de la supervivencia
discreta $S_t=\prod(1-h_k)$, y es especialmente útil para comparar curvas de supervivencia entre grupos/perfiles. Su visualización nos permite responder  preguntas descriptivas del tipo : ¿qué grupo sobrevive más?, ¿en qué momento empieza a agravarse el riesgo en un perfil?, ...

```{r}
#| label: fig-u15-km
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-cap: "Kaplan–Meier de la cohorte por brazo de tratamiento (x2): describe la caída de S(t) sin modelo, y el log-rank contrasta los dos grupos. KM solo admite grupos discretos —no la fragilidad continua x1— y no entrega hazard ratios; para eso hace falta un modelo."
library(survival)
library(survminer)

km <- survfit(Surv(tiempo, evento) ~ x2, data = cohorte)
ggsurvplot(km, data = cohorte, conf.int = TRUE, pval = TRUE,
           legend.title = "Tratamiento",
           legend.labs  = c("No", "Sí"),
           xlab = "Periodo de revisión", ylab = "Supervivencia S(t)")
```

En la cohorte el patrón es el esperado: las **tratadas** (`x2` = Sí) se mantienen **por encima**
de las no tratadas y la separación **crece con los periodos** —arrancan casi juntas y, al sexto,
la supervivencia ronda $0{,}73$ frente a $\approx0{,}56$—; el **log-rank** confirma que la
diferencia no es ruido ($p=0{,}0027$).

KM **describe** y **compara**: visualiza $S(t)$ y, con la prueba **log-rank** [@mantel1966],
contrasta si dos grupos tienen curvas distintas. Pero tiene limitaciones importantes y se queda como una primera herramienta descriptiva: **no ajusta por covariables**, no
admite predictores **continuos** (la fragilidad `x1` se queda fuera) y no entrega un **tamaño
de efecto** interpretable. Para eso hace falta una **regresión**.

### Modelos de Riesgos Proporcionales {#sec-u15-prop}

El modelo de Cox que veremos a continuación y el GLM de tiempo discreto que ajustaremos en la [Sección 5.3](#sec-u15-glm) parecen herramientas distintas, pero comparten el mismo esqueleto: ambos son modelos de riesgos proporcionales. Esta familia es el paraguas del que cuelgan los modelos de regresión para supervivencia —incluidos los que, como el nuestro, se expresan como un GLM—; por eso conviene presentarla antes que cualquiera de los dos: explica por qué aparecen tanto Cox como el GLM y en qué se diferencian.

Todos sus miembros comparten una misma anatomía —una forma temporal del riesgo, común a todas las pacientes, modulada por un efecto multiplicativo de las covariables— y se distinguen solo en cómo tratan esa forma temporal, el hazard base: Cox la deja libre (semiparamétrico), los modelos paramétricos le imponen una fórmula, y el GLM de tiempo discreto la modela periodo a periodo. El efecto de las covariables —el hazard ratio— se lee igual en todos. Veamos la anatomía en detalle.


El modelo de **riesgos proporcionales** separa el riesgo en dos piezas:
$$
h(t;\mathbf{x})=h_0(t)\,\exp(\mathbf{x}'\boldsymbol\beta).
$$
- El primer factor es el **hazard base** $h_0(t)$, que representa la **forma del riesgo a lo largo del tiempo** para el individuo de referencia ($\mathbf{x}=\mathbf 0$) —en nuestra cohorte, una mujer **no tratada,
de fragilidad media y en la clínica de referencia**—. Es el "telón de fondo" temporal: dice
si el riesgo de fractura **crece, decrece o es plano** con los periodos, al margen de quién
sea la paciente. 
>En el modelo discreto que ajustaremos, $h_0$ no es una curva continua sino
**un valor por periodo**, recogido por los **interceptos $\alpha_t$** (uno por revisión):
estimar el hazard base será, literalmente, estimar esos $\alpha_t$ —y en la [Sección 5.3](#sec-u15-glm) veremos que
**crecen**, recuperando el riesgo base creciente del simulador—.

- El segundo factor, $\exp(\mathbf{x}'\boldsymbol\beta)$, es la **escala**, ese telón de fondo común **en todos los instantes**. 

Estos modelos se denominan de "riesgos proporcionales" porque el **cociente de riesgos** entre dos
pacientes no depende de $t$.
$$
\frac{h(t;\mathbf{x}_a)}{h(t;\mathbf{x}_b)}=\exp\bigl((\mathbf{x}_a-\mathbf{x}_b)'\boldsymbol\beta\bigr),
$$
Gráficamente se muestra en la @fig-u15-ph-ilustra. 
- En escala **log-hazard** las curvas de dos grupos van
**paralelas** (separadas por una distancia fija) y **nunca se cruzan**. Esa constancia es
**el supuesto** del modelo: el tratamiento —o la fragilidad— multiplica el riesgo por el
**mismo** factor en el primer periodo y en el sexto. 
- En la escala log-riesgo el modelo es
**aditivo**, $\log h(t;\mathbf{x})=\log h_0(t)+\mathbf{x}'\boldsymbol\beta$, y cada
$\exp(\beta_k)$ es un **hazard ratio**: el factor por el que se multiplica el riesgo ante un
aumento unitario de $x_k$ ($>1$ aumenta el riesgo, $<1$ protege, $=1$ indiferente). 


```{r}
#| label: fig-u15-ph-ilustra
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-width: 9
#| fig-height: 3.6
#| fig-cap: "Qué quiere decir 'proporcional', en dos escalas (hazard base creciente de juguete, HR = 2). Izquierda (hazard): el grupo con la covariable más alta es el de referencia multiplicado por un factor constante exp(β) en todos los periodos —la brecha se ensancha porque es multiplicativa—. Derecha (log-hazard): ese mismo efecto es una distancia vertical fija (β); las curvas van paralelas y nunca se cruzan. Si esa distancia cambiara con el tiempo (o las curvas se cruzaran), la proporcionalidad fallaría."

beta <- log(2)                                  # efecto de la covariable: HR = exp(beta) = 2
base <- tibble(periodo = 1:6,
               h0 = c(0.05, 0.07, 0.09, 0.11, 0.13, 0.15))  # hazard base creciente (ilustrativo)

base |>
  mutate(`Referencia (x = 0)` = h0,
         `Grupo (x = 1)`      = h0 * exp(beta)) |>
  pivot_longer(c(`Referencia (x = 0)`, `Grupo (x = 1)`),
               names_to = "grupo", values_to = "hazard") |>
  mutate(`Escala hazard` = hazard, `Escala log-hazard` = log(hazard)) |>
  pivot_longer(c(`Escala hazard`, `Escala log-hazard`),
               names_to = "escala", values_to = "valor") |>
  ggplot(aes(periodo, valor, color = grupo)) +
  geom_line(linewidth = 1) +
  geom_point(size = 2) +
  facet_wrap(~escala, scales = "free_y") +
  scale_color_manual(values = c("Referencia (x = 0)" = "grey55",
                                "Grupo (x = 1)"      = "#2c7fb8")) +
  labs(x = "Periodo de revisión", y = NULL, color = NULL) +
  theme_minimal(base_size = 12) +
  theme(legend.position = "top")
```

::: {.callout-note}
## Cuándo falla la proporcionalidad
El supuesto de proporcionalidad se puede romper de dos formas.

- En **un efecto que se desvanece**, el hazard ratio se mueve **hacia 1 sin llegar a
  cruzarlo**: la ventaja se diluye y las curvas **convergen, pero no se cortan**. Es lo que
  ocurre con una **vacuna** cuya protección decae con el tiempo, o por **depleción de
  susceptibles** —los pacientes más frágiles tienen el evento pronto y queda una población
  seleccionada más robusta, de modo que el efecto del factor de riesgo se atenúa—.
- En **curvas que se cruzan**, el hazard ratio **pasa de $>1$ a $<1$** y la ventaja se
  **invierte**. El caso de manual es **cirugía frente a tratamiento conservador**: riesgo alto
  al principio (mortalidad perioperatoria) y menor después, así que las curvas de supervivencia
  **se cortan**.

La señal práctica: si las curvas de **Kaplan–Meier** **convergen** sin tocarse, es un efecto que
se desvanece; si **se cortan**, es un cruce. En cualquiera de los dos casos —o si el efecto de
una covariable **cambia con el tiempo**, o si el tratamiento tiene un **efecto inicial fuerte que
luego desaparece**— un **único** hazard ratio deja de describir bien el efecto, y el **modelo de
Cox básico** (o nuestro GLM con un solo HR) puede ser **inadecuado**: hay que recurrir a sus
extensiones —coeficientes que varían en el tiempo, estratificación—.
:::

**¿Es razonable asumirla aquí?** La proporcionalidad es un supuesto, pero **se puede comprobar**, y
con una covariable a mano el chequeo es directo. Para la binaria `x2`, la propia curva de
Kaplan–Meier ya da una primera respuesta: bajo proporcionalidad las curvas de los dos grupos **no se
cruzan** y se separan de forma ordenada —justo lo que veíamos, las tratadas siempre por encima con la
brecha creciendo—, así que no hay señal de alarma. El chequeo gráfico **más fino** es el **diagrama
log–log**: representar $\log(-\log\widehat S(t))$ frente a $\log t$ —es decir, el `cloglog` de la
supervivencia de Kaplan–Meier— debería dar curvas aproximadamente **paralelas** si el supuesto se
cumple. Que el diagnóstico natural sea precisamente el `cloglog` no es casualidad: es el enlace del
modelo que construiremos.

```{r}
#| label: fig-u15-km-loglog
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-width: 7
#| fig-height: 4
#| fig-cap: "Diagrama log-log de Kaplan-Meier por tratamiento: log(-log S(t)) frente a log(tiempo). Bajo riesgos proporcionales las dos curvas son aproximadamente paralelas. Con solo seis periodos el chequeo es grueso, pero no se aprecia cruce ni convergencia, así que la proporcionalidad para x2 es asumible."
ggsurvplot(km, data = cohorte, fun = "cloglog",
           legend.title = "Tratamiento", legend.labs = c("No", "Sí"),
           xlab = "log(periodo)", ylab = "log(-log S(t))")
```

Para la covariable **continua** `x1`, Kaplan–Meier no sirve directamente (habría que
**categorizarla** en grupos); ahí el chequeo general son los **residuos de Schoenfeld** del modelo
ajustado —una tendencia temporal en ellos delata la no-proporcionalidad—, que dejamos para cuando
tengamos el ajuste.

:::: {.callout-tip}
## 🔧 En R. Estimar y dibujar la curva de Kaplan–Meier
Hace falta el paquete **`survival`** (y **`survminer`** para dibujarla). Los datos van **a nivel de
paciente** —una fila por mujer—, con el **tiempo** hasta el evento y el **indicador** de
evento/censura (1 = evento, 0 = censura). La respuesta se construye con `Surv(tiempo, evento)` y la
curva se estima con `survfit`:

```r
library(survival); library(survminer)
km <- survfit(Surv(tiempo, evento) ~ x2, data = cohorte)  # ~ 1 para la curva global
ggsurvplot(km, data = cohorte, pval = TRUE)               # añade la prueba log-rank
```

A la derecha de `~` va la variable de grupo (aquí `x2`), y `evento` debe codificarse 1 = evento,
0 = censura; con `fun = "cloglog"` se obtiene el diagrama log–log de arriba.
::::


### El modelo de Cox {#sec-u15-cox}

De las regresiones de supervivencia, la de **Cox** [@cox1972] es, con diferencia, la **más
utilizada**, y la razón es lo **poco** que exige. A diferencia de los modelos paramétricos, **no
supone ninguna distribución** para el tiempo hasta el evento —ni exponencial, ni Weibull, ni
ninguna otra—: la forma del riesgo a lo largo del tiempo, $h_0(t)$, se deja **completamente
libre**. Su único compromiso es la **proporcionalidad** de la subsección anterior. De ahí el
nombre **semiparamétrico**: el modelo es **paramétrico** en lo que interesa —el efecto de las
covariables, $\exp(\mathbf{x}'\boldsymbol\beta)$— y **no paramétrico** en el resto —el hazard
base—,
$$
h(t;\mathbf{x})=h_0(t)\exp(\mathbf{x}'\boldsymbol\beta).
$$

¿Cómo se estima $\boldsymbol\beta$ sin decir nada de $h_0(t)$? Mediante la **verosimilitud
parcial** [@cox1975]:
$$
L_{\text{parc}}(\boldsymbol\beta)=\prod_{i:\,\delta_i=1}
\frac{\exp(\mathbf{x}_i'\boldsymbol\beta)}{\sum_{j\in R(t_i)}\exp(\mathbf{x}_j'\boldsymbol\beta)},
$$
donde $R(t_i)$ es el **conjunto de riesgo** (los que siguen en seguimiento justo antes de $t_i$)
y $\delta_i=1$ marca un evento. Cada factor compara, **en el instante de cada evento**, el riesgo
de quien falla con el del resto de los que estaban en riesgo; en ese cociente **el hazard base
$h_0(t)$ se cancela**. Así, Cox estima los efectos **sin especificar ni estimar** la forma
temporal del riesgo —y de ahí buena parte de su **robustez**: no puede equivocarse en una
distribución que nunca llega a asumir—.

Esa combinación —**pocos supuestos**, manejo natural de la **censura** y un resultado
directamente **interpretable** (hazard ratios)— explica por qué Cox es el **estándar** en el
análisis de supervivencia y aparece en casi cualquier artículo clínico.

:::: {.callout-tip}
## 🔧 En R. Ajustar un modelo de Cox
Con el paquete **`survival`**. Los datos van **a nivel de paciente** (una fila por mujer), con el
**tiempo** hasta el evento y el **indicador** de evento/censura; la respuesta es de nuevo
`Surv(tiempo, evento)` y el modelo se ajusta con `coxph`:

```r
library(survival)
m_cox <- coxph(Surv(tiempo, evento) ~ x1 + x2, data = cohorte)
summary(m_cox)        # coeficientes, HR = exp(coef), IC y p-valores
```

`exp(coef(m_cox))` son los **hazard ratios**; `survfit(m_cox, newdata = ...)` predice la
supervivencia de perfiles concretos y `cox.zph(m_cox)` contrasta la proporcionalidad (residuos de
Schoenfeld). Para el bosque de hazard ratios, `survminer::ggforest(m_cox)`.
::::

```{r}
#| label: fig-u15-cox
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-cap: "Hazard ratios del modelo de Cox sobre la cohorte (fragilidad x1, tratamiento x2) con IC 95%. A diferencia de KM, Cox sí admite covariables continuas. Son, en esencia, los mismos efectos que recuperará el GLM de tiempo discreto en la Sección 5.3: la fragilidad eleva el riesgo (HR > 1) y el tratamiento lo reduce (HR < 1)."
m_cox <- coxph(Surv(tiempo, evento) ~ x1 + x2, data = cohorte)
ggforest(m_cox, data = cohorte)
```

Sobre la cohorte ($N=877$, $254$ eventos), Cox estima un HR de **2,31** para la fragilidad
(IC 95 % $2{,}02\!-\!2{,}64$) y de **0,64** para el tratamiento (IC $0{,}49\!-\!0{,}83$), ambos
con $p<0{,}001$: la fragilidad **más que dobla** el riesgo por cada desviación típica y el
tratamiento lo **reduce** alrededor de un tercio. El de fragilidad **clava** la verdad del
simulador ($e^{0{,}85}\approx2{,}34$); el del tratamiento queda algo **atenuado** frente a la
suya ($e^{-0{,}65}\approx0{,}52$), el sesgo hacia 1 propio de **omitir la fragilidad compartida
de las clínicas**.

Más allá de los coeficientes, Cox permite **predecir la curva de supervivencia** de un perfil
concreto de paciente —es la lectura clínica del hazard ratio—. El gráfico siguiente la traza a
lo largo de los seis periodos para tres niveles de fragilidad ósea y los dos brazos de
tratamiento:

```{r}
#| label: fig-u15-cox-pred
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-width: 9
#| fig-height: 4
#| fig-cap: "Supervivencia predicha por el modelo de Cox a lo largo de los periodos, para tres niveles de fragilidad ósea (x1 = -1, 0, 1) y los dos brazos de tratamiento (x2). A más fragilidad la curva cae antes; el tratamiento (panel derecho) la sostiene más arriba. Las curvas no se cruzan: es la firma de los riesgos proporcionales."
perfiles <- tidyr::expand_grid(
  frag = factor(c("Baja", "Media", "Alta"), levels = c("Baja", "Media", "Alta")),
  trat = factor(c("Sin tratamiento", "Con tratamiento"),
                levels = c("Sin tratamiento", "Con tratamiento"))) |>
  mutate(x1 = c(-1, -1, 0, 0, 1, 1), x2 = c(0, 1, 0, 1, 0, 1))

sf <- survfit(m_cox, newdata = as.data.frame(perfiles[, c("x1", "x2")]))
pred <- tibble(t    = rep(sf$time, nrow(perfiles)),
               S    = as.vector(sf$surv),
               frag = rep(perfiles$frag, each = length(sf$time)),
               trat = rep(perfiles$trat, each = length(sf$time)))
pred <- bind_rows(distinct(pred, frag, trat) |> mutate(t = 0, S = 1), pred) |>
  arrange(frag, trat, t)

ggplot(pred, aes(t, S, color = frag)) +
  geom_step(linewidth = 1) +
  facet_wrap(~trat) +
  scale_y_continuous(limits = c(0, 1)) +
  labs(x = "Periodo de revisión", y = "Supervivencia S(t) predicha (Cox)",
       color = "Fragilidad ósea")
```

Se lee de un vistazo: a más fragilidad la curva cae **antes y más hondo**, y el tratamiento
(panel derecho) **sostiene** la supervivencia más arriba en todos los periodos. Y, fieles al
supuesto, las curvas **no se cruzan**: el efecto es el mismo en cada periodo, solo cambia el
nivel.

Otra forma de mirarlo es **fijar el horizonte** y ver la supervivencia como **función de la
fragilidad**. El gráfico siguiente traza la probabilidad de llegar **sin fractura al final del
seguimiento** (sexto periodo) frente a `x1`, para los dos brazos de tratamiento:

```{r}
#| label: fig-u15-cox-x1
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-width: 8
#| fig-height: 4
#| fig-cap: "Supervivencia predicha por Cox al final del seguimiento (sexto periodo) en función de la fragilidad ósea (x1), para las pacientes sin y con tratamiento. Cae de forma suave y monótona al aumentar la fragilidad; el tratamiento (curva superior) la desplaza hacia arriba en todo el rango. La separación vertical entre ambas curvas es el efecto del tratamiento a esa fragilidad."
gx <- tidyr::expand_grid(x1 = seq(-2.5, 2.5, by = 0.1), x2 = c(0, 1))
sfx <- survfit(m_cox, newdata = as.data.frame(gx))
gx$S <- sfx$surv[nrow(sfx$surv), ]                 # supervivencia en el último periodo
gx$Tratamiento <- factor(gx$x2, levels = c(0, 1),
                         labels = c("Sin tratamiento", "Con tratamiento"))

ggplot(gx, aes(x1, S, color = Tratamiento)) +
  geom_line(linewidth = 1) +
  scale_y_continuous(limits = c(0, 1)) +
  labs(x = "Fragilidad ósea (x1, en puntuación z)",
       y = "Supervivencia al sexto periodo  S(6)", color = NULL) +
  theme(legend.position = "top")
```

A más fragilidad, **menos** supervivencia, de forma suave y monótona; y la curva de las tratadas
va **por encima** en todo el rango —el tratamiento ayuda a cualquier nivel de fragilidad, aunque
no invierte la tendencia—. Hay un detalle clínico importante: aunque el hazard ratio es
**constante**, el **beneficio absoluto** del tratamiento (la separación vertical entre las dos
curvas) **no** lo es. Es pequeño en las pacientes de baja fragilidad (casi todas llegan sin
fractura, $S(6)\approx0{,}95$ en ambos grupos) y en las de fragilidad extrema (donde el riesgo es
alto haya o no tratamiento), y **máximo en la franja intermedia**. Es la diferencia entre efecto
**relativo** (el HR, fijo) y efecto **absoluto** (depende del riesgo de base): la misma historia
de los hazard ratios, ahora en la escala que una paciente entiende —su probabilidad de no
fracturar—.

Cox nos ha dado el efecto de las covariables y la predicción **sin** modelar el riesgo base.
Pero queda **fuera del marco GLM** y, con nuestro tiempo discreto, no es la herramienta más
natural. En la [Sección 5.3](#sec-u15-glm) construimos el modelo equivalente **dentro** del GLM;
una vez ajustado, lo comparamos con Cox y derivamos las conclusiones clínicas.

## 5.3 La modelización GLM del problema de supervivencia {#sec-u15-glm}

Las técnicas de la [Sección 5.2](#sec-u15-ph) eran el **precedente**; ahora construimos
**nuestro** modelo, y lo hacemos como en la práctica: **a partir de los datos**, sin suponer que
conocemos el proceso que los generó. De las 877 mujeres de la cohorte tenemos tres piezas —un
**evento** binario (fractura sí/no), un **tiempo** discreto (la revisión en que se observa) y una
**censura** (seguimientos que terminan sin fractura)— y el objetivo es **integrarlas** en un
modelo que estime el riesgo, respete la censura y dé efectos interpretables, todo **dentro del
marco GLM**. La sección lo construye paso a paso: qué es la variable de interés y qué supuestos
hacemos, qué enlace usamos y por qué, cómo se ajusta e interpreta sobre la cohorte, en qué mejora
a Cox y qué conclusiones clínicas se derivan.

### De los datos al modelo: qué es $T$ y qué asumimos {#sec-u15-anatomia}

Démosle la vuelta al problema. En la práctica **no conocemos** el proceso que generó los datos;
solo tenemos lo observado, y de ahí hay que **construir** un modelo razonable. ¿Qué tenemos,
exactamente, de cada paciente de la cohorte?

- un **indicador de fractura** (ocurrió / no ocurrió),
- el **periodo de revisión** en que se observó el desenlace, y
- una marca de **censura** (si el seguimiento terminó sin fractura).

**La variable de interés es un tiempo discreto.** Llamemos $T$ al **tiempo hasta la fractura**.
Como no vemos el instante exacto, sino solo la **revisión** en que se detecta, $T$ es una
**variable aleatoria discreta** que toma valores en los periodos $\{1,2,\dots,6\}$. De cada
paciente observamos su último periodo y si terminó en **evento** ($\delta=1$) o en **censura**
($\delta=0$, es decir, solo sabemos que $T$ es **mayor** que ese periodo).

**No modelamos $T$ directamente, sino su hazard.** La distribución de $T$ con censura es incómoda
de manejar; en cambio, el **hazard discreto**
$$
h_t=\Pr(T=t\mid T\ge t)
$$
—la probabilidad de fracturar **en** la revisión $t$ **entre quienes llegan** sin fractura a ella
([Sección 5.1](#sec-u15-hazard))— se deja estimar de forma natural. La razón es que descompone la
historia de cada paciente en una **secuencia de preguntas binarias**: en cada revisión a la que
llega, ¿fractura, sí o no? Y una secuencia de respuestas sí/no es justo lo que un **GLM binomial**
sabe modelar.

Esa descomposición es el formato **persona-periodo** de la [Sección 5.1](#sec-u15-hazard): **una
fila por paciente y revisión en riesgo**, con `y = 1` solo en la revisión donde se detecta la
fractura. La **censura encaja sin esfuerzo**: una paciente censurada aporta las filas de los
periodos que sí se observaron (en todas, "no fractura") y deja de aportar después —no hay que
inventarle un valor de $T$—.

**Qué asumimos** (todo esto **sin** mirar cómo se generaron los datos):

- *Independencia condicional.* Las varias filas de una misma paciente cuentan como ensayos
  independientes **dadas** las covariables y el periodo —no porque sean sujetos distintos, sino
  por la factorización de la verosimilitud de supervivencia (el «punto fino» de la
  [Sección 5.1](#sec-u15-hazard))—. Por eso basta un GLM ordinario, sin efecto aleatorio de
  paciente.
- *Censura no informativa.* El motivo de abandonar el seguimiento **no** está ligado al riesgo de
  fracturar; bajo ese supuesto, la censura no introduce sesgo.
- *Riesgos proporcionales.* Asumimos que las covariables **multiplican** el hazard por un factor
  **constante en el tiempo** (el supuesto de la [Sección 5.2](#sec-u15-ph)). Esto permite resumir
  cada efecto en un único hazard ratio y, como veremos, **fija el enlace**. Si no se cumpliera, se
  relajaría con interacciones covariable × periodo.
- *Forma del riesgo base libre.* No imponemos ninguna fórmula a cómo cambia el riesgo con las
  revisiones: dejamos **un parámetro por periodo**, $\alpha_t$ (uno por revisión). Es lo más
  flexible posible —el análogo discreto de "no especificar $h_0(t)$" que hacía Cox—.

Con esas piezas, el modelo casi se escribe solo: un **GLM binomial** para el hazard de cada
periodo, con predictor lineal
$$
\eta_t(\mathbf{x})=\alpha_t+\mathbf{x}'\boldsymbol\beta
$$
—un intercepto por revisión ($\alpha_t$, el riesgo base) más los efectos de las covariables
($\boldsymbol\beta$)—. Queda **una sola decisión**: el **enlace** que conecta el hazard (una
probabilidad en $[0,1]$) con ese predictor. Y no es libre: el supuesto de **riesgos
proporcionales** lo determina. Lo vemos a continuación.

### El enlace complementary log-log y por qué es el natural {#sec-u15-cloglog}

El hazard es una **probabilidad**; el predictor lineal $\alpha_t+\mathbf{x}'\boldsymbol\beta$ vive
en toda la recta real. Hace falta un **enlace** que los conecte —como en cualquier GLM—, y aquí el
supuesto de proporcionalidad **elige uno concreto**: el **complementary log-log**,
$$
\operatorname{cloglog}(p)=\log(-\log(1-p))=\eta,\qquad p=1-\exp(-\exp(\eta)).
$$
A diferencia del logit y el probit, es **asimétrico** (se aproxima a 1 más despacio que a 0).

```{r}
#| label: fig-u15-enlaces
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-cap: "De los enlaces comparados en la Unidad 2, aquí retenemos solo logit y cloglog —los relevantes en supervivencia discreta—. La asimetría del cloglog (se aproxima a 1 más despacio que a 0) es lo que lo hace el enlace natural de los riesgos proporcionales."

tibble(eta = seq(-4, 4, by = 0.02)) |>
  mutate(logit   = plogis(eta),
         cloglog = 1 - exp(-exp(eta))) |>
  pivot_longer(c(logit, cloglog), names_to = "enlace", values_to = "p") |>
  ggplot(aes(eta, p, color = enlace)) +
  geom_line(linewidth = 1) +
  labs(x = expression(eta), y = expression(p == g^{-1}(eta)), color = "Enlace")
```

¿Por qué este enlace y no el logit? Porque **se deduce** del supuesto de riesgos proporcionales.
Si el tiempo de un modelo PH solo se observa **agrupado en intervalos**, la probabilidad de fallar
en el intervalo $t$ habiendo llegado en riesgo es, usando $S(t)=\exp\{-H(t)\}$,
$$
h_t(\mathbf{x})=1-\exp\!\bigl(-\Delta H_0(t)\,e^{\mathbf{x}'\boldsymbol\beta}\bigr).
$$
Despejando $1-h_t$, tomando $-\log$ y luego $\log$:
$$
\underbrace{\log(-\log(1-h_t))}_{\operatorname{cloglog}(h_t)}
=\underbrace{\log\Delta H_0(t)}_{\alpha_t}+\mathbf{x}'\boldsymbol\beta.
$$
Es decir: **al agrupar en el tiempo un modelo de riesgos proporcionales, el hazard discreto
satisface exactamente un modelo lineal con enlace cloglog**, con un intercepto por periodo
$\alpha_t$ que absorbe el riesgo base [@prentice1978; @allison1982]. Con `logit` se obtiene la
aproximación "logística discreta", razonable cuando el riesgo por periodo es pequeño, pero solo el
`cloglog` **preserva exactamente** los riesgos proporcionales subyacentes —y por eso reproduce los
**mismos hazard ratios que Cox**—.


### Ajuste en tiempo discreto e interpretación {#sec-u15-lab}

En formato **persona-periodo**, cada fila es un sujeto en un periodo en riesgo e `y` marca si
fracturó en ese periodo. Ajustarlo es, literalmente, un **GLM binomial con enlace cloglog** —de ahí
que sigamos dentro del marco—. (Aquí trabajamos sobre la cohorte, no sobre GLOW, por lo dicho en la
introducción: GLOW no tiene tiempo.) Construido el modelo **a ciegas**, ahora aprovechamos que la
cohorte es **simulada** para hacer algo que con datos reales no podríamos: **contrastar las
estimaciones con la verdad** del proceso generador —la validación que anticipábamos—.

```{r}
#| label: u15-ajuste
m_pp <- glm(y ~ periodo + x1 + x2, family = binomial("cloglog"), data = pp)
summary(m_pp)

exp(coef(m_pp)[c("x1", "x2")])               # hazard ratios estimados
exp(attr(cohorte, "verdad")$binaria$beta)    # HR verdaderos: e^0.85, e^-0.65
```

Los coeficientes de `periodo` son los **interceptos por periodo** $\alpha_t$, y aquí **crecen**
($0{,}39,\ 0{,}50,\ \dots,\ 0{,}78$ sobre el periodo 1): el modelo recupera el **riesgo base
creciente** del DGP. Los de `x1` y `x2`, exponenciados, son **hazard ratios**: `x1` da
$\widehat{\text{HR}}\approx2{,}32$, que **clava** la verdad ($e^{0{,}85}\approx2{,}34$), y `x2` da
$\approx0{,}64$, algo **atenuado** frente a su verdad ($e^{-0{,}65}\approx0{,}52$) —el efecto
protector se queda corto, en parte por omitir la fragilidad—. La **censura** no exige nada extra:
un sujeto censurado simplemente **deja de aportar filas** tras su último periodo en riesgo, sin un
`y = 1`; aporta lo que se observó (que no falló mientras estuvo) y nada más.

El riesgo base por periodo y la supervivencia que implica:

```{r}
#| label: fig-u15-hazard-base
#| fig-width: 10
#| fig-height: 4
#| fig-cap: "Riesgo base por periodo (h0t, evaluado en x1 = x2 = 0) y la supervivencia base St = prod(1 - h0k) que implica. En este DGP el riesgo base es creciente, de modo que la supervivencia cae cada vez más deprisa."
base <- tibble(periodo = factor(levels(pp$periodo), levels = levels(pp$periodo)),
               x1 = 0, x2 = 0)
base$h0 <- predict(m_pp, newdata = base, type = "response")   # hazard base por periodo
base$t  <- seq_len(nrow(base))
base$S  <- cumprod(1 - base$h0)                               # supervivencia base

pH <- ggplot(base, aes(t, h0)) +
  geom_col(fill = "grey70") +
  labs(x = "Periodo", y = expression(hazard~base~~h[0][t]), title = "Riesgo base por periodo")
pS <- ggplot(base, aes(t, S)) +
  geom_line(linewidth = 1) + geom_point(size = 2) +
  scale_y_continuous(limits = c(0, 1)) +
  labs(x = "Periodo", y = expression(supervivencia~~S[t]), title = "Supervivencia base")
pH + pS
```

**Fragilidad: el centro como efecto aleatorio.** Las pacientes se agrupan en clínicas; añadir un
**intercepto aleatorio por centro** —la *fragilidad*, en el lenguaje de la supervivencia— es,
otra vez, un **GLMM cloglog**, y reactiva la [Unidad 4](#sec-u14) sobre el reloj de supervivencia.
Y, como pedíamos, se ajusta **sin bayesiana**, con `lme4`:

```{r}
#| label: u15-fragilidad
library(lme4)
m_frail <- glmer(y ~ periodo + x1 + x2 + (1 | centro),
                 family = binomial("cloglog"), data = pp)
VarCorr(m_frail)               # sigma de la fragilidad (verdad del DGP: 0.70)
fixef(m_frail)[c("x1", "x2")]  # efectos fijos, ahora condicionales al centro
```

`VarCorr` estima la **varianza de la fragilidad**: $\hat\sigma_u\approx0{,}37$, **por debajo** de la
verdad ($0{,}70$) —la misma subestimación de las varianzas que con datos binarios y pocos grupos
(24 centros)—; mide cuánto varía el riesgo base de unas clínicas a otras. Al **condicionar al
centro**, los efectos crecen algo en magnitud ($x_1: 0{,}84\!\to\!0{,}89$;
$x_2: -0{,}45\!\to\!-0{,}47$): es el salto marginal→condicional de 1.4, ahora sobre el hazard,
aunque leve porque $\hat\sigma_u$ es pequeño.

### Cloglog frente a logit: ¿cuánto importa el enlace? {#sec-u15-logit}

Elegimos el `cloglog` por una razón **estructural**: preserva los riesgos proporcionales. Pero los
datos persona-periodo son binarios, así que **nada impide** ajustarlos con el enlace por defecto, el
**logit** —la *regresión logística de tiempo discreto*—. La diferencia es de **interpretación**: con
`cloglog` los $\exp(\beta)$ son **hazard ratios** (riesgos proporcionales); con `logit` son **odds
ratios** (un supuesto de *odds proporcionales*). Ajustemos los dos y comparémoslos:

```{r}
#| label: u15-logit
m_pp_logit <- glm(y ~ periodo + x1 + x2, family = binomial("logit"), data = pp)

# cloglog -> hazard ratios ; logit -> odds ratios
cbind(cloglog_HR = exp(coef(m_pp)[c("x1", "x2")]),
      logit_OR   = exp(coef(m_pp_logit)[c("x1", "x2")]))
c(AIC_cloglog = AIC(m_pp), AIC_logit = AIC(m_pp_logit))
```

La tabla los pone lado a lado, y la diferencia es **mínima**: para la fragilidad, HR $=2{,}32$
(cloglog) frente a OR $=2{,}44$ (logit); para el tratamiento, $0{,}64$ frente a $0{,}61$. El patrón
es el esperado —el **odds ratio se aleja un poco más de 1** que el hazard ratio (algo más alto cuando
el efecto es de riesgo, algo más bajo cuando es protector), porque con hazards por periodo
**moderados** odds y riesgo se parecen pero no son idénticos—. Y los **AIC casi empatan** ($1656{,}8$
con cloglog frente a $1657{,}3$ con logit), con una ventaja **mínima** para el cloglog. En estos
datos, el enlace **apenas mueve** los resultados.

```{r}
#| label: fig-u15-logit-cmp
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-width: 7
#| fig-height: 4
#| fig-cap: "Supervivencia base (x1 = x2 = 0) reconstruida desde el GLM con enlace cloglog y con logit. Las dos curvas son casi indistinguibles: con hazards por periodo moderados ambos enlaces dan prácticamente lo mismo. La preferencia por el cloglog es estructural (preserva los riesgos proporcionales y reproduce a Cox), no numérica."
base2 <- tibble(periodo = factor(levels(pp$periodo), levels(pp$periodo)), x1 = 0, x2 = 0)
pred2 <- base2 |>
  mutate(cloglog = predict(m_pp,       newdata = base2, type = "response"),
         logit   = predict(m_pp_logit, newdata = base2, type = "response")) |>
  pivot_longer(c(cloglog, logit), names_to = "enlace", values_to = "h") |>
  mutate(t = as.integer(periodo)) |>
  arrange(enlace, t) |>
  group_by(enlace) |>
  mutate(S = cumprod(1 - h)) |>
  ungroup()

ggplot(pred2, aes(t, S, color = enlace)) +
  geom_line(linewidth = 1) + geom_point(size = 2) +
  scale_y_continuous(limits = c(0, 1)) +
  labs(x = "Periodo", y = "Supervivencia base S(t)", color = "Enlace")
```

El gráfico lo confirma: la supervivencia base reconstruida con uno y otro enlace es **casi la misma
curva**. La conclusión es doble. Numéricamente, aquí **da casi igual**; pero conceptualmente la
elección **no es indiferente**: nos quedamos con el `cloglog` por lo que **significa** —es el único
que preserva los **riesgos proporcionales** y, por tanto, el que **reproduce a Cox** y permite leer
los efectos como hazard ratios—. El `logit` sería la opción si quisiéramos razonar en **odds**.

### GLM frente a Cox: comparativa y ventajas {#sec-u15-comparativa}

Con el GLM ya ajustado podemos **cerrar el círculo** con Cox. Los hazard ratios **coinciden**
—Cox daba $2{,}31$ y $0{,}64$; el GLM persona-periodo da $\approx2{,}32$ y $0{,}64$—, como debe
ser: ambos son modelos de riesgos proporcionales [@prentice1978]. La diferencia está en **cómo**
llegan ahí:

| Aspecto | **Cox** (semiparamétrico) | **GLM de tiempo discreto** (cloglog) |
|---|---|---|
| Hazard base $h_0(t)$ | **sin especificar**; no se estima para $\boldsymbol\beta$ | **modelado** por los interceptos de periodo $\alpha_t$ |
| Estimación | verosimilitud **parcial** (cocientes sobre el conjunto de riesgo) | verosimilitud **completa**, familia exponencial |
| ¿Es un GLM? | **No** (queda en la frontera del marco) | **Sí** (maquinaria IWLS estándar) |
| Empates (varios eventos a la vez) | aproximaciones (Breslow, Efron) | **exactos** (varias filas con $y=1$) |
| Predicción del riesgo base | indirecta (estimador de Breslow) | **directa** ($\hat h_{0t}$ por periodo) |
| Fragilidad / efectos mixtos | requiere extensiones (*frailty models*) | **dentro del marco**, con `glmer` |
| Efecto de covariables | $\exp(\beta)$ = hazard ratio | $\exp(\beta)$ = hazard ratio (**idéntico**) |

¿Qué **gana** el GLM en este problema? Se queda **dentro del marco** GLM —misma maquinaria, mismos
diagnósticos—; con tiempo **discreto y muchos empates** los trata de forma **exacta**, sin las
aproximaciones de Breslow/Efron que Cox necesita; **modela** el riesgo base ($\alpha_t$), lo que
permite **predecir el hazard y la supervivencia por periodo** de forma directa; y se **extiende**
sin salir del marco a la **fragilidad** por centro con `glmer` (un GLMM cloglog), reactivando los
efectos mixtos de la [Unidad 4](#sec-u14). ¿Qué gana Cox? **No** tiene que comprometerse con una
forma del riesgo base —útil cuando el tiempo es **continuo** y esa forma es desconocida o
irrelevante— y es el **estándar** universal. Por eso lo dejamos como **frontera** semiparamétrica
(Caso 3) y aquí trabajamos con el GLM. En una frase: **Cox = proporcionalidad + hazard base no
paramétrico**; **nuestro modelo = proporcionalidad + hazard base modelado por periodo**.

### Conclusiones para la práctica clínica {#sec-u15-clinica}

Traducidas a quien trata pacientes, las estimaciones del modelo dicen:

- **La fragilidad ósea manda.** Cada desviación típica de baja densidad mineral **multiplica por
  ~2,3** el riesgo de fractura en cada periodo: es el factor dominante. Identificar y vigilar de
  cerca a las pacientes de alta fragilidad es la palanca de mayor impacto.
- **El tratamiento ayuda, pero no basta por sí solo.** Reduce el riesgo **alrededor de un tercio**
  (HR $0{,}64$), un efecto **real y estable en el tiempo**, pero **parcial**: en pacientes muy
  frágiles el riesgo sigue siendo alto pese al tratamiento. Conviene combinarlo con el manejo de
  la fragilidad.
- **El riesgo no se relaja con el seguimiento.** El hazard base es **creciente**: la probabilidad
  de fractura por periodo aumenta con el tiempo, así que la vigilancia **no debe disminuir** en las
  revisiones tardías.
- **Importa el centro.** La *fragilidad* entre clínicas indica que el **riesgo basal varía de un
  centro a otro**: parte del riesgo individual depende del contexto asistencial, no solo de la
  paciente.
- **Un solo número resume el efecto.** Al cumplirse los riesgos proporcionales, el hazard ratio es
  **estable en los seis periodos**: una única cifra (×2,3 por fragilidad; ×0,64 por tratamiento)
  describe el efecto durante todo el seguimiento, lo que facilita comunicárselo a la paciente.

El siguiente gráfico hace tangible la decisión clínica: el **riesgo acumulado de fractura**
($1-S_t$) a lo largo de los periodos para tres perfiles de paciente.

```{r}
#| label: fig-u15-clinica
#| code-fold: true
#| code-summary: "Código de la figura"
#| fig-width: 8
#| fig-height: 4.2
#| fig-cap: "Riesgo acumulado de fractura (1 - St) predicho por el GLM cloglog para tres perfiles. La distancia vertical entre 'alta fragilidad sin tratar' y 'con tratamiento' es la reducción de riesgo absoluta atribuible al tratamiento; crece con el seguimiento. Los valores exactos se leen sobre el eje al renderizar."
arquetipos <- tibble(
  arquetipo = factor(c("Alta fragilidad, sin tratamiento",
                       "Alta fragilidad, con tratamiento",
                       "Fragilidad media, con tratamiento"),
                     levels = c("Alta fragilidad, sin tratamiento",
                                "Alta fragilidad, con tratamiento",
                                "Fragilidad media, con tratamiento")),
  x1 = c(1, 1, 0), x2 = c(0, 1, 1))

grid <- tidyr::expand_grid(periodo = factor(levels(pp$periodo), levels(pp$periodo)),
                           arquetipo = arquetipos$arquetipo) |>
  left_join(arquetipos, by = "arquetipo")
grid$h <- predict(m_pp, newdata = grid, type = "response")
grid <- grid |>
  mutate(t = as.integer(periodo)) |>
  arrange(arquetipo, t) |>
  group_by(arquetipo) |>
  mutate(S = cumprod(1 - h), riesgo_acum = 1 - S) |>
  ungroup()

ggplot(grid, aes(t, riesgo_acum, color = arquetipo)) +
  geom_line(linewidth = 1) + geom_point(size = 2) +
  scale_y_continuous(labels = scales::percent) +
  labs(x = "Periodo de revisión", y = "Riesgo acumulado de fractura (1 - S)",
       color = NULL) +
  theme(legend.position = "top")
```

Los números ponen cifras a la decisión: tras los seis periodos, una paciente de **alta fragilidad
sin tratar** acumula un riesgo de fractura de **≈68 %**; con tratamiento baja a **≈52 %** (unos
**16 puntos** menos); y si además su fragilidad fuera **media**, el riesgo caería a **≈27 %**. La
fragilidad pesa **más** que el fármaco —reducirla, cuando se puede, mueve más la aguja que el
tratamiento por sí solo—.

Estas conclusiones son **didácticas** —descansan en una cohorte simulada—, pero el **flujo** es el
real: de los hazard ratios y la curva de supervivencia a una recomendación accionable. Con datos
reales, el mismo modelo daría las cifras sobre las que decidir.

::: {.callout-important}
## Tarea del estudiante (1.5)
1. **Ajuste persona-periodo (RA7).** Ajusta el modelo cloglog sobre `pp`, reporta los **hazard
   ratios** de `x1` y `x2` (con su IC) y contrástalos con la verdad del DGP.
2. **Riesgo base.** Representa el **hazard base por periodo** ($\hat h_{0t}$) y la **supervivencia**
   $S_t$ que implica; comenta su forma (¿creciente el riesgo?, ¿se acelera la caída de $S_t$?).
3. **Censura.** Explica por qué, siendo **no informativa**, la censura no sesga la estimación: qué
   información aporta —y cuál no— un sujeto censurado.
4. **Fragilidad.** Añade `(1 | centro)` y compáralo con el ajuste sin fragilidad: interpreta
   $\sigma_u$ y comenta qué cambia (o no) en los efectos fijos y sus errores estándar.
5. **Cox y predicción.** Ajusta un Cox con las mismas covariables, compara sus hazard ratios con
   los del GLM y representa la **supervivencia predicha** para un par de perfiles de paciente;
   discute qué aporta cada enfoque.
:::

<!-- Partial: incluir con {{< include _unidad_1_5.qmd >}}. Sin YAML.
     Andamiaje de evaluación: actividades, instrucciones de la tarea y rúbricas.
     Los criterios exactos deben cuadrar con la §8 de la guía docente. -->

# 6 · Estudio de caso, exposición y evaluación {#sec-u16}

::: {.callout-note}
## Sobre esta unidad
Cierre del Caso 1: trabajo acompañado del estudio de caso y **exposiciones** con
Q&A individual. No hay teoría nueva; se integra y se evalúa todo lo anterior.
Esta unidad reúne la **maquinaria de evaluación** (actividades, instrucciones,
rúbricas). **RA implicados:** RA8 (desarrollar, presentar y defender estudios de
caso reproducibles) y, de forma acumulativa, RA1–RA7.
:::

## 6.1 El estudio de caso (entregable) {#sec-u16-caso}

<!-- TODO: enunciado del estudio de caso del grupo, que debe atravesar las tres
     "lentes" del Caso 1 sobre datos propios o asignados:
       - respuesta binaria (logística + interpretación + evaluación),
       - agrupamiento (intercepto aleatorio / extensión politómica),
       - tiempo-a-evento discreto (persona-periodo + cloglog).
     Indicar datos admisibles (origen y licencia) o el uso de la cohorte simulada. -->

## 6.2 Componentes de evaluación {#sec-u16-componentes}

Alineados con los instrumentos de la guía:

- **Informe (P).** Documento reproducible (`.qmd`) con el ciclo dato → modelo →
  diagnóstico → interpretación, en la escala correcta.
- **Reflexión (R).** Documento breve sobre decisiones de modelización y sus
  límites.
- **Exposición (E).** Presentación del estudio de caso.
- **Q&A individual (w).** Defensa individual ante preguntas.

## 6.3 Actividades a completar (cuestiones evaluables) {#sec-u16-actividades}

<!-- TODO: lista de cuestiones evaluables que el material deja "abiertas" a lo
     largo de 1.1-1.4 (las "Tarea del estudiante"), reorganizadas como entregables
     del caso. Conectar cada cuestión con el RA y el componente (P/R/E/w). -->

## 6.4 Rúbricas {#sec-u16-rubricas}

<!-- TODO: insertar las rúbricas EXACTAS de la guía docente (§8: informe P,
     reflexión R, exposición E, Q&A w). NO se reproducen aquí de memoria; se
     copian de la fuente. Pendiente: confirmar criterios y pesos con la guía. -->

::: {.callout-important}
## Pendiente de la fuente
Los criterios, niveles y pesos de las rúbricas deben tomarse literalmente de la
§8 de la guía docente. Este andamiaje deja los huecos marcados para rellenarlos
con esa fuente.
:::



# Reproducibilidad {#sec-repro}


```{r}
#| label: session-info
sessionInfo()
# renv::snapshot()   # fijar el estado del entorno al cerrar el caso
```


# Referencias {.unnumbered}

::: {#refs}
:::

